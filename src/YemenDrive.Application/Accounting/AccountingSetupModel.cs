namespace YemenDrive.Application.Accounting;

/// <summary>Deliberately empty: adds missing standard system accounts only, never rewrites existing accounts.</summary>
public sealed record AccountingSetupModel();
