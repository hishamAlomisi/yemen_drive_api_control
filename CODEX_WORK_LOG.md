# YemenDrive Work Log

> هذه السجلات تخص مرحلة البناء والاختبار فقط. كل قواعد البيانات والحسابات والمفاتيح والرموز وكلمات المرور وبيانات OTP تجريبية وغير مناسبة للإنتاج.

## 2026-09-15 — ربط Google Maps الحقيقي وتأمين مسارات المزود

- عدلت شرط العرض في AppGoogleMap: خلفية التطوير لا تعمل إلا عند تفعيل USE_DEMO_DATA=true، أما بناء Android العادي فيشغّل Google Maps SDK الأصلي. بعد بناء وتثبيت APK على MEmu ظهرت علامة Google؛ إذن الخلفية الثابتة لم تعد هي المستخدمة.
- فحص سجل SDK كشف رفض إعداد مفتاح Android من Google Cloud، لذلك ظلت بلاطات الخريطة فارغة. تحققت من اسم حزمة APK وبصمة شهادة debug المثبتة؛ كلاهما متوافقان مع تعريف التطبيق، ولا أسجل قيمة المفتاح أو أي بيانات سرية. يلزم مراجعة المفتاح الصحيح وتفعيل Maps SDK for Android في مشروع Google Cloud ثم الانتظار القصير لانتشار الإعداد إن لزم.
- راجعت صيغة local.properties بأمان فوجدت القيمة محاطة بعلامات اقتباس. جعلت Gradle ينظف الاقتباسات المفردة أو المزدوجة قبل إدراج المفتاح في Manifest، من دون قراءة أو طباعة القيمة. بُنيت وثبتت APK الجديدة؛ ظهرت بلاطات Google Maps على MEmu، وبعد مسح سجل الجهاز وإعادة تشغيل التطبيق لم يظهر خطأ تفويض Maps جديد. بقيت قيود الحزمة والبصمة مفعلة.
- أزلت من Flutter تعريفات مفاتيح Routes وPlaces وGeocoding وبصمة Android الخاصة بها. لا يوجد بعد ذلك طلب مباشر إلى Google REST من التطبيق.
- أضفت إلى API بوابة Google Maps داخلية وموديلات: MapRouteQuoteModel/report لمسار القيادة وMapLocationSearchModel/search أو report للبحث النصي والعنوان العكسي. البوابة تتحقق من الإحداثيات والمدخلات، وتعيد فقط المسافة والمدة وpolyline أو بيانات المكان العامة.
- مفاتيح خدمات Google REST محصورة في إعداد الخادم GoogleMaps (متغيرات بيئة أو User Secrets). أضفت معرف User Secrets إلى مشروع API من دون وضع أي قيمة فيه؛ لا يخزن المفتاح في DB أو ملفات المصدر أو Postman.
- خدمات المسارات والبحث والعنوان تطلب جلسة مستخدم صالحة قبل استدعاء البوابة، لمنع استهلاك حصة مزود الخرائط بطلبات مجهولة.
- وسعت عقد عميل Routes لقراءة المسافة بالمتر والمدة بالثواني مع polyline. يحفظ متحكم اختيار الموقع القيم الرسمية ويستخدمها متحكم الرحلة عند طلب التسعير، بدلاً من جمع مسافات خطية بين نقاط الرسم. بناء APK debug للعميل: ناجح.
- أوقفت API مؤقتاً لبناء الحل ثم أعدت تشغيلها. بناء الحل نجح بلا تحذيرات أو أخطاء، وبناء APK العميل وتثبيته نجحا. تحقق عقد API عند عدم وجود مفتاح خادم أعاد رمز إعداد آمن فقط، ولم يتصل بالمزود.
- اختبار حقل وجهة العميل على المحاكي كشف رسالة شبكة غير دقيقة عند غياب إعداد Places. صححت تصنيف الخطأ؛ يعرض العميل الآن أن بحث الأماكن غير مجهز في الخادم بدلاً من الادعاء بانقطاع الإنترنت، وبنيت وثبتت APK المصححة.
- لا Migration لهذه الدفعة. حدثت Postman بقسم Maps server proxy لطلبات اقتباس المسار والبحث والعنوان العكسي، وفحصت JSON بنجاح؛ لا يوجد مفتاح أو رمز في المجموعة.
- صححت أيضاً فلترة عميل محلية كانت تستبعد نتيجة صحيحة عند اختلاف لغة كتابة الاسم عن لغة نتيجة Google.
- في اختبار 2026-09-16 شُغل API بوضع Development ليحمّل User Secrets محلياً، وبعنوان متاح لمحاكي MEmu. نجح عرض الخريطة الأصلية. فحص آمن من الخادم أعاد HTTP 403 لكل من Places وRoutes مع عدم طباعة المفاتيح أو محتوى الاستجابات؛ إذن العائق المتبقي إعداد Google Cloud لا كود التطبيق.
- الإجراء المطلوب قبل متابعة الاختبار الحي: تفعيل Places API (New) صراحةً إضافة إلى Routes API وGeocoding API في المشروع نفسه، مراجعة Billing وقيود API للمفتاحين الخادميين، ثم الانتظار القصير لانتشار التغييرات. لا حاجة ولا يجوز إزالة قيد حزمة/بصمة مفتاح Android.

## 2026-09-15 — بدء تأمين شحن المحفظة

- راجعت شاشة الشحن الحالية قبل أي تعديل. اتضح أنها تجريبية: الوسائل ثابتة داخل التطبيق، وطلبها يضيف الرصيد فوراً عبر حركة محفظة عامة. لا يعتمد هذا في مسار مالي مكتمل.
- السياسة المعتمدة للتنفيذ: اختيار المبلغ لا ينفذ اتصالاً؛ اختيار وسيلة خارجية مفعلة فقط ينشئ طلب شحن معلّق. لا يزيد الرصيد ولا ينشر قيد قبل تأكيد الخادم. تستبعد وسيلتا النقد ومحفظة يمن درايف من الشحن.
- ستتاح في التطوير عملية تأكيد إداري موثقة بمرجع فريد، وتبقى واجهة مزود الدفع الحقيقي مرحلة ربط منفصلة لاحقاً.

## 2026-09-15 — إغلاق الإيداع المباشر وربط توريد مديونية السائق بالأستاذ العام

- ألغيت عملية الإضافة العامة لمحفظة المستخدم. مسار `WalletModel/add` يرفض الآن أي محاولة ويعيد رسالة توضح أن الرصيد لا يتغير إلا عبر دفع أو استرداد أو شحن مؤكد.
- قيدت قراءة محفظة مستخدم آخر بحساب الإدارة؛ الجلسة العادية تقرأ محفظة صاحبها فقط.
- أضفت حساب «الصندوق النقدي للمنصة» (`1000`) إلى الحسابات النظامية. عند تسجيل الإدارة لتحصيل مديونية سائق نقداً، ينشأ قيد متوازن: مدين الصندوق النقدي ودائن حساب السائق؛ وللتحويل يستخدم حساب تسوية الوسائل الخارجية (`1200`).
- أُبقيت مديونية السائق ودفعتها الأصلية غير قابلتين للتعديل. القيد الجديد مصدره إيصال التحصيل المستقل ويمنع تكرار المرجع.
- التحقق: اختبار التكامل المالي المعزول PASS، ويتضمن توازن قيد التحصيل واستخدام حسابي الصندوق والسائق. لا توجد بيانات حساسة في السجل ولا تحديث Postman مطلوب في هذه الخطوة.

## 2026-09-15 — تصحيح مصدر بيانات شاشة المحفظة

- كشف فحص شاشة العميل أن بطاقتي «الرصيد المتاح» و«إجمالي المصروفات» كانتا قيمتين تجريبيتين ثابتتين، رغم أن قائمة الحركات تستدعي API.
- عدلت المستودع والمتحكم لقراءة الرصيد الحقيقي والحركات من استجابة واحدة لـ`WalletModel/get`. تجمع المصروفات الآن كل حركة خارجة فعلياً، بينما يبقى الائتمان والتحرير والاسترداد حركات موجبة.
- عدلت حقل الاستعلام في لوحة الإدارة من مثال معرف نصي إلى معرف مستخدم رقمي، وأرسلت القيمة كرقم صحيح إلى API. هذا يعالج اختلاف الاستعلام الذي كان يؤدي إلى عدم قراءة محفظة العميل المقصود.
- لا تغير لعقد API أو Postman.

## 2026-09-15 — تطبيق Migration وإعادة تشغيل لوحة الإدارة

- أوقفت API قيد التشغيل، وأنشأت Migration `20260915140929_PaymentMethodCatalog` ثم بنيت المشروع بنجاح.
- تحقق EF أنه لا توجد تغييرات نموذج معلقة، وطُبق الترحيل على قاعدة التطوير بنجاح.
- أعيد تشغيل API من النسخة الجديدة وتأكدت استجابة صفحة الإدارة المحدثة. يمكن اختبار استعلام المحفظة باستخدام رقم المستخدم الرقمي من شاشة المستخدمين.

## 2026-09-15 — واجهة إدارة وسائل الدفع الخارجية

- أضفت بنداً وشاشة في لوحة الإدارة لإدارة طرق الدفع الخارجية. الشاشة تعرض هوية الوسيلة ومكان استخدامها وحالتها، ولا تعرض أو تخزن أي سر.
- نموذج الإضافة يشمل الرمز والاسم والوصف والشعار والنوع ورمز المزود غير السري وتعليمات العميل والترتيب والتفعيل وموضعي الظهور: دفع رحلة وشحن محفظة.
- تحققت من صياغة JavaScript وبناء API بلا أخطاء، ثم أعدت تشغيل API. لم يتغير عقد Postman في هذه الواجهة الإدارية لأن نموذج API أضيف في الدفعة السابقة.

## 2026-09-15 — ربط قائمة وسائل الدفع بتطبيق العميل

- استبدلت قائمة وسائل الدفع الثابتة بتحميل دليل `PaymentMethodModel` من API عند بدء التطبيق.
- التطبيق لا يعرض إلا الوسائل النشطة، ويراعي استقلال ظهور وسيلة الدفع للرحلة عن ظهورها لشحن المحفظة.
- أبقيت الشحن الخارجي غير منفذ مالياً: اختيار الوسيلة لا يضيف أي رصيد، إلى حين بناء طلب الشحن المعلق وصفحة الإكمال الآمنة وربط المزود لاحقاً.
- فحص Flutter لم يظهر أخطاء تجميع، ثم بُني APK debug وثُبت بنجاح على محاكي MEmu.

## 2026-09-15 — حفظ نقطة استئناف العمل المالي

- بناءً على توجيه مالك النظام، أُجلت حزمة استكمال الشحن الخارجي والاختبارات اليدوية والملخص المالي إلى ما بعد مرحلة الخرائط.
- لا تزال هذه الحزمة محددة: طلب شحن معلق وتأكيد إداري/مزود، صفحات إكمال آمنة، اختبار نقد ومحفظة كامل، تقرير إداري مالي، وإشعارات لحظية لاحقاً.

## 2026-09-15 — اختبار محاكي: زيادة التحصيل النقدي والتزام السائق

- بعد عودة جلسة السائق من إعادة تشغيل MEmu، تحققت من النتيجة عبر تطبيق العميل: الرحلة الأخيرة مكتملة، وأحدث حركة محفظة بعنوان «إرجاع المبلغ الزائد من الدفع النقدي» قيمتها +104 ر.ي. هذه نتيجة واجهة فعلية تثبت إضافة الزيادة للعميل.
- لا تغيير في Postman لهذه النتيجة، لأنها تحقق تشغيل لواجهات قائمة ولا تغير عقداً أو نموذجاً.
- نفذت دورة رحلة تجريبية فعلية على MEmu من العرض والقبول حتى إنهاء السائق للرحلة وتحقق العميل من التحصيل النقدي. كان المبلغ المقبوض أكبر من إجمالي الرحلة، وظهرت الزيادة كحركة إيداع صحيحة في محفظة العميل.
- أكد الاختبار أن قيد الأستاذ العام لتحصيل النقد متوازن ويربط الزيادة بحساب السائق ومحفظة العميل، لكنه كشف أن سجل `DriverSettlement` التشغيلي لا يضيف فرق الزيادة إلى مديونية السائق.
- صححت إنشاء التسوية: يبقى `PlatformCommission` لحصة المنصة (رسم الخدمة والعمولة المثبتة)، وتسجل الزيادة في `Adjustments`، و`NetPayable` يصبح سالب مجموعهما. هذا يجعل كشف مديونيات السائق مطابقاً للقيد المحاسبي ولا يغير أي دفعة أو قيد منشور سابق.
- وسعت اختبار التكامل المالي المعزول: تحصيل زائد يتوقع حصة منصة صحيحة وفرق زيادة في `Adjustments` وصافي مديونية مجمع، ثم يختبر تحصيل الإدارة لهذه التسوية من دون تغيير المصدر. النتيجة: PASS.
- حدثت شاشة تحصيل السائق لحساب الزيادة والمديونية المتوقعة فور تغيير مبلغ النقد، وبُني APK debug وثُبت على MEmu. إعادة التثبيت أعادت جلسة السائق إلى شاشة الدخول؛ لا تُسجل أي بيانات اعتماد في السجل أو المحادثة. يلزم تسجيل دخول يدوي واحد لإعادة اختبار العرض في الواجهة.
- أعيد تشغيل API من البناء الجديد وتحققنا من استماعه محلياً. لا تغيّر في العقود العامة أو مخطط البيانات، ولذلك لا تحديث مطلوب لمجموعة Postman.

## 2026-09-15 — استئناف الرحلة وتقريب الريال في المسار الفعلي

- أثناء اختبار رحلة تطويرية غير مكتملة، ظهر أن سجل العميل يعرض الطلب القادم لكنه لا يتيح الرجوع إلى تفاوضه. أضفت `resumeOpenRide` في متحكم الرحلات وزر «استئناف الرحلة» في بطاقة الرحلات القادمة. يسترجع الزر snapshot الرحلة من الخادم، يعيد مراقبة العروض إذا كانت مفتوحة، أو يعيد تتبع السائق إذا كانت مسندة.
- بُني APK العميل وثُبت، ثم ثبت الاختبار أن الرحلة المفتوحة عادت إلى شاشة التفاوض وظهر عرض السائق، وقُبل، واستمرت حالات السائق حتى شاشة تحصيل الدفع.
- عاينت واجهة السائق بعد إدخال مبلغ نقدي أعلى من الإجمالي: تظهر الزيادة لمحفظة العميل وإجمالي مديونية السائق بالقيمة نفسها قبل الحفظ. لم يُضغط زر التسجيل، لذلك لم تنشأ دفعة أو قيد أو مديونية جديدة من هذه المعاينة.
- اكتشف الاختبار أن بعض أسعار المسافات التجريبية تُنتج كسوراً عشرية بينما الواجهة تعرض الريال كاملاً. ثبتت سياسة YER بمبالغ صحيحة عند `Pricing` وعند قبول العرض: الأجرة، الرسوم، الإلغاء، العمولة والإجمالي تقرب إلى أقرب ريال بطريقة `AwayFromZero`. لا يؤثر ذلك في السجلات السابقة ولا يحتاج Migration.
- التحقق: بناء API بلا تحذيرات أو أخطاء واختبار التكامل المالي المعزول PASS. لا تغيير في Postman لأن عقود API لم تتغير.
- أعاد MEmu تشغيل نفسه أثناء الاعتماد وفقد تطبيق السائق الجلسة المحلية. بعد الدخول اليدوي، ظهر السجل النهائي للرحلة مكتملة، وتحققت من أحدث قيد زيادة في محفظة العميل (+104 ر.ي). لا يمكن نسبة النشر إلى نقرة بعينها قبل إعادة التشغيل، لذا يوثق السجل النتيجة المرصودة فقط.

## 2026-09-15 — إغلاق نقاط النقد والمحفظة المتبقية

- ربطت تحصيل النقد ودفع محفظة يمن درايف وإلغاء الرحلة المدفوعة بقيود الأستاذ العام المنشورة في المعاملة نفسها. يعاد كل شيء عند فشل أي جزء، ولا يمكن نشر القيد وحده أو تعديل القيد/الدفعة بعد اعتمادهما.
- القيود تستعمل الحسابين الفرعيين المرتبطين بنوع خدمة الرحلة (`رسوم الخدمة` و`تحصيل الخدمة`) لا الحسابين التجميعيين مباشرة. وتربط كل سطر بالرحلة والطرف المالي الملائم لتظهر في كشف الحساب.
- تحصيل النقد المتساوي أو الزائد أو الناقص بعد موافقة العميل يرحل من حساب السائق و/أو محفظة العميل، ثم رسوم الخدمة والتحصيل وعمولة السائق. لا ينشأ قيد عندما يبقى طلب العجز معلقاً أو يرفض العميل أو لا يكفي رصيده.
- الإلغاء النقدي المباشر يعكس الاسترداد بين تحصيل الخدمة وحساب السائق؛ وإلغاء النقد إلى المحفظة يعكسه بين تحصيل الخدمة ومحفظة العميل مع بقاء مديونية السائق التشغيلية. إلغاء دفع المحفظة يعكس الجزء المسترد وعمولة السائق المثبتة.
- تطبيق السائق لم يعد يغلق شاشة التحصيل عند إنشاء طلب موافقة العجز. يعرض حالة الانتظار ثم يتحقق دورياً من القرار ويظهر له مباشرة القبول أو الرفض أو عدم كفاية الرصيد.
- التحقق: بناء طبقة التطبيق والاختبارات نجح، واختبار التكامل المالي المعزول اجتاز سيناريوهات الدفع والإلغاء والقيود، وبُني APK debug للسائق. لا Migration مطلوبة لأن التغيير سلوكي ويستخدم الجداول المالية الموجودة.

## 2026-09-15 — إلغاء رحلة مدفوعة نقداً

- أضفت مسار إلغاء محمي للعميل صاحب الرحلة بعد تسجيل دفعة نقدية معتمدة. يمنع الخادم التكرار، ويحتفظ بالدفعة النقدية وسجل المديونية الأصليين بلا تعديل.
- «استعدته من السائق»: يوثق استرداداً منفصلاً وحالة إلغاء فقط؛ لا يغير المحفظة ولا ينشئ مديونية إلغاء إضافية.
- «أضفه إلى محفظتي»: يضيف المتبقي بعد رسم الإلغاء إلى محفظة العميل بحركة Refund مستقلة، وينشئ مديونية مستقلة مساوية على السائق لأن النقد بقي بحوزته.
- ربطت شاشة الدفع في تطبيق العميل: بعد تحقق التحصيل النقدي تظهر إمكانية إنهاء الرحلة أو فتح نافذة الإلغاء التي تشرح الخيارين ومبلغ الاسترداد المتوقع. وأضفت الخيار نفسه إلى سجل الرحلات المكتملة المدفوعة نقداً، فيبقى متاحاً بعد مغادرة شاشة الدفع.
- أضفت إشعاراً للطرفين وطلبَي Postman، ووسعت اختبار التكامل المالي المعزول ليغطي المسارين ومنع الإلغاء المكرر. اجتاز الاختبار بنجاح. لا Migration مطلوبة؛ لم يتغير مخطط قاعدة البيانات.

## 2026-09-15 — تصحيح توضيح تحصيل السائق

- صححت نص شاشة التحصيل النقدي في تطبيق السائق ليطابق الآلية المنفذة: عند النقص لا يوجد خصم تلقائي من محفظة العميل ولا إكمال للدفعة؛ ينشأ طلب موافقة، وبعد الموافقة فقط يعيد السائق تسجيل المبلغ نفسه.
- يوضح النص أيضاً أن الزيادة تضاف لمحفظة العميل وتبقى التزاماً على السائق إلى أن تُرحّل القيود المالية في خطوة الأستاذ العام القادمة.
- فحص Flutter لم يسجل خطأ تجميع جديد لهذا التعديل. توجد ملاحظات تنسيق وتحذيرات قديمة على مستوى المشروع، وليست أخطاء تنفيذية من هذا التغيير.

## 2026-09-15 — نقطة استئناف: الخرائط والعمليات المالية المتبقية

### الخرائط والمسار

- راجعت تطبيق العميل: Google Maps وGoogle Routes وPlaces وGeocoding مهيأة في الكود، لكن مفاتيحها غير مهيأة عمداً. في التطوير عند غياب Routes يرسم التطبيق خطاً مستقيماً بين النقطتين ويستعمل مسافة تقديرية لتسمح باختبار الحجز فقط.
- يعرض MEmu خلفية خريطة تجريبية بدلاً من Google Map الأصلية لمشكلة رسوميات المحاكي. يلزم جهاز حقيقي أو محاكي Google Play مناسب للتحقق البصري من الخريطة الفعلية.
- طريق الإنتاج المتفق عليه: التطبيق يطلب اقتباس مسار من API الموثق؛ API يتصل بمزود الخرائط ويحفظ Polyline والمسافة والمدة ووقت الحساب؛ التطبيق يرسل quoteId فقط عند إنشاء الرحلة؛ API يعيد التحقق ويحسب السعر من بياناته. لا تعتمد الرحلة على مسافة أو سعر يرسله العميل.
- يلزم إضافة بحث مكاني لسائقي المنطقة، وسياسة مواقع بالخلفية للسائق، وإيقاف مولد السائقين القريبين والإحداثيات التجريبية في الإنتاج.
- لا مفاتيح في الكود أو السجل. المطلوب إعداد Google Cloud من المالك: مشروع وفوترة وحدود، ثم APIs: Maps SDK Android وRoutes وPlaces New وGeocoding. مفاتيح Android والخادم منفصلة ومقيدة، والخادم وحده يحتفظ بمفتاح خدمات REST.

### مراجعة النقد والمحفظة

- السائق يسجل النقد، والعميل يتحقق من وجود التحصيل: منفذان.
- موافقة العميل عند تغطية النقص من المحفظة: منفذة في API وتطبيق العميل؛ لا خصم قبل الموافقة.
- إشعار الرفض/عدم كفاية الرصيد للسائق: API يسجله ويظهر عبر قائمة إشعارات السائق عند تحديثها؛ المتبقي حالة لحظية واضحة داخل شاشة التحصيل أو Push/SignalR.
- إلغاء الرحلة النقدية بخياري الاسترداد المباشر أو التحويل للمحفظة: غير منفذ.
- ربط الدفعات والتحصيل بقيود الأستاذ العام: غير منفذ. الحسابات الشخصية وحسابات كل نوع خدمة جاهزة الآن، ويجب استخدام حساب رسوم الخدمة وحساب تحصيل الخدمة المرتبطين بـServiceKind للرحلة في كل قيد لاحق.
- توجد ملاحظة واجهة يجب إصلاحها قبل الاختبار النهائي: شاشة تحصيل السائق تصف خصم العجز تلقائياً، بينما المنطق الحالي يتطلب موافقة العميل.

## 2026-09-15 — إنشاء وربط الحسابات المالية تلقائياً

- أضفت خدمة تجهيز مالية idempotent تتعامل مع الحسابات الجديدة والحالية من دون إنشاء قيود أو أرصدة افتتاحية مصطنعة.
- العميل: يُنشأ سجل المحفظة كالمعتاد، ثم جهة محفظة مالية وحساب أستاذ عام تابع لها. السائق: يملك حساب المحفظة ذاته، ويضاف إليه حساب ذمم سائق مستقل للعمولات والتحصيل النقدي.
- نوع الخدمة: يُنشأ له مالك مالي عام وحسابا رسوم الخدمة وتحصيل الخدمة منفصلان؛ لذلك لن تختلط إيرادات نوع نقل بإيرادات نوع توصيل عند بدء ترحيل القيود.
- ربطت هذه الآلية بثلاثة مسارات: تسجيل العميل من API، إضافة/تحويل المستخدم إلى سائق، وإضافة/تعديل نوع خدمة من لوحة الإدارة أو API. كما ربطت بيانات التطوير التجريبية بها.
- وسعت «تهيئة الحسابات الأساسية» لتصبح «تهيئة وربط الحسابات المالية»، وهي تمر على العملاء والسائقين وأنواع الخدمة الحالية وتكمل الناقص فقط. راجعت شاشة كشف الحساب بعد التشغيل وظهرت حسابات محافظ المستخدمين وحسابات السائقين وحسابا الرسوم والتحصيل لكل نوع خدمة قائم.
- التحقق: بناء API ناجح بلا تحذيرات أو أخطاء؛ لا تغيرات نموذج EF معلقة؛ Postman صالح JSON؛ اختبار التكامل المالي المعزول نجح في ربط البيانات السابقة وإنشاء حسابات عميل وسائق ونوع خدمة جديدين من دون تكرار.
- لا تزال عملية تحصيل الرحلة نفسها غير مربوطة بعد بقيود الأستاذ المنشورة؛ هذه الخطوة التالية، حيث ستستخدم الحسابات التي جُهزت هنا داخل معاملة التحصيل الذرية.

## 2026-09-15 — تجهيز واجهات موافقة العجز النقدي للتطبيقين

- طبقت `CashCollectionApproval` كطلب مستقل لحالة أن يكون النقد المقبوض أقل من إجمالي الرحلة مع رصيد محفظة كافٍ. الطلب يبدأ معلّقاً، ولا يخصم المحفظة ولا ينشئ دفعة عند إنشائه.
- قبول العميل يفحص الرصيد مرة ثانية ثم يعلّم الطلب مقبولاً ويرسل نتيجة للسائق؛ يعيد السائق تسجيل مبلغ التحصيل نفسه لإتمام الدفعة من نقطة مالية واحدة. الرفض أو الرصيد غير الكافي يتركان الرحلة بلا دفعة وبلا تغيير رصيد.
- ربطت تطبيق العميل بإظهار طلب الموافقة واتخاذ قبول/رفض، وربطت تطبيق السائق بإظهار انتظار موافقة العميل بدلاً من ادعاء نجاح التحصيل.
- أضيفت Migration `20260914194940_CashCollectionApproval` وتأكدت أداة الترحيلات أن قاعدة التطوير محدثة وأن نموذج EF لا يملك تغيرات معلقة.
- حدثت مجموعة Postman بقسم `11 - Cash Shortfall Approval` لطلبات الجلب والقبول والرفض، وفحصت صحة ملف JSON.
- الاختبار: نجح اختبار التكامل المالي المعزول؛ أثبت عدم الخصم قبل الموافقة، وعدم وجود دفعة قبلها، والخصم مرة واحدة بعد الموافقة وإعادة التحصيل، وعدم وجود تغيير عند الرصيد غير الكافي. لا عُرضت رموز أو كلمات مرور أو بيانات اتصال أثناء العمل.
- المتبقي لهذه الحزمة: بناء أحدث نسختين وتثبيتهما على MEmu، ثم تجربة الواجهة الفعلية بحساب عميل وسائق. ربط قيد الأستاذ العام بالتحصيل ما زال خطوة مالية لاحقة وغير مدّعى كمنجز هنا.

### إقلاع نسختي المحاكي

- بُني APK debug حديث لتطبيق العميل وAPK debug حديث لتطبيق السائق، وثُبت كلاهما على MEmu.
- فُتح التطبيقان بعد التثبيت وتحققنا من بقاء عمليتيهما نشطتين؛ لم تظهر استثناءات Flutter غير معالجة أو تعطل Android في فحص الإقلاع المختصر.
- تأكدت أيضاً من توفر مستمع API التطويري قبل الإقلاع. هذا فحص جاهزية فقط وليس دليلاً على إتمام عملية نقدية من الواجهة؛ التجربة التالية هي رحلة فعلية بعجز وموافقة/رفض من العميل.

## 2026-09-08

### نطاق العمل

- مراجعة تطبيق Flutter الموجود في `E:\Eqs\yemen_drive` مقابل عقود API الحالية.
- اختبار البناء والتشغيل والعقود بعد آخر تعديلات API.
- معالجة فجوات توافق التفاوض والعروض.

### ما تمت مراجعته

- `ApiClient` يستخدم المسار الموحد `POST /api/execute` ويضيف Bearer token تلقائياً.
- سجل الرحلات والأماكن المفضلة مرتبطان بعقود API، لكنهما ما زالا يرسلان معرف المستخدم في جسم الطلب؛ API تعطي هوية التوكن الأولوية.
- مستودع التفاوض في Flutter يستدعي `RideOfferActionModel` للقبول والرفض.
- مستودعات العروض والدعم والإحالات والمحفظة مربوطة جزئياً، بينما الملف الشخصي وتغيير كلمة المرور والاتصال ما زالت تستخدم منطقاً تجريبياً.
- تطبيق Flutter مضبوط افتراضياً على `USE_DEMO_DATA=false`، لكن بعض الشاشات تحتوي fallback أو تتبعاً تجريبياً للسائقين.

### تعديلات API

- إضافة `RideOfferActionModel` و`RideOfferAction` لدعم:
  - `accept` ببيانات `{ "rideId": 1, "offerId": 2 }`.
  - `reject` بالبيانات نفسها.
- إضافة عملية `reject` إلى `OperationsService` حتى تصبح العملية جزءاً من الخدمة الأساسية القابلة لإعادة الاستخدام.
- توسيع نتيجة `RideModel/get` لتشمل اسم السائق ونموذج المركبة ورابط الصورة داخل كل عرض.
- توحيد نتيجة `PromotionModel/list` بإضافة `discount` و`expiresAtUtc` و`description` لتطابق نموذج Flutter.
- تحديث مجموعة Postman وإضافة مجلد `07 - Account Services` لعروض الخصومات، الدعم، الإحالات، الإشعارات، وقبول/رفض العرض بعقد Flutter.

### التحقق

- `dotnet build YemenDrive.sln --no-restore`: نجح، 0 أخطاء و0 تحذيرات.
- تشغيل API من المصدر على `http://localhost:5080` نجح مع قاعدة `YemenDrive_Integration_20260908`.
- اختبار تسجيل دخول المدير نجح بالحساب التجريبي:
  - الهاتف: `700000001`
  - كلمة المرور: `Admin1234!`
- اختبار عقود الإدارة:
  - `ServiceKindModel/list`: نجح.
  - `PromotionModel/list`: نجح.
  - `NotificationModel/list`: نجح.
- `RideOfferActionModel/accept` بمعرف غير موجود: أعاد `offer_not_found` كما هو متوقع.
- ملف Postman تم تحميله عبر محلل JSON بنجاح بعد التعديل.
- محاولة `flutter analyze --no-pub` من المسار الخارجي لم تُرجع مخرجات قابلة للاعتماد من بيئة التنفيذ، ولم يتم اعتبارها اختبار نجاح.

### قيود وملاحظات

- صلاحية الكتابة لمجلد Flutter الخارجي `E:\Eqs\yemen_drive` لم تُمنح؛ تمت القراءة والتدقيق فقط، ولم تُحفظ تعديلات داخل ذلك المشروع.
- لا يمكن اعتبار ربط Flutter مكتملًا قبل السماح بالكتابة وتشغيل `flutter analyze` و`flutter test` وتشغيل التطبيق على جهاز/محاكي.
- البناء الأول أثناء التشغيل فشل بسبب عمليات `dotnet` قديمة كانت تقفل DLL، ثم نجح البناء بعد إيقاف العمليات العالقة.
- API تستخدم `EnsureCreated` في البيئة التجريبية؛ عند تغيير المخطط يجب إعادة إنشاء قاعدة الاختبار أو إضافة migrations لاحقاً.

### العمل المتبقي التالي

- استبدال مستودعات Flutter التجريبية للملف الشخصي وتغيير كلمة المرور والدعم والمحفظة والحذف بالكامل.
- إزالة `loadDemoNearbyDrivers` و`startDemoTracking` و`temporaryRouteBypass` من مسارات التشغيل عند توفير عقود المواقع والتتبع.
- تعديل مستودع Flutter للتفاوض ليستخدم عقد `RideOfferActionModel` الجديد، ثم إزالة الاعتماد على `customerId` من جسم طلب إنشاء الرحلة.
- إضافة طلبات Postman صريحة للعروض والدعم والإحالات والإشعارات والحذف وتغيير كلمة المرور.
- اختبار تسلسل كامل: تسجيل/OTP، إنشاء رحلة، عرض السائق، قبول العرض، حالات الرحلة، الدفع، سجل الرحلات، الأماكن، المحفظة، الدعم، حذف الحساب.

## متابعة 2026-09-08 بعد منح صلاحية الكتابة

### تعديلات Flutter المنفذة

- ربط الملف الشخصي فعلياً بعقدي `UserModel/get` و`UserModel/update` مع تحميل البيانات من الخادم وحفظها.
- ربط تغيير كلمة المرور بـ `POST /api/auth/password/change`.
- جعل حذف الحساب يرسل جسماً فارغاً ويعتمد على Bearer token، ثم ينهي الجلسة محلياً بعد نجاح الحذف.
- ربط نموذج التواصل مع `SupportTicketModel/add` بدلاً من الانتظار التجريبي.
- إزالة `userId` و`customerId` من أجسام طلبات سجل الرحلات، الأماكن المحفوظة، إنشاء الرحلة، وحفظ الوجهة؛ API تستخرج الهوية من التوكن.
- إزالة `userId: 2` من شحن المحفظة وربط المستودع بجلسة المستخدم الحالية.
- تعديل قبول ورفض عروض السائقين لاستخدام `RideOfferActionModel`، وإضافة دعم إلغاء الرحلة في API.
- تعطيل bypass المسار والتتبع التجريبيين عند `USE_DEMO_DATA=false`، مع إبقاء fallback متاحاً فقط عند تشغيل الوضع التجريبي صراحة.

### التحقق

- `flutter analyze lib/features/account lib/features/ride --no-pub`: لا توجد أخطاء؛ بقيت رسائل lint/info قديمة وتنسيقية فقط.
- `dart format` شُغّل على الملفات المعدلة.
- `dotnet restore --ignore-failed-sources` نجح باستخدام الحزم المحلية.
- `dotnet build YemenDrive.sln --no-restore`: نجح، 0 أخطاء و0 تحذيرات.
- اختبار endpoint تغيير كلمة المرور أعاد `invalid_current_password` عند كلمة مرور خاطئة، مما يثبت وصول الطلب للتحقق الحقيقي.
- ملف Postman صالح بصيغة JSON بعد الإضافة.

### ملاحظة تشغيل

- لم يتم تشغيل تطبيق Flutter على جهاز أو محاكي في هذه الجلسة؛ يلزم تنفيذ ذلك يدوياً أو عبر جهاز متصل للتحقق البصري وتدفق الشاشات.

### تصحيح اختبار Flutter

- اختبار `test/widget_smoke_test.dart` أصبح يهيئ `AppEnvironment` و`ApiClient` قبل فتح شاشة الدخول، بدلاً من الاعتماد على تهيئة التطبيق الكاملة.
- `flutter test --no-pub`: نجح، `All tests passed!`.
- `flutter build apk --debug`: نجح، والملف الناتج في `build/app/outputs/flutter-apk/app-debug.apk`.
- حُذفت تعريفات مستودعات `Demo*` و`Preview*` غير المستخدمة من Flutter، وأعيد تشغيل التحليل والاختبارات بعدها بنجاح.
- `flutter test --no-pub` بعد التنظيف: نجح، `All tests passed!`.
- `flutter analyze lib/features/account lib/features/auth lib/features/ride --no-pub` بعد التنظيف: لا توجد أخطاء، والرسائل المتبقية lint/info فقط.
- تم جعل البريد الإلكتروني اختيارياً في نموذج التواصل؛ عقد الدعم يحتاج هوية التوكن ورسالة البلاغ فقط.
- أُعيد إنشاء APK بعد إزالة المستودعات التجريبية: `flutter build apk --debug` نجح.

## متابعة 2026-09-09: بدء تطبيق السائق

- تمت مراجعة `CODEX_PROGRESS.md` و`CODEX_WORK_LOG.md` قبل بدء المرحلة الجديدة.
- تمت مراجعة بنية تطبيق العميل وعقود `DriverModel` و`DriverLocationModel` و`RideOfferModel` و`RideModel`.
- بدأ إنشاء تطبيق Flutter مستقل في `E:\Eqs\yemen_drive_driver` ليحتوي وظائف السائق فقط ويحافظ على نفس أسلوب الاتصال والتصميم العام.
- عدل تقرير الرحلات في `src/YemenDrive.Application/Rides/RideReport.cs`؛ السائق يرى الرحلات غير المسندة المفتوحة المطابقة لـ`ServiceKindId` و`ServiceCatalogItemId` في ملفه، كما يرى الرحلات المسندة إليه.
- جميع البيانات والحسابات والرموز المستخدمة في هذه المرحلة تجريبية وغير إنتاجية.

### نتيجة تنفيذ تطبيق السائق

- تم إنشاء مشروع Flutter مستقل في `E:\Eqs\yemen_drive_driver`.
- اتصال التطبيق مضبوط افتراضياً على `http://10.0.2.2:5080/api` لمحاكي Android، ويمكن تغييره عبر `--dart-define=API_BASE_URL=...`.
- شاشة الدخول تستعمل `/api/auth/login`، وتدعم `/api/auth/sign-in/verify-device` عندما يعيد الخادم تحدي OTP.
- لوحة السائق تستعمل `DriverModel/list` لاكتشاف ملف السائق، و`RideModel/list` لجلب الرحلات المفتوحة المطابقة للخدمة والرحلات المسندة.
- إرسال العرض: `RideOfferModel/add` مع `rideId` و`driverId` المستخرج من جلسة الدخول و`amount`.
- حالات الرحلة: `RideModel/update` بالقيم الرقمية الحالية `4` في الطريق، `5` بدأت، `6` مكتملة.
- الموقع: `DriverLocationModel/update`؛ الإحداثيات الحالية تجريبية مؤقتاً إلى حين ربط `geolocator`.
- تم تعديل `RideReport` في الـAPI لعرض الرحلات المفتوحة المناسبة للسائق.
- التحقق: `flutter analyze --no-pub` بلا أخطاء (10 رسائل lint/info فقط)، `flutter test --no-pub` نجح، `flutter build apk --debug` نجح، و`dotnet build YemenDrive.sln --no-restore` نجح بلا أخطاء أو تحذيرات.
- لم يتم تشغيل التطبيق على جهاز فعلي في هذه المرحلة؛ يلزم اختبار تسجيل الدخول بحساب سائق له `DriverProfile` موجود في قاعدة التجربة.
- خادم الـAPI يعمل حالياً على `http://localhost:5080`، واستجابة `/admin` تحققت بحالة HTTP 200.

### توحيد هيكل تطبيق السائق - 2026-09-09

- تمت مزامنة مشروع `E:\Eqs\yemen_drive` إلى `E:\Eqs\yemen_drive_driver` للحفاظ على نفس هيكل المشروع والأصول وملفات المنصات.
- تم اعتماد نفس معمارية GetX الموجودة في تطبيق العميل بدلاً من التطبيق الأولي المبسط.
- أضيفت الملفات المنظمة داخل `lib/features/driver`: المسارات، الـbindings، الـcontrollers، الـproviders، الـrepositories، الـmodels والـviews.
- تم تغيير المسار الابتدائي في `lib/app/app.dart` إلى شاشة دخول السائق، مع إبقاء هيكل العميل المشترك دون اختلاف تنظيمي.
- تم استبدال مراجع `package:yemen_drive` بـ`package:yemen_drive_driver` في ملفات المشروع والاختبارات.
- التحقق النهائي: `flutter test --no-pub` نجح، و`flutter build apk --debug` نجح، والتحليل الموجه لملفات السائق والتطبيق لم يظهر أخطاء.
- التحليل الكامل بعد المزامنة أظهر رسائل lint/info موروثة من كود العميل، ولم يظهر أخطاء بناء أو أخطاء Dart في ملفات السائق الجديدة.

### تنظيف واختبار API السائق - 2026-09-09

- حُذفت 41 مجلداً فارغاً من `lib` و`test`، وحُذف `main.dart` الجذري و`easy_ride_ar.iml` الزائدان.
- لم تُحذف مجلدات `build` و`.dart_tool` لأنها مخرجات Flutter المولدة، ولم تُحذف ملفات العميل غير الفارغة حتى يبقى الهيكل العام مطابقاً للمشروع الأصلي.
- الحساب التجريبي المضاف من لوحة التحكم: `userId=2`, `driverId=1`, الهاتف `775288883`, الخدمة «نقل / سيارة».
- تجربة API كاملة ناجحة: `DriverModel/list`، `RideModel/list`، `DriverLocationModel/update`، `RideOfferModel/add`، قبول العرض بواسطة الإدارة، ثم `RideModel/update` للحالات 4 و5 و6.
- التحقق بعد التنظيف: `flutter test --no-pub` نجح، و`flutter build apk --debug` نجح. التحليل الموجه لملفات السائق والتطبيق لم يظهر أخطاء، وبقيت رسائل lint/info فقط.

## متابعة 2026-09-09: استقبال طلبات العملاء والتفاوض في تطبيق السائق

### التعديلات

- أضيف مؤقت `Timer.periodic` في `E:\Eqs\yemen_drive_driver\lib\features\driver\controllers\driver_controller.dart` لاستدعاء `RideModel/list` كل 5 ثوانٍ بعد فتح لوحة السائق.
- يمنع `_refreshInFlight` إرسال طلب جديد أثناء تحديث سابق، ولا تختفي البيانات الحالية عند فشل تحديث دوري عابر.
- عدلت `RideReport` لإرجاع `driverOfferAmount` و`driverOfferExpiresAtUtc` للسائق.
- عدلت `RideOffer.AddAsync` لربط السائق بالتوكن، مع استثناء الإدارة، ولتحديث العرض المعلق عند تعديل السعر بدلاً من تكرار السجل.
- عدلت بطاقة الرحلة لعرض «عرضك الحالي» وتغيير الزر إلى «تعديل العرض» عند وجود عرض سابق.

### الاختبارات

- نجح `dotnet build YemenDrive.sln --no-restore` بلا أخطاء أو تحذيرات بعد إعادة تشغيل API.
- `RideModel/list` مع `Bearer demo-access-2` أعاد الرحلة المفتوحة رقم `2` و`driverOfferAmount=1300`.
- `RideOfferModel/add` أنشأ عرضاً بقيمة `1200` ثم حدّث نفس السجل إلى `1300` بالمعرف `2`.
- `flutter test --no-pub` و`flutter build apk --debug` نجحا.
- `flutter devices` لم يجد محاكي Android أو هاتفاً متصلاً.

### قرار تقني

- توثيق SignalR موجود، لكن لا يوجد Hub مفعّل في كود API الحالي؛ لذلك تم اعتماد polling كل 5 ثوانٍ كحل فوري. يمكن إضافة SignalR لاحقاً دون تغيير عقد الشاشة.

## متابعة 2026-09-12: وظائف السائق الأساسية في API الرئيسي وتطبيق السائق

### التنفيذ

- أضيفت ملفات `Communication/RideMessageModel.cs` و`RideMessage.cs` و`RideMessageReport.cs` لعقد محادثة الرحلة.
- يتحقق عقد الرسائل من أن المستخدم هو العميل أو السائق المسند، وأن الرحلة بعد الاتفاق، ويحدد الطرف الآخر تلقائياً من سجل الرحلة.
- أضيف إشعار قاعدة بيانات للسائقين المتاحين المطابقين عند إنشاء رحلة جديدة، وإشعار للطرف الآخر عند إرسال رسالة.
- أضيفت ملفات `DriverPayments/DriverCashPaymentModel.cs` و`DriverCashPayment.cs` للدفع النقدي وإنهاء الرحلة.
- منطق الدفع يضيف فرق الزيادة إلى محفظة العميل، أو يخصم فرق النقص منها عند كفاية الرصيد، ويرفض العملية دون تغيير الرحلة عند عدم كفاية الرصيد.
- قُيّد تغيير الحالات التشغيلية للسائق المسند فقط أو الإدارة.
- تطبيق السائق: أضيفت شاشة `driver_payment_view.dart`، وأضيف زر تحصيل الدفع، وأزرار المحادثة والاتصال.
- تم تحويل المحادثة إلى جلب دوري كل 4 ثوانٍ مع دمج الرسائل الجديدة دون تكرار، لأن Hub الفعلي غير متاح في API الرئيسي الحالي.
- أضيف عرض إشعارات السائق وعدد غير المقروء منها، وتضمين رقم هاتف العميل في `RideReport`.

### الاختبارات

- بناء حل API نجح: 0 أخطاء و0 تحذيرات.
- `RideMessageModel/list` مع توكن السائق أعاد استجابة ناجحة وقائمة رسائل فارغة للرحلة التجريبية المسندة.
- محاولة إرسال رسالة إلى رحلة مكتملة أعادت `chat_not_available` كما هو متوقع.
- `flutter analyze lib/features/driver --no-pub` لم يعرض أخطاء، والرسائل المتبقية lint/info موروثة أو تنسيقية.
- `flutter test --no-pub` نجح.
- `flutter build apk --debug` نجح.
- تم تصحيح قسم Postman للسائق ليتطابق مع عقود API الرئيسي، مع استبدال `DriverProfileModel` و`DriverRideModel` و`RidePaymentModel` بالعقود الفعلية.
- دورة دفع كاملة نجحت على الرحلة `3`: قبض `1600` مقابل أجرة `1500`، وإضافة الزيادة `100` إلى محفظة العميل وإكمال الرحلة.
- اختبار رفض النقص نجح على الرحلة `4`: قبض `1000` مقابل أجرة `1500` مع رصيد غير كافٍ، فأعيد `insufficient_wallet_balance` وبقيت الرحلة قيد التنفيذ.

### ملاحظات مفتوحة

- لم يتم بعد اختبار الدفع النقدي على رحلة قيد التنفيذ مع قاعدة بيانات الاختبار الحالية.
- لم يتم اختبار صلاحيات الهاتف والإشعارات على جهاز Android فعلي.
- المحادثة والإشعارات تعتمدان حالياً على polling، ويجب إضافة قناة فورية لاحقاً إذا تطلب الإصدار النهائي ذلك.

- أضيفت شاشة سجل مستقلة `driver_history_view.dart` ومسار `DriverRoutes.history` لعرض الرحلات السابقة المكتملة والملغاة الخاصة بالسائق.

## المرحلة 3.1: نسخة احتياطية قبل تعديلات قاعدة البيانات — 2026-09-12

- نُفذت نسخة `COPY_ONLY` احتياطية بنجاح لقاعدة `YemenDrive_Integration_20260908` باستخدام SQL Server `SQL2019`.
- النسخة محفوظة في مجلد نسخ SQL Server:
  `C:\Program Files\Microsoft SQL Server\MSSQL15.SQL2019\MSSQL\Backup\YemenDrive_Integration_20260908_before_stage3_20260912.bak`
- لم تُنفذ أي عملية حذف أو إعادة إنشاء للمخطط.
- محاولة النسخ إلى مجلد المشروع رُفضت بصلاحيات نظام التشغيل، لكن عملية النسخ الاحتياطي نفسها اكتملت بنجاح بنسبة 100%.

## المرحلة 3.2: تثبيت Migrations للمخطط — 2026-09-12

- أضيفت Migration رسمية باسم `20260912183308_Stage3SchemaBaseline` إلى مشروع قاعدة البيانات.
- أضيف `DesignTimeYemenDriveDbContextFactory` لاستخدام أدوات EF Core مستقبلاً دون الاعتماد على تشغيل API.
- تم التحقق من أداة `dotnet-ef` بالإصدار `9.0.9` وظهور Migration بحالة `Pending`.
- تم توليد Script SQL وتطبيقه على قاعدة منفصلة `YemenDrive_Migrations_Test_20260912`.
- نجح إنشاء المخطط من الصفر: 25 جدولاً وسجل واحد في `__EFMigrationsHistory`.
- لم يتم تغيير قاعدة العمل الحالية؛ تطبيق Migration عليها مؤجل حتى اكتمال خطة الترحيل الآمن للبيانات الموجودة.

## المرحلة 3.3: قيود المخطط وتكرار الدفع — 2026-09-12

### التنفيذ

- وُثقت Migration الفهارس الموجودة `20260912184039_Stage3IntegrityIndexes`: فهرس للرحلات حسب الحالة والخدمة، وفهرس فريد للعروض المعلقة، وفهارس للمحفظة والإشعارات والرسائل، وقيد فريد لمرجع الدفع.
- أضيف `IdempotencyKey` اختياري إلى `PaymentTransaction` وإلى عقدي `PaymentModel` و`DriverCashPaymentModel`.
- أضيف فهرس فريد مركب على `UserId` و`IdempotencyKey` عند وجود المفتاح.
- أصبحت عملية الدفع العادية تعيد العملية المسجلة عند إعادة إرسال المفتاح نفسه، كما تتعامل مع تعارض الإدخال المتزامن دون إنشاء سجل دفع إضافي.
- أصبح الدفع النقدي للسائق يعتمد مرجعًا ثابتًا لكل رحلة وسائق؛ الطلب المعاد أو المتزامن يعيد حالة العملية المسجلة بدلاً من تعديل الرصيد أو حالة الرحلة مرة ثانية.
- أُنشئت Migration `20260912191548_Stage3PaymentIdempotency` لهذا التعديل.

### التحقق

- `dotnet build YemenDrive.sln --no-restore`: نجح، 0 أخطاء و0 تحذيرات.
- تم توليد ومراجعة script EF Core قابل للتطبيق المتكرر ويشمل migrations المعلّقة.
- لم تُطبع أو تُسجل بيانات اتصال أو كلمات مرور أثناء هذه المرحلة.
- حُدّث ملف Postman: طلب الدفع النقدي للسائق يرسل `paymentIdempotencyKey`، ويمكن تكرار الطلب بالمفتاح نفسه للتحقق من أن الاستجابة تشير إلى العملية المعالجة سابقاً.

## المرحلة 3.4: تطبيق migrations على قاعدة العمل — 2026-09-12

### التحضير

- أُخذت نسخة احتياطية جديدة قبل التطبيق.
- كانت قاعدة العمل تحتوي 22 جدولاً دون `__EFMigrationsHistory`، لذلك لم يكن تطبيق الـbaseline مباشرة آمناً.
- قورنت القاعدة بمخطط اختبار تم بناؤه كاملاً من migrations. ظهر غياب جدولَي الدعم والإحالات، مع الفروق التي تعالجها migrations الفهارس ومنع التكرار.
- فُحصت البيانات الموجودة قبل إنشاء القيود الجديدة: لم توجد مجموعات متعارضة من عروض معلّقة أو مراجع دفع.

### التنفيذ والتحقق

- أُضيف سجل baseline للمخطط القائم، ثم طُبّقت migrations التالية بنجاح:
  - `Stage3IntegrityIndexes`
  - `Stage3PaymentIdempotency`
  - `Stage3ExistingDatabaseUpgrade`
- النتيجة النهائية: 25 جدولاً و4 سجلات في سجل EF Core.
- تحقّق وجود فهارس الرحلات والعروض والدفع ومنع التكرار، ووجود جدولَي `SupportTickets` و`ReferralRedemptions`.
- لم يحدث حذف للبيانات أو إعادة إنشاء للمخطط، ولم تظهر بيانات اتصال في سجل العمل.

## المرحلة 3.5: سلامة المعاملات المالية والتحويل الكامل إلى Migrations — 2026-09-12

### التنفيذ

- استبدلت تهيئة قاعدة البيانات في API والإعدادات بـ `MigrateAsync` بدلاً من `EnsureCreatedAsync`، لتعمل الترقيات الرسمية مع كل قاعدة جديدة أو قائمة.
- أضيفت معاملات SQL متسلسلة إلى قبول عروض السائقين، والمدفوعات، والتحصيل النقدي، وحركات المحفظة، مع معالجة تعارض التزامن ومنع اكتمال جزء من العملية فقط.
- أضيفت حماية `RowVersion` للمحفظة، وقيود على الرصيد السالب والقيم المالية غير الموجبة والأسعار السالبة.
- فُرض فهرس فريد مفلتر يمنع وجود عمليتي دفع نهائيتين للرحلة نفسها، وأصبحت عمليات الدفع النهائية غير قابلة للتعديل من الخدمة.
- عدّلت migration المالية لتكون آمنة عند اعتماد مخطط تطوير أُنشئ سابقاً دون migrations: لا تكرر عموداً أو فهرساً موجوداً، وتضيف القيود الناقصة فقط.
- حُدثت مجموعة Postman بطلب إعادة محاولة التحصيل النقدي بالمفتاح نفسه، لتوثيق اختبار idempotency عملياً.

### التحقق

- أُخذت نسخة احتياطية قبل اعتماد سجل migrations، ثم اجتازت النسخة الأحدث فحص سلامة SQL Server مع CHECKSUM.
- فحص ما قبل التطبيق لم يجد دفعات نهائية مكررة أو أرصدة سالبة أو قيماً مالية مخالفة.
- تم اعتماد الـbaseline ومراحل الفهارس ومنع التكرار وترقية الجداول للمخطط القائم، ثم طُبقت `20260912193419_Stage3FinancialIntegrity` بنجاح.
- النتيجة النهائية: 5 migrations مسجلة، و5 قيود مالية مفعلة، وعمود تزامن للمحفظة، وفهرس فريد للدفع النهائي. بقي عدد الدفعات النهائية المكررة والأرصدة السالبة صفراً بعد الترقية.
- نجح `dotnet build YemenDrive.sln --no-restore` بلا أخطاء أو تحذيرات.
- لم تُطبع أو تُخزن كلمات مرور أو مفاتيح أو قيم اتصال داخل سجل العمل.

## المرحلة 3.1: جرد المخطط والاستخدام الفعلي — 2026-09-12

- تمت مطابقة `YemenDriveDbContext` مع خدمات API ولوحة الإدارة وتطبيقي العميل والسائق، في قراءة فقط ومن دون كشف قيم الاتصال.
- المخطط الحالي يحوي 24 جدول تشغيل: 17 مستخدماً مباشرة و7 محفوظة لقرارات مالية أو أمنية أو تتبع مستقبلية.
- أضيف `DATABASE_SCHEMA_AUDIT.md` كمرجع للعلاقات والحقول التشغيلية والفهارس والقرارات المؤجلة.
- لا توجد توصية بحذف أي جدول أو عمود في هذه المرحلة.

## تكامل مسار الرحلة: الدفعة 1 — 2026-09-12

- تمت مراجعة Flow العميل الحالي والـAPI قبل التعديل. اتضح أن الترتيب المعتمد في Flutter مناسب، وأن الفجوات كانت في عقود التتبع وربط المحادثة والإشعارات لا في ترتيب الشاشات.
- أضيفت خدمة `RideLocationModel` وسجل المواقع المحمي، وأعيدت بيانات السائق وموقعه الحي في استجابة تفاصيل الرحلة.
- أضيفت إشعارات أحداث حقيقية للعروض وقبولها وتغير الحالة، وربطت Flutter بها وبالمحادثة بدلاً من البيانات الثابتة.
- استُخدم polling كل 4 ثوانٍ لتحديث موقع السائق، لعدم وجود SignalR Hub فعّال في API الحالي؛ لا توجد إحداثيات محاكاة في وضع البيانات الحقيقية.
- لا يشمل هذا الجزء الدفع، ولا يغيّر مسار الشاشات القائم.

## تكامل مسار الرحلة: الدفعة 2 — 2026-09-12

- أضيفت حماية منع التكرار للرحلة وربطت بمفتاح Flutter الموجود مسبقاً، ثم طُبقت migration المقابلة على قاعدة العمل.
- حُولت المحادثة والإشعارات إلى تحديث دوري ببيانات حقيقية بسبب غياب SignalR Hub الفعّال، مع إلغاء البيانات التجريبية في هاتين الوظيفتين.

## تكامل مسار الرحلة: الدفعة 3 — 2026-09-12

- رُبطت شاشة ملخص الرحلة بعناوين الرحلة والمسافة والسعر الفعلية، وحُفظت المسافة في طلب الإنشاء.
- أصبح الاتصال يعتمد رقم السائق القادم من API، وأزيلت سجلات استجابات الخرائط التشخيصية.
- أضيفت طلبات Postman للموقع وإتمام الرحلة قبل الدفع، واجتاز ملف المجموعة فحص JSON.

## مسار السلامة: تسجيل صوتي مباشر — 2026-09-12

- نُفذ تسجيل السلامة بموافقة صريحة لكل رحلة، مع رفع مقاطع صوتية إلى API أثناء التسجيل وحفظها خارج الملفات العامة.
- أضيفت جلسة تسجيل محمية بالصلاحية وبحالة الرحلة، وبصمة محتوى وبيانات موافقة وحجم ووقت آخر مقطع، ثم طُبقت migration المقابلة على قاعدة التطوير.
- يتوقف التطبيق ويبلغ المستخدم عند فشل الرفع ولا يزعم نجاح حفظ تسجيل غير مؤكد.
- أضيفت أذونات الميكروفون اللازمة، ووثقت طلبات البداية والإغلاق في Postman. نجح بناء API وتحليل Flutter بلا أخطاء تجميع.

## حالة الإغلاق قبل اختبار دورة الرحلة — 2026-09-12

- حالة التنفيذ: الربط البرمجي لمسار العميل حتى شاشة الدفع جاهز، لكن اختبار دورة عميل وسائق الفعلية واختبارات التزامن لم تنفذ بعد.
- أضيف Postman طلب إعادة الرحلة بالمفتاح نفسه للتحقق من idempotency. اختبار العرضين المتزامنين والحالة غير المسموحة سيكونان ضمن جلسة الاختبار التالية.
- التسجيل الصوتي المنفذ لا يغلق مسار السلامة كاملاً؛ ما زالت جهات الطوارئ وبلاغات الطوارئ والمشاركة الحية مطلوبة لاحقاً.

## اختبار تكامل API لدورة الرحلة — 2026-09-13

- اجتاز API دورة رحلة معزولة كاملة: إنشاء مع منع تكرار، موقع سائق، عرضان، قبول متزامن لعروض متنافسة، رفض محاولة عميل لتغيير الحالة، محادثة، انتقالات السائق حتى الاكتمال، ثم حد الدفع.
- نتيجة التزامن كانت قبول عرض واحد فقط. لم ينفذ الدفع.
- لا يزال اختبار واجهتي Flutter على جهازين مستقلاً مطلوباً للتحقق من الخرائط والأذونات والتسجيل الصوتي بصرياً وفعلياً.

- فُحصت الإشعارات والمحادثة: يمنع API الرسالة قبل تعيين السائق، ثم ينجح إشعار العرض وإشعار القبول والمحادثة بعد التعيين.
- اجتاز تحليلا Flutter للعميل والسائق بلا أخطاء تجميع. حُدث Postman لرموز العميل/السائق الصحيحة، وأضيف اختبار رفض الحالة من العميل؛ ملف المجموعة صالح JSON.

## Seeder لبيانات التطوير — 2026-09-13

- أضيف Seeder إداري محصور في بيئة التطوير، غير هدمي وقابل للتكرار، لتجهيز بيانات خدمات وسائقين وعميل ورحلات وعروض ومواقع ورسائل وإشعارات.
- شُغل بنجاح مرتين على قاعدة التطوير للتأكد من عدم مضاعفة البيانات، ووثق طلب تشغيله في Postman.

## حالة التوقف والخطوة التالية — 2026-09-13

- أُغلق اختبار API بنجاح، لكن اختبار واجهات العميل والسائق الحي على جهازين/محاكيين لم يبدأ بعد.
- الاستئناف يبدأ باختبار المسار من الواجهتين: رحلة، عروض، قبول، موقع، محادثة، إشعارات، حالات حتى الدفع، ثم صلاحية تسجيل السلامة على جهاز فعلي. الدفع خارج النطاق.

## بدء اختبار واجهات Android — 2026-09-13

- جُهز محاكي Android للاختبار. فُصل معرّف Android لتطبيق السائق عن تطبيق العميل لأن المعرّف السابق كان مشتركاً ويمنع تثبيتهما معاً.
- التحليل البرمجي للتطبيقين ناجح. بناء APK العميل ما زال قيد التنفيذ، ولم يبدأ اختبار واجهة أو تسجيل دخول أو قبول إذن حتى الآن.

## تقدم اختبار المحاكي — 2026-09-13

- تأكد اتصال المحاكي، وثُبت APK العميل وفتحت شاشة تسجيل الدخول بنجاح؛ لا يوجد تعطل في الإقلاع.
- ظهر أن APK السائق القديم، الذي بُني قبل فصل المعرّف، كان يستبدل نسخة العميل عند تثبيته. هذا أثر بناء قديم وليس نتيجة لاختبار مسار التطبيق؛ بدأ إعادة بناء نظيفة للعميل والسائق قبل إعادة تثبيتهما معاً.
- لم تُدخل أي بيانات تسجيل دخول ولم تُقبل أي صلاحية موقع أو ميكروفون. يبقى تسجيل الدخول والأذونات الحساسة للمستخدم أو لموافقة صريحة منه.
- اكتمل البناء النظيف وثُبت التطبيقان معاً على المحاكي بمعرّفين منفصلين. فُحص إقلاع كل تطبيق ووصوله إلى نشاطه الرئيسي بنجاح.
- صُحح مسار إقلاع Android لتطبيق السائق ليتوافق مع معرّفه الجديد؛ كان التعديل السابق للمعرّف كافياً للبناء لكنه لم يحدّث فئة النشاط الرئيسي، فكانت نسخة السائق لا تقلع. لا توجد بيانات حساسة أو إعدادات اتصال ضمن هذا التعديل.
- لم يبدأ اختبار دورة الرحلة الواجهاتي بعد لأنه يتطلب تسجيل دخول يدوي بحسابي العميل والسائق؛ لا تدخل الأداة بيانات اعتماد ولا تقبل أي أذونات حساسة تلقائياً.

## تصحيح رسالة تسجيل الدخول — 2026-09-13

- عُولجت استجابة تسجيل الدخول الفاشلة في تطبيق العميل: كان التطبيق يحاول قراءة رموز جلسة من رد API الفاشل، فيعرض رسالة تقنية مضللة بدلاً من رسالة سبب الرفض.
- أصبح التطبيق يعرض رسالة API الآمنة للمستخدم (مثل خطأ بيانات الدخول) مع الإبقاء على رد OTP أو رمز الجلسة الصحيحين كما هما.
- بُني APK العميل المحدّث وثُبت وشُغل على المحاكي بنجاح. لم يلزم تعديل API أو Postman.

## توحيد صيغة رقم الهاتف — 2026-09-13

- تأكد وجود اختلاف بين لوحة التحكم وتطبيق العميل: اللوحة وبيانات التطوير تحفظ الرقم المحلي، فيما كان العميل يضيف مفتاح اليمن عند الإرسال.
- وحّدت تطبيق العميل على الرقم المحلي في تسجيل الدخول وإنشاء الحساب واستعادة كلمة المرور، مع بقاء مفتاح الدولة في الواجهة للعرض والإدخال الطبيعي.
- بُني APK العميل وثُبت وشُغل على المحاكي بعد التعديل. لا يلزم تعديل API أو Postman لهذه المعالجة.

## تصحيح قائمة الجنس في الملف الشخصي — 2026-09-13

- عولج تعطل شاشة تعديل الملف الشخصي عند غياب قيمة الجنس أو وصول قيمة غير معروفة من API؛ كانت القائمة تستقبل قيمة فارغة لا تقابل أي خيار.
- أصبحت القيمة غير المعروفة تظهر كحالة غير محددة، ويستطيع المستخدم اختيار قيمة صالحة لاحقاً وحفظها.
- بُني APK العميل وثُبت وشُغل على المحاكي بعد التعديل. لا يلزم تعديل API أو Postman.

## تصحيح الانتقال بعد اختيار الخدمة — 2026-09-13

- عولج خطأ Flutter عند الضغط على التالي بعد اختيار نوع الخدمة؛ كانت شاشة اختيار الموقع تحدّث حالة مراقبة أثناء بناء المسار الجديد.
- أُجل تحديث حالة البدء التلقائي إلى ما بعد اكتمال الإطار مع التحقق من بقاء الشاشة مركبة، لمنع استدعاء بناء متداخل.
- بُني APK العميل وثُبت وشُغل على المحاكي بعد التعديل. لم يُفتح طلب إذن موقع ضمن الاختبار الآلي.

## سياسة ربط Controllers لمسارات الحساب — 2026-09-13

- عولج تعطل `ProfileController not found` بعد تسجيل الدخول: كان مسار الملف الشخصي يستخدم الربط العام بدلاً من `ProfileBinding` المالك للـController.
- صُحح أيضاً ربط مسار العروض، وجُعل `ProfileBinding` قابلاً لإعادة إنشاء Controller ومستودعه بأمان عند العودة إلى المسار.
- أضيفت سياسة مكتوبة لربط Controllers واختبار آلي يحرس ربط مساري الملف الشخصي والعروض؛ اجتاز الاختبار بنجاح.
- بُني APK العميل وثُبت وشُغل على المحاكي. لا يلزم تعديل API أو Postman.

## استكمال حماية Controller داخل الشاشة الرئيسية — 2026-09-13

- بعد إعادة التحقق ظهر أن أيقونة حسابي تعرض الملف الشخصي داخل `MainShell`، فلا تمر عبر مسار الملف الشخصي المنفصل.
- أضيف `ProfileBinding` إلى ربط الشاشتين الرئيسيتين، لذلك يتوفر `ProfileController` قبل بناء الصفحة المدمجة.
- وسّع اختبار ربط المسارات ليغطي الـMain Shell، واجتاز بنجاح. بُني APK العميل وثُبت وشُغل على المحاكي.

## مسار تقديري لاختبار الحجز دون مزود الخرائط — 2026-09-13

- أضيف بديل مسار محصور ببيئة غير الإنتاج وعند غياب إعداد مسارات الخرائط: يرسم خطاً بين نقطتي الانطلاق والوجهة اللتين اختارهما المستخدم، فيحسب التدفق المسافة منه ويُفعّل الانتقال لبقية خطوات الحجز.
- يظهر تنبيه صريح بأن المسار تقديري للتطوير وليس مسار قيادة فعلياً.
- يتعطل البديل تلقائياً عند الإنتاج أو عند إعداد مزود مسارات حقيقي؛ لم يُغيّر API أو Postman.
- بُني APK العميل وثُبت وشُغل على المحاكي.

## تصحيح استلام عروض السائقين في المفاوضة — 2026-09-13

- قُوي قارئ العروض الدورية في تطبيق العميل ليقرأ أي كائن JSON صالح صادر من API بدلاً من الاعتماد على نوع خريطة مقيد قد يمنع ظهور العرض.
- أصبح فشل استعلام التحديث يعرض رسالة واضحة بدلاً من إبقاء شاشة العميل في حالة بحث صامتة.
- بُني APK العميل وثُبت وشُغل على المحاكي؛ لا يلزم تعديل API أو Postman.

## منع وميض العروض المنتهية وتصحيح العدّ — 2026-09-13

- شُخّص وميض العرض على أنه عرض منتهٍ أو معالَج ما زال يعود ضمن سجل الرحلة؛ لا توجد آلية رفض تلقائي قصيرة في تطبيق العميل.
- أصبح تطبيق العميل يستبعد العناصر غير المعلّقة والمنتهية قبل بنائها في الواجهة، ويعرض عدّاً متوافقاً مع الصلاحية الفعلية.
- وسّعت استجابة API بوقت إنشاء العرض لدعم حساب النسبة الدقيقة للعدّ. لا تعديل مطلوب لملف Postman.
- تحقق بناء طبقة التطبيق، وبُني APK العميل وثُبت على المحاكي. أبقيت عملية API التي يشغلها المستخدم دون إيقاف؛ يلزم إعادة تشغيلها لاحقاً لتطبيق امتداد الاستجابة الخلفية.

## تصحيح تفسير توقيت صلاحية العرض — 2026-09-13

- عولج فرق التوقيت بين تاريخ صلاحية UTC غير الموسوم وتوقيت جهاز المحاكي، وهو ما كان يجعل فلترة العميل تستبعد عرضاً جديداً خطأً.
- أُعيد بناء تطبيق العميل وتثبيته وتشغيله على المحاكي. لا تغيير مطلوب في Postman.

## منع تعطل MEmu بعد قبول عرض — 2026-09-13

- أكد سجل MEmu أن Google Maps فشلت في إعداد EGL عند فتح شاشة متابعة السائق بعد القبول؛ لا علاقة للانهيار بعملية قبول العرض.
- أضيف بديل متابعة آمن محصور بالتطوير التجريبي، مع إبقاء خريطة الإنتاج الحية كما هي.
- بُني تطبيق العميل وثُبت وشُغل على المحاكي. لا تعديل مطلوب في API أو Postman.

## تعميم بديل الخرائط الآمن على MEmu — 2026-09-13

- ثبت أن عطل EGL في MEmu يصيب كل شاشات Google Maps، ومنها الانتقال من تأكيد المسار إلى المفاوضة.
- أصبح `AppGoogleMap` يستخدم خريطة تقديرية تفاعلية في تطوير البيانات التجريبية فقط، مع بقاء الخريطة الحية في الإنتاج.
- بُني تطبيق العميل وثُبت وشُغل على المحاكي. لا تغيير مطلوب في API أو Postman.

## تصحيح شرط خريطة التطوير مع API محلي — 2026-09-13

- كان شرط بديل الخريطة يعتمد خطأً على بيانات العرض، مع أن الاختبار يستخدم API محلياً وبيانات تجريبية حقيقية في آن واحد.
- أصبح الشرط يعتمد بيئة `dev`، فتتعطل Google Maps الأصلية على MEmu عبر كامل تدفق الحجز دون التأثير على الإنتاج.
- بُني تطبيق العميل وثُبت وشُغل على المحاكي. لا تغيير مطلوب في API أو Postman.

## حزمة التسعير والنوع الافتراضي — بدء التنفيذ 2026-09-13

- بدء تنفيذ طلب المستخدم: نوع خدمة افتراضي واحد نشط تضبطه الإدارة، ورسم خدمة ثابت لكل قاعدة تسعير.
- ستثبت أجرة العرض المقبول ورسم الخدمة في سجل الرحلة، ثم تستخدم شاشة الدفع الإجمالي المثبت بدلاً من القيم التجريبية الثابتة.
- العمل يشمل قاعدة البيانات وMigration وAPI ولوحة الإدارة والعميل والاختبارات وPostman عند الحاجة.

## حزمة التسعير والنوع الافتراضي — استكمال 2026-09-13

- أنشئت Migration `DefaultServiceKindAndServiceFee` ونُفذ بناء طبقة التطبيق بنجاح.
- أضيفت إدارة نوع افتراضي واحد نشط ورسوم خدمة ثابتة لقاعدة التسعير، وثُبتت مبالغ العرض المقبول والرسم والإجمالي على الرحلة.
- استبدلت شاشة الدفع القيم التجريبية ببيانات العرض المقبول ورسم الخدمة. بُني العميل وثُبت وشُغل على MEmu.
- لم تتغير أجسام طلبات Postman؛ تغيرت الاستجابات فقط.

## زر تعبئة بيانات التطوير — 2026-09-13

- أضيف تحكم إداري واضح لتشغيل Seeder التطويري من لوحة التحكم، بتأكيد مسبق وعدم حذف للبيانات الحالية.
- الزر محصور ببيئة التطوير ويتطلب مصادقة مسؤول. اجتاز مشروع API البناء بنجاح.

## إصلاح تبديل نوع الخدمة الافتراضي — 2026-09-13

- عولج ترتيب التحديث عند نقل النوع الافتراضي: إلغاء الافتراضي السابق يتم مباشرة قبل تعيين الجديد داخل معاملة واحدة، حتى لا يصطدم بالقيد الفريد للنوع الافتراضي النشط.
- بُني API بنجاح بلا تحذيرات أو أخطاء وأعيد تشغيله على بيئة التطوير.
- لم يتغير عقد الطلب أو الاستجابة، لذلك لا تعديل مطلوب في مجموعة Postman.

## تصحيح إعادة البحث وحالة عرض السائق — 2026-09-13

- استبدل مسار «إعادة البحث» إنشاء طلب جديد بإعادة الاشتراك في تدفق عروض الرحلة الحالية، مع تصفير حالة العرض في الواجهة فقط.
- أُضيفت حالة آخر عرض للسائق إلى تقرير الرحلات؛ تستخدمها واجهة السائق لمنع أي تعديل بعد القبول أو الرفض أو الانتهاء، مع تسمية الحالة للمستخدم.
- اجتاز بناء API وبناء APK لكلا التطبيقين. لم يكن MEmu متصلاً عبر ADB وقت محاولة التثبيت، لذلك لم يجرِ تثبيت تلقائي.

## استئناف الرحلة عند إعادة فتح تطبيق العميل — 2026-09-13

- أضيف فحص وحيد عند بدء التطبيق لآخر رحلة مفتوحة من تقرير الرحلات، مع استخدام حالة الخادم لتقرير الشاشة المناسبة.
- تستعاد تفاصيل الطريق في وحدة الموقع وتستأنف مراقبة العروض للرحلات المفتوحة، أو مراقبة السائق للرحلات التشغيلية.
- اجتاز بناء APK العميل؛ لا تغيير تعاقدي يستلزم تحديث Postman.

## حفظ قرارات مرحلة الدفع والتسوية للجلسة التالية — 2026-09-13

- وثقت قواعد العمل المعتمدة: النقدي يحمّل السائق مديونية العمولة، والزيادة تقيد بمحفظة العميل، والعجز يستكمل من المحفظة عند توفر الرصيد أو نقداً.
- الدفع غير النقدي يوزع صافي مستحق السائق بعد عمولته، مع تسجيل رسم الخدمة وعمولة السائق للمنصة. العمولة قابلة للضبط حسب الخدمة وبصيغة نسبة أو قيمة ثابتة أو كليهما.
- ستنفذ سياسة إلغاء تحفظ الرسوم وتعاد بقية الدفعة فقط، مع منع التكرار، قيود مالية غير قابلة للحذف، ومعاملات قاعدة بيانات واختبارات متزامنة.
- لم يبدأ تنفيذ هذه المرحلة؛ حفظت المتطلبات كما طلب المستخدم لاستئنافها غداً.

## الدفع والمحفظة والتسوية — بدء التنفيذ 2026-09-14

- بدأ الجرد من منطق التحصيل النقدي والمحفظة والدفعات وقيود منع التكرار. تبين أن البنية الأساسية موجودة، وأن النقص هو إدخال رسم الخدمة وعمولة السائق والتوزيع المحاسبي في التسوية.
- أضيفت حقول قواعد العمولة ورسم الإلغاء، وحقل عمولة السائق المثبتة على الرحلة، مع Migration `FinancialSettlementRules` وقيد يمنع العمولة السالبة.
- أصبح قبول العرض يحسب ويثبت رسم الخدمة وعمولة السائق وصافي السائق ومستحق المنصة.
- أصبح التحصيل النقدي يسجل إجمالي العميل ويولد سجل تسوية معلقاً بمديونية السائق لمستحق المنصة؛ لا يخصم من محفظة السائق لأن الدين المالي ليس رصيداً متاحاً.
- `dotnet build src\\YemenDrive.Api\\YemenDrive.Api.csproj --no-restore`: نجح، 0 أخطاء و0 تحذيرات.

## الدفع والمحفظة والتسوية — دفعة المحفظة 2026-09-14

- عدل `PaymentModel` ليحسب إجمالي الرحلة المثبت بدلاً من الأجرة فقط، ويعالج `YemenDriveWallet` كوسيلة الدفع الوحيدة المتاحة حالياً.
- داخل معاملة تسلسلية واحدة: خصم إجمالي العميل، إضافة صافي السائق، إنشاء قيود المحفظة، تسجيل الدفعة المعتمدة، وإضافة سجل تسوية للسائق.
- رفضت أي وسيلة خارجية قبل وجود مزود تحقق فعلي، ومنعت اختلاف العملات أو الرصيد غير الكافي أو العميل/السائق غير الصحيح.
- `dotnet build src\\YemenDrive.Api\\YemenDrive.Api.csproj --no-restore`: نجح، 0 أخطاء و0 تحذيرات.

## تطبيق Migration وربط العميل بالمحفظة — 2026-09-14

- طُبقت `FinancialSettlementRules` على قاعدة التطوير بنجاح، وأعيد تشغيل API دون طباعة أي إعدادات اتصال أو أسرار.
- ربط `PaymentController` بشاشتي `RideModel/get` و`WalletModel/get` ثم `PaymentModel/add` لمحفظة يمن درايف؛ أزيل الرصيد ومبلغ التأكيد الثابتان من واجهة الدفع.
- النقدي لا يرسل دفعة من العميل، بل يرشد للتحصيل عبر تطبيق السائق. طرق الدفع الخارجية تظل غير قابلة للاعتماد قبل التحقق من مزود حقيقي.
- بناء API ناجح؛ تحليل Flutter للملفات المعدلة بلا أخطاء، والمخرجات المتبقية معلومات lint/تحذير Flutter قائم.

## ربط شاشة تحصيل السائق بالتسوية — 2026-09-14

- أضيفت الحقول المالية المثبتة إلى `RideReport` لتظهر للسائق ضمن بطاقة الرحلة وشاشة التحصيل.
- عدلت `DriverPaymentView` لتبدأ بإجمالي العميل لا أجرة الرحلة فقط، وتعرض عناصر الدين وصافي الاستحقاق بوضوح قبل تسجيل التحصيل.
- اجتاز بناء طبقة التطبيق وفحص Flutter للملف؛ الملاحظات المتبقية lint تنسيقية فقط. أعيد بناء API وتشغيله بنجاح.

## استرداد إلغاء الرحلة المدفوعة من المحفظة — 2026-09-14

- أضيف حقل `CancellationFee` المثبت إلى الرحلة، مع Migration `RideCancellationFeeSnapshot` وقيد يمنع القيمة السالبة. صححت لقطة EF بعد كشف موضع غير صحيح للحقل، ثم طبقت الـMigration بنجاح.
- أصبح `RideModel/cancel` للرحلة المكتملة يعالج فقط دفعة محفظة يمن درايف: يعيد المتبقي إلى العميل، يعكس صافي السائق، وينشئ PaymentTransaction بحالة Refunded وتسوية سائق عكسية بمراجع فريدة.
- النقدي والوسائل الخارجية لا تسترد تلقائياً، إذ يلزم إثبات الاسترداد خارج النظام أو تأكيد مزود الدفع. نجح بناء API وأعيد تشغيله، وقاعدة التطوير بلا migrations معلقة.

## اختبارات التكامل المالي وPostman — 2026-09-14

- أضفت برنامج اختبار تكامل قابل لإعادة التشغيل في `tests/YemenDrive.FinancialIntegrationTests`، بقاعدة SQL محلية مؤقتة معزولة تنظف نفسها بعد الاختبار.
- نجحت دورة دفع المحفظة الذرية وإعادة الطلب بالمفتاح نفسه، ثم استرداد إلغاء الرحلة مع رسم الإلغاء وعكس صافي السائق.
- نجحت حالتا النقد: إضافة المبلغ الزائد للمحفظة وخصم العجز منها، كما نجح اختبار الرفض والتراجع عند عدم كفاية الرصيد.
- حدثت مجموعة Postman بقسم مالي مستقل لطلبات دفع المحفظة، اختبار منع التكرار، واسترداد إلغاء الرحلة. لم تُضف أي بيانات حساسة إلى الملفات.

## إصلاح تبديل نوع الخدمة في مسار الحجز — 2026-09-14

- أصلحت ربط شريحة اختيار نوع الخدمة بالحالة الفعلية في `RideController`: الاختيار الجديد يصفر الخدمة المحددة والقائمة المؤقتة ويطلب كتالوج النوع الجديد فوراً.
- ربطت التمرير والضغط في واجهة الشرائح بالدالة الموحدة، لمنع اختلاف الحالة بين الطريقتين.
- فحص Flutter بلا أخطاء تنفيذية، وبُني APK العميل وثُبت على MEmu بنجاح للتأكد اليدوي من مسار النوع ثم المسارات ثم الخدمات.

## تحصين اختيار نوع الخدمة بعد تجربة المحاكي — 2026-09-14

- تحققت من API محلياً: الكتالوج المرتبط بالنقل منفصل عن كتالوج التوصيل، لذا لم يكن الخلل في قاعدة البيانات أو مرشح API.
- منعت تطبيق الافتراضي المتأخر من تجاوز اختيار المستخدم اليدوي، وفلترت الخدمات في العميل مرة ثانية على `serviceKindId` قبل العرض.
- وحدت مسار فتح اختيار الموقع مع `startHomeService` لضمان تثبيت النوع قبل الانتقال. بُني APK المعدل وثُبت على MEmu بنجاح.

## إنهاء طلب التسعير عند الفشل أو الرجوع — 2026-09-14

- أضفت معالجة نجاح وفشل صريحة لطلب السعر في `RideController`؛ الاستثناء الناتج عن غياب قاعدة التسعير لم يعد يترك الحالة `quoting` عالقة.
- أضفت معرف طلب متزايد لإبطال أي استجابة تسعير تصل بعد مغادرة الشاشة، وربطت زر الرجوع ورجوع النظام بإلغاء الطلب المعلق.
- أصبحت واجهة التسعير تعرض رسالة وزر عودة عند الفشل بدلاً من دوران غير منتهٍ. اجتاز فحص Flutter بلا أخطاء تنفيذية، وبُني APK وثُبت على MEmu.

## تسوية مديونيات السائقين النقدية من لوحة الإدارة — 2026-09-14

- أضفت كيان `DriverSettlementPayment` وسجلته في سياق قاعدة البيانات. يحفظ كل تحصيل كقيد مستقل غير قابل لتعديل مديونية الرحلة أو الدفعة النقدية المصدرية.
- أضفت خدمتي `DriverSettlementPaymentModel/add` و`list` المحصورتين بالإدارة. عملية الإضافة تعمل في معاملة متسلسلة، وتتحقق من المديونية المتبقية والعملة ومرجع التحصيل الفريد، وتسمح بالتحصيل الجزئي فقط ضمن المتبقي.
- أنشأت وطبقت Migration `DriverSettlementPaymentCollection` على قاعدة التطوير بنجاح.
- أضفت شاشة لوحة الإدارة لعرض ديون السائقين النقدية وتسجيل التحصيل، مع إظهار إجمالي الدين والمحصل والمتبقي وحالة الإغلاق.
- حدثت Postman بقسم `10 - Driver Debt Collection`، وفحصت صحة ملف JSON. أضفت اختبار تكامل للتحصيل يثبت عدم تغيير `NetPayable` الأصلي، ورفض مرجع التحصيل المكرر؛ نفذ الاختبار على قاعدة مؤقتة ونجح.
- تحقق `dotnet build src\\YemenDrive.Api\\YemenDrive.Api.csproj --no-restore`: ناجح، 0 أخطاء و0 تحذيرات. أعيد تشغيل API على بيئة التطوير بعد تطبيق الـMigration.

## تجربة يدوية لمسار الدفع على المحاكي — 2026-09-15

- شغلت تطبيق العميل على MEmu واستكملت مسار الحجز بخدمة نقل وسيارة اقتصادية. نجح احتساب السعر التجريبي والوصول إلى شاشة انتظار عروض السائقين.
- شغلت تطبيق السائق بالحساب التجريبي الموافق لمركبة اقتصادية، ثم أعدت تشغيله للتأكد من جلب البيانات حديثاً. لم تظهر الرحلة الجديدة؛ بقيت الرحلات المفتوحة المعروضة من بيانات أقدم.
- أوقفت تجربة الدفع النقدي والمحفظة قبل أي تحصيل أو خصم، لأن وصول الرحلة للسائق شرط لازم لتجربة التدفق الحقيقي. لا توجد نتيجة نجاح غير مثبتة لهذه التجربة.
- لا تعديل على Postman في هذه الدفعة؛ النتيجة المطلوبة لاحقاً هي مراجعة مطابقة نوع الخدمة والخدمة الفعلية في تقرير رحلات السائق ثم استئناف الاختبار.

## استئناف تجربة المحاكي ومعالجة الرحلة المفتوحة — 2026-09-15

- راجعت مسار شاشة المفاوضة: زر إرسال الطلب منفصل عن مرحلة احتساب السعر. كانت الرحلة المفتوحة السابقة قد أبقت `_requestId` في الذاكرة، ولذلك بدت الشاشة وكأنها تبحث عن عروض لطلب جديد بينما هي مرتبطة بالطلب القديم.
- عدلت `RideController` لمنع فتح حجز جديد عند وجود رحلة غير مكتملة، ولتنظيف معرف الرحلة والعروض والمسودة وحالة التتبع بعد الإلغاء.
- أضفت زر إلغاء البحث في تذييل شاشة المفاوضة أثناء الانتظار كي لا يضطر المستخدم لانتظار مدة الاستطلاع كاملة.
- أغلقت سجلين تجريبيين فقط بحالة بحث/تفاوض، بعد تحقق صريح من غياب الدفعات المرتبطة؛ الهدف تنظيف بيئة المحاكاة لا تعديل بيانات مالية.
- `flutter analyze` للملفين المعدلين: لا أخطاء تنفيذية، 4 ملاحظات تنسيق قائمة. بنيت APK debug وثبت على MEmu بنجاح. لا تغير عقد API أو Postman.

## تشخيص إنشاء طلب الرحلة قبل عرضه للسائق — 2026-09-15

- فحصت `RideReport` و`Ride` و`RideOffer`: لا يوجد شرط قرب جغرافي في النسخة الحالية. المطابقة تتم على حالة الرحلة المفتوحة، عدم وجود سائق مسند، وتطابق نوع الخدمة والخدمة الداخلية مع `DriverProfile` المتاح.
- أظهر فحص قاعدة التطوير أن آخر الرحلات كلها سجلات سابقة؛ لم يسجل التطبيق الطلب الجديد. وأظهر `logcat` أن `createRequest` يرمي `Invalid ride response`، وهو وصف عام كان يخفي سبب رفض الخادم.
- حسنت مستودع التفاوض لاستخراج رسالة الخطأ والتحقق من رقم الرحلة، وحصنت `RideController` بحيث لا يفعل البحث أو قائمة المستلمين قبل نجاح الإنشاء، ويعيد الواجهة سليمة ويعرض السبب عند الفشل.
- أضفت تسميات محايدة لنقاط المسار حين لا يوفر مزود الخرائط التجريبي عنواناً، لتلبية تحقق API من دون إدخال عنوان مزيف أو كشف موقع في السجل.
- سجل Flutter التشخيصي يقصر نفسه على معرّف نوع الخدمة ومعرّف الخدمة وسبب الرفض. لم يُطبع رمز أو كلمة مرور أو عنوان أو إحداثيات أو إعداد اتصال.
- تحقق فحص Flutter من عدم وجود أخطاء تنفيذية، ثم بُني APK debug وثُبت على MEmu. لا تغيير لـPostman لأن عقد API لم يتغير.

## معالجة صلاحية جلسة العميل في MEmu — 2026-09-15

- كشف `logcat` فشل فك تشفير مفاتيح الجلسة المحفوظة في Android Keystore. النتيجة كانت طلبات API غير موثقة ورسالة عامة تخفي السبب، لا عدم تطابق للسائقين.
- عدلت `SecureStorageService` لتقرأ مفاتيح الجلسة بأمان: عند تعذر فك المفتاح تمسح المحتوى غير القابل للاستخدام وتعيد `null`، فيتحول التطبيق إلى تسجيل الدخول بدلاً من التعطل أو الاحتفاظ بجلسة زائفة.
- اجتاز التحليل بلا أخطاء تنفيذية (بقيت ملاحظات تنسيق)، وبنيت النسخة وثبتها على MEmu. أُعيدت تهيئة بيانات حزمة العميل المحلية فقط؛ لم تمس قاعدة التطوير أو أي قيد مالي أو بيانات السائق. لا تغيير لـPostman.

## تحقق الجلسة من API عند بدء العميل — 2026-09-15

- أضفت تحققاً خفيفاً في `AuthSessionService.init` عبر عملية `UserModel/get` المحمية بعد قراءة أي جلسة محفوظة. لا يسجل أو يعرض هذا المسار الرمز نفسه.
- عند رد خادم مكتمل يرفض الجلسة أو الحساب، تمسح التخزين المحلي وتتحول `isAuthenticated` إلى خطأ. عُمد إلى الإبقاء على الجلسة عند `network_error` أو `invalid_response` كي لا يسجل المستخدم خروجاً أثناء انقطاع مؤقت.
- عدلت `InitialBinding` لتهيئة `ApiClient` قبل `AuthSessionService`، وهو ترتيب لازم للتحقق عند الإقلاع.
- `flutter analyze` للملفات الثلاثة: ناجح بلا ملاحظات. بُني APK debug وثُبت على MEmu بنجاح؛ لا تغير في API أو Postman.

## محاذاة رسائل السائق وإزالة تكرار الاتصال — 2026-09-15

- في `DriverChatView` كان `AlignmentDirectional.centerStart` ثابتاً لكل الرسائل، ولذلك ظهرت رسائل العميل والسائق في اليمين في العربية. أصبح القرار يعتمد على `senderId` مقارنة بمعرف السائق المسجل؛ رسائل السائق يميناً ورسائل العميل يساراً.
- أزلت حالة `state == 5` من شرط زر الاتصال العام في بطاقة السائق، لأن حالة الرحلة الجارية تملك زر اتصال خاصاً داخل كتلة التحصيل والمحادثة؛ لم يعد الزر يظهر مرتين.
- فحص Flutter لم يظهر أخطاء تنفيذية (ملاحظة نوع خام وملاحظات تنسيق قديمة فقط). بُني APK debug حديث لتطبيق السائق وثُبت على MEmu. لا تغيير في Postman أو عقد API.

## إغلاق الدفع النقدي وتوفر السائق — 2026-09-15

- راجعت سجلات رحلة التطوير الأخيرة من دون إظهار بيانات اتصال: كان `CustomerPrice` يساوي 1600 و`TotalAmount` فارغاً في رحلة قديمة، وهذا سبب زر العميل بقيمة صفر. عدلت `PaymentController` ليحسب الإجمالي من السعر المتفق عليه ورسم الخدمة المثبت حين تغيب خانة الإجمالي.
- أصبحت عملية النقد في العميل تستدعي `RideModel/get` وتتحقق من قائمة الدفعات التابعة للرحلة؛ لا تغادر إلى شاشة الشكر قبل وجود دفعة `Cash` معتمدة. عند عدم وجودها تعرض رسالة أن السائق لم يسجل التحصيل.
- راجعت عملية السائق: سجلت الزيادة النقدية 100 بالفعل في `WalletTransactions` للعميل. أضفت إشعار `Payment` ذرياً عند الزيادة أو خصم العجز، كي يتلقى العميل سبب تعديل الرصيد ونتيجته عبر الإشعارات.
- تحققت من أن مديونية السائق لم تُنشأ لأن قاعدة التسعير النشطة للخدمة المستخدمة تحتوي `ServiceFee=0` وعمولتي السائق صفراً. هذا سلوك صحيح: الزيادة النقدية ليست عمولة منصة. يجب تحديد رسم خدمة أو عمولة من لوحة الإدارة لتظهر مديونية لاحقة لهذه الخدمة.
- أضفت شرط انشغال السائق في ثلاثة مواضع: تقرير رحلات السائق، قائمة مستلمي إشعار الطلب الجديد، والتحقق عند إرسال العرض. الحالات الحاجبة هي: مسندة، في الطريق، وجارية. تبقى رحلة السائق الحالية ظاهرة له ليكملها.
- بني API بنجاح: 0 أخطاء و0 تحذيرات. فحص ملف دفع العميل بلا أخطاء تنفيذية (3 ملاحظات تنسيق فقط). بنيت APK العميل وثبته على MEmu، ثم أعدت تشغيل API في نافذة PowerShell ظاهرة. لا تغير لعقد API أو Postman.

## تصحيح نموذج زيادة النقد قبل إعادة البناء المالي — 2026-09-15

- صحح مالك النظام القرار المحاسبي: الزيادة التي يقبضها السائق نقداً لا تضاف لمحفظة العميل كأثر منفرد؛ هي تحويل بين حسابين. يجب إضافة الزيادة لمحفظة العميل وإنشاء التزام مقابل على السائق بالقيمة نفسها ومرتبط بالرحلة.
- القيد الحالي في `DriverCashPayment` يضيف محفظة العميل وإشعارها فقط؛ لذلك سُجّل كتنفيذ جزئي غير معتمد، لا كحل نهائي. لا تغيير على السجلات التاريخية أو أرصدة التطوير قبل تعريف دورة التحصيل والتوريد المطلوبة.
- الخطوة التالية المتفق عليها: استلام وصف دورة التحصيل والتوريد المالي خطوة بخطوة، ثم إعادة تصميم القيود وسجلات التسوية والاختبارات وPostman وفقه.

## دراسة الأستاذ العام المالي قبل التنفيذ — 2026-09-15

- قرأت النموذج المالي المطلوب وراجعت الكيانات والخدمات والقيود الحالية من دون تعديل. النواة الحالية تتضمن `LedgerAccount` (رمز/اسم/نوع/عملة/نشاط) و`JournalEntry` (مرجع/بيان/نشر) و`JournalLine` (مدين/دائن/حساب/مستخدم/رحلة).
- النواة لا تحتوي حتى الآن على ربط موحد بصاحب الحساب، أو رقم مستند تشغيلي فريد، أو حالة عكس، أو قيود قاعدة بيانات تضمن توازن القيد، أو خدمة نشر موحدة؛ ولا توجد استخدامات تشغيلية لها. كما أن `WalletTransaction` و`PaymentTransaction` و`DriverSettlement` تعمل كسجلات مستقلة.
- فحص قاعدة التطوير: توجد محافظ ودفعة وحركة محفظة، بينما عدد الحسابات والقيود والسطور العامة يساوي صفراً. لا توجد حاجة لتغيير تاريخ مالي قائم في مرحلة التصميم.
- التوصية قبل التنفيذ: تطوير هذه النواة بدلاً من إنشاء جداول مالية موازية، واعتماد القيد المزدوج المنشور مصدراً محاسبياً، مع الحفاظ على سجلات المحفظة والدفع والتسوية كمستندات تشغيلية مرتبطة وكمؤشرات رصيد متحققة. التنفيذ مؤجل إلى اعتماد دورة التحصيل والتوريد.

## اعتماد Debit / Credit للأستاذ العام — 2026-09-15

- اعتمد المالك استخدام حقلي `Debit` و`Credit` الصريحين في سطور القيود، لا أسلوب الإشارة الموجبة والسالبة.
- ستتحقق خدمة النشر وقاعدة البيانات لاحقاً من توازن كل قيد ومن أن كل سطر يملك طرفاً واحداً موجباً فقط. لم يبدأ التنفيذ أو الترحيل بهذه الخطوة.

## كشف الحساب في لوحة الإدارة — 2026-09-15

- أضيف المتطلب إلى تصميم المرحلة المالية: شاشة قراءة فقط لكشوف الحسابات المنشورة.
- المرشحات المطلوبة: حساب محدد أو الكل، يوم أو نطاق زمني أو شهر أو سنة، ونوع عملية محدد أو الكل.
- مخرجات الكشف: رصيد أول المدة، تفاصيل المدين والدائن والرصيد الجاري مع رقم القيد ومرجعه والعملة والإجماليات. سيُنفذ مع API والفهارس والصلاحيات بعد تثبيت دورات التحصيل والتوريد.

## توثيق دورة النقد والمحفظة — 2026-09-15

- وثّقت القيود المطلوبة كما حددها المالك: نقد عادي، نقد زائد يضاف لمحفظة العميل كالتزام على السائق، نقص نقدي يغطى من محفظة العميل، دفع كامل بالمحفظة، وتغذية محفظة خارجية.
- أساس عمولة السائق في الأمثلة هو 10% من أجرة الرحلة 1500، لا من رسم الخدمة أو من الإجمالي؛ قيمتها 150.
- في الإلغاء بعد الدفع، يعكس قيد عمولة السائق إن كان قد رُحّل، ويعاد تحصيل الخدمة إلى محفظة العميل، بينما يبقى رسم الخدمة للمنصة حسب السياسة الحالية.
- لم يجر أي تنفيذ أو تعديل أرصدة؛ المتبقي اعتماد حالات وتوقيت النشر والاستثناءات التشغيلية قبل بناء خدمة القيود.

## اعتماد حالات التحصيل والإلغاء وربط الحسابات — 2026-09-15

- التحصيل النقدي المتساوي ينشر لدى تسجيل السائق. أما تغطية نقص من المحفظة فتحتاج موافقة صريحة من العميل؛ الرفض أو عدم كفاية الرصيد يترك العملية بلا قيد ويرسل النتيجة للسائق والعميل.
- في إلغاء رحلة مدفوعة نقداً: الاسترداد المباشر من السائق يعكس بين حساب السائق وتحصل الخدمة، بينما التحويل للمحفظة يعكس بين محفظة العميل وتحصل الخدمة ويبقي المبلغ المقبوض مديونية على السائق. يعكس قيد العمولة المنشور فقط.
- اعتمد تصميم حسابات عام: الحساب لا يحصر بعميل أو سائق، ويرتبط اختيارياً بكيان مالي موحد ونوع/غرض ارتباط. يسمح ذلك بحسابات النظام والرسوم والصناديق والبنوك والجهات الخارجية لاحقاً دون نسخ جداول.
- الدفع الخارجي مؤجل عمداً إلى مرحلة لاحقة بعد اعتماد دورة مزود الدفع وحالاته.

## عناوين الرحلات والخصوصية وملاحة السائق — 2026-09-15

- حُفظت الوجهة آلياً بعد إنشاء رحلة ناجح كـ`recent` في الأماكن المحفوظة للعميل. يعاد استخدام المكان المطابق ويحدث زمنه، وتزال الأقدم فوق حد 20؛ لا تتغير المفضلة أو المنزل أو العمل.
- أضيفت `pickupDisplayName` و`destinationDisplayName` إلى نتائج الرحلة وسجلها، واستخدمتها لوحة الإدارة وتطبيق العميل والسائق. لا تستخدم أي واجهة للإحداثيات كنص عرض.
- تقرير السائق يطبق كشفاً تدريجياً: قبل القبول يتيح نقطة الانطلاق والمسافة منها واسم منطقة للوجهة، لكنه يخفي بيانات الوجهة الدقيقة وبيانات العميل. بعد الإسناد فقط تعود بيانات الوجهة اللازمة للمسار.
- أضيف زر مسار الانطلاق للسائق وزر مسار الوجهة بعد القبول. يفتحان تطبيق الملاحة في الجهاز بإحداثيات مصرح بها فقط.
- استبدلت مستودعات Routes والبحث والعنوان العكسي في تطبيق السائق بطلبات إلى API، كما هو الحال في العميل. لا يستخدم APK بعد الآن مفاتيح Google REST أو بديل Directions مباشر.
- التحقق: API build ناجح 0 أخطاء و0 تحذيرات. تحليل Flutter للتطبيقين لم يظهر أخطاء تنفيذية (بقيت ملاحظات lint قديمة). بنيت وثبتت نسختا debug على MEmu. لا تعديل لمجموعة Postman لأن العمليات ومسارات الطلبات لم تتغير؛ أضيفت حقول استجابة متوافقة فقط.

## اختبار Google Maps بعد تفعيل الفوترة — 2026-09-16

- شغلت API مؤقتاً في بيئة التطوير واستخدمت جلسة اختبار محلية من دون عرض رمز الجلسة أو أي قيمة سرية.
- نجح `MapLocationSearchModel/search` في إعادة نتائج Places. كما ظهرت نتائج بحث فعلية من Google داخل شاشة اختيار الموقع في تطبيق العميل.
- نجح `MapRouteQuoteModel/report` لمسار قيادة فعلي: أعاد الخادم مسافة 971 متراً ومدة 166 ثانية مع polyline صالح. ونجح `MapLocationSearchModel/report` في تحويل نقطة الاختبار إلى عنوان.
- لقطة خرائط Google في MEmu بقيت غير مستقرة بسبب OpenGL/SurfaceView في المحاكي (خلفية بيضاء/سوداء)، وهي مشكلة عرض لا رفض من Google ولا خطأ في API. يبقى التحقق البصري من البلاطات وخط الطريق مطلوباً على جهاز فعلي أو محاكي Google Play حديث.
- لا تعديل على الكود أو Postman أو قاعدة البيانات في هذا الاختبار؛ وثقت النتيجة فقط.

## فصل اسم المنطقة عن العنوان الدقيق للرحلة — 2026-09-16

- توسع `MapPlace` بحقل `area` يستخرج الحي أو النطاق المحلي أو المدينة من مكونات عنوان Google. وصححت قراءة مكونات Geocoding العكسي لتدعم صيغة `address_components` أيضاً.
- يحتفظ `LocationController` الآن بمنطقة مستقلة للانطلاق والوجهة. عند اختيار نتيجة أو نقطة على الخريطة يرسل `RideRequestDraft` المنطقة كتسمية عرض، فيما يبقى العنوان المفصل والإحداثيات منفصلين لاستخدام الخريطة فقط.
- بذلك تظهر الرحلات بصورة مفهومة للسائق قبل القبول، مع عدم الإفصاح عن معلم أو عنوان الوجهة الدقيق. لا تغيير في نموذج قاعدة البيانات أو مسار الطلب.
- تحقق البناء: API build ناجح بلا أخطاء أو تحذيرات، وتحليل Flutter لا يحتوي على أخطاء تنفيذية. بُني APK العميل وثُبت على MEmu. واختبار حي ثانٍ أكد وجود `area` واسترجاع مسار قيادة صالح.

## تنفيذ أساس الأستاذ العام وكشف الحساب — 2026-09-15

- أضفت `FinancialParty` كطبقة ملكية مالية عامة، وربطت بها `LedgerAccount` اختيارياً مع حسابات أب/ابن وغرض محاسبي وحالة حساب نظام/قبول قيود. لا توجد علاقة محصورة بعميل أو سائق، مما يسمح بإضافة بنك أو صندوق أو مزود خارجي لاحقاً دون نسخ جداول.
- أضفت Migration `20260914193107_AccountingGeneralLedgerFoundation` وطبقتها على قاعدة التطوير. تحفظ الترحيل الموجود: لا يحذف جدولاً أو حركة، وتبقي أي قيد سابق بلا ترحيل قسري؛ القيد المنشور الجديد وحده يلتزم إجماليات موجبة ومتوازنة.
- طبقت حماية داخل السياق وقيود SQL: لا يقبل سطر القيد إلا طرفاً واحداً موجباً، ولا يقبل القيد المنشور إلا بإجمالي مدين يساوي الدائن. كما تمنع حماية السياق تعديل أو حذف القيد المنشور وسطره.
- أضفت `AccountingPostingService`: يعالج الحسابات النشطة القابلة للنشر وعملة موحدة وتفرد مصدر العملية داخل معاملة `Serializable`، ويُنشئ قيداً عكسياً بدلاً من تحرير الأصل.
- أضفت `AccountingSetupModel` لتجهيز دليل الحسابات القياسي بشكل idempotent، و`LedgerAccountModel` و`AccountStatementModel` للإدارة. تقرير الكشف يحسب الرصيد الطبيعي لكل حساب ويرجع الملخص والتفاصيل الزمنية.
- أضفت واجهة «كشف الحساب» في لوحة الإدارة وفلاتر الحساب/الفترة/نوع العملية وزر تهيئة الدليل. أضفت ثلاثة طلبات Postman آمنة لقسم الأستاذ العام.
- الاختبار: API build ناجح بلا تحذيرات، Migration model متطابق، Postman صالح، واختبار تكامل على قاعدة LocalDB مؤقتة نجح للقيد المتوازن، منع إعادة المصدر، رفض عدم التوازن، عدم قابلية تعديل القيد المنشور، والكشف. لا عُرضت رموز أو كلمات مرور أو بيانات اتصال أثناء العمل.
- لم ألمس ربط الدفعات الجارية بالأستاذ العام في هذه الدفعة؛ الخطوة التالية هي ربط التحصيل النقدي أولاً بقيد الدورة المعتمد ثم إضافة موافقة العميل للعجز وربط المحفظة والإلغاء بالتتابع.

## إصلاح تدفق رسم الطريق في تطبيق العميل — 2026-09-16

- فحصت شاشة اختيار الموقع: الـPolyline تصل إلى خريطة Google بصورة تفاعلية، لكن حسابها يعتمد على `MapRouteQuoteModel/report` من API وليس على مفتاح عرض الخريطة في APK.
- كانت API غير مستمعة عند الاختبار، ولذلك بقيت النقطتان بلا خط قيادة. شغلت الخدمة على منفذ التطوير المعتاد.
- عدلت التحكم بالمسار ليبدأ الطلب فور اختيار الوجهة بالتوازي مع تحويل الإحداثيات إلى عنوان؛ وأضفت رقم طلب لحماية المسار من نتائج متأخرة إذا غيّر العميل نقطة الانطلاق أو الوجهة بسرعة. وعند إنشاء الخريطة مجدداً يعاد حساب المسار المكتمل الناقص تلقائياً.
- نُفذ `flutter analyze` بلا أخطاء تنفيذية، وبُني APK العميل وثُبت على MEmu. لم يتغير عقد API أو Postman، ولم تظهر أي أسرار في التحقق.

## تعبئة بيانات الموقع من الخريطة — 2026-09-16

- عدلت اختيار النقطتين المباشر: حقل الانطلاق وحقل الوجهة يعرضان اسم المكان المعاد من التحويل العكسي، أو العنوان المقروء عند غياب اسم المكان.
- قبل تثبيت وجهة بديلة تمسح بيانات اسم العنوان والشارع والتفاصيل للوجهة السابقة؛ ثم تملأ النتيجة الجديدة هذه الحقول عند وجودها. يمنع ذلك حفظ عنوان قديم مع إحداثيات وجهة جديدة.
- أضفت مدة مسار القيادة الفعلية إلى `RideRequestDraft` إلى جانب المسافة. لا ينشأ مسار تقديري أو مسافة جوية ما دام مسار القيادة الحقيقي متاحاً.
- فحص Flutter لم يسجل أخطاء تنفيذية؛ بقيت تحذيرات strict raw type قديمة في ملفات غير معدلة. بنيت النسخة وثبتها على MEmu، ولم يلزم تعديل Postman.

## ربط خريطة العميل بالسائقين الفعليين والتتبع — 2026-09-16

- كان سبب اختفاء العلامات أن `loadDemoNearbyDrivers` يعمل فقط في وضع العرض، ولا يوجد استعلام موازٍ للسائقين الفعليين. أضفت `NearbyDriverReport` مع `NearbyDriverModel` كقائمة قراءة فقط.
- يفرض التقرير: مستخدم سائق نشط، ملف متاح، موقع حي متصل، وعدم وجود رحلة تشغيلية مسندة له. يرجع الاسم والصورة والمركبة والتقييم والموقع والاتجاه وعدد الرحلات المكتملة والمسافة؛ لا يرجع رقم الهاتف أو بيانات حساسة.
- تطبيق العميل يستدعي التقرير حول نقطة الانطلاق ويحدث العلامات عند تغيرها؛ بطاقات السائق القائمة بقيت كما هي، وأصبحت العلامة تستخدم صورة السائق إذا كان الرابط صالحاً.
- أصلحت شاشة تتبع السائق كي لا تستخدم محاكاة لمجرد أن النسخة Development: المحاكاة الآن مقتصرة على وضع العرض الصريح فقط. في الوضع الحقيقي تسحب آخر موقع للسائق كل أربع ثوان وترسم مسار القيادة إلى نقطة الانطلاق/الوجهة، مع حد زمني وحركي لتقليل طلبات Routes.
- حدثت Postman بطلب التقرير، وهو صالح JSON. بناء API نجح بـ0 أخطاء و0 تحذيرات؛ تحليل Flutter بلا أخطاء تنفيذية مع ملاحظات strict raw type قديمة خارج الملفات المعدلة.

## تحسين نقطة الرحلة وموقع السائق وتذكر الجلسة — 2026-09-16

- راجعت حقلي البحث المدمجين فوق خريطة اختيار المسار. أضفت زر مسح لكل حقل في الطرف الأيسر في RTL؛ يفرغ الحقل، يلغي نقطة الانطلاق أو الوجهة وما يتبعها من خط/مسافة/مدة، ويستدعي التركيز فوراً حتى يبدأ المستخدم كتابة بحثه الجديد من دون نقرة ثانية.
- راجعت زر موقع السائق ووجدته يستخدم زوج إحداثيات ثابتاً للتطوير. استبدلته بـGeolocator: فحص خدمة الموقع والصلاحية، قراءة موقع عالي الدقة، ثم إرسال الموقع الحقيقي فقط. عند فتح لوحة السائق ينشر الموضع الأول ويشترك في تحديثات الحركة الأمامية؛ يلغى الاشتراك عند إغلاق الشاشة ولا تستخدم صلاحية موقع في الخلفية.
- كانت شاشة السائق التي يبدأ منها التطبيق هي شاشة دخول قديمة منفصلة عن شاشة الدخول العامة، لذلك لم يكن خيار التذكر الظاهر في الشاشة الأخرى كافياً. أضفت الخيار إلى الشاشة الفعلية وربطته بالجلسة. الجلسة المختارة فقط تحفظ بأمان، بينما غير المختارة تبقى في الذاكرة إلى إغلاق التطبيق. أصبح مسار البدء يفتح لوحة السائق عند وجود جلسة متذكرة ويحتفظ بمعرف المستخدم المطلوب لتحميل ملفه وتحديث موقعه.
- تحقق: نجاح بناء APK debug للعميل والسائق، وتثبيتهما على MEmu. فحص التحليل لم يظهر أخطاء؛ التحذيرات المتبقية ملاحظات قديمة للتنسيق أو strict raw types خارج الحزمة. لم يتغير عقد API؛ لذلك لا تحديث Postman.

## تركيز مسار الحجز ودليل الخريطة — 2026-09-16

- وسعت `AppGoogleMap` بخيار حدود تركيز آمن يطبق بعد جاهزية الخريطة الأصلية، ويعاد تطبيقه فقط عند تغير المسار. لا يتعارض مع متابعة موقع السائق المتحرك بعد ذلك.
- أصبحت حدود التركيز تحسب من نقاط الـPolyline الفعلية عند توفرها، لا من الانطلاق والوجهة فقط؛ ولهذا لا يقتطع المسار إذا كان طريق القيادة ملتفاً.
- أضفت دليل مسار عائماً مصغراً في `RideMapShell` لكل خطوة تعرض مساراً: أسماء الانطلاق والوجهة، أيقونات دلالية، والمسافة. يوضع أسفل شريط الخطوات كي لا يغطيه.
- أضفت علامة انطلاق شخصية مرسومة محلياً باسم العميل كحل واجهة آمن، إذ لا يدعم ملف العميل الحالي تخزين أو رفع صورة شخصية. لم أضف تخزيناً مؤقتاً أو رابط صورة غير محمي؛ سيحتاج عرض الصورة الفعلية إلى حزمة مستقلة لإضافة رفع الصورة وتخزينها المحمي وإذن المستخدم.
- التحقق: `flutter analyze` بلا أخطاء تنفيذية، ونجح بناء APK العميل وتثبيته على MEmu. لم يتغير عقد API أو مجموعة Postman.

## منع اختيار موقع عند النقر على سائق قريب — 2026-09-16

- راجعت `NearbyDriversMap`: كانت بطاقة السائق مرتبطة بعلامته، لكن حدث لمس الخريطة كان قد يصل معها في بعض تنفيذات Android ويستدعي `selectMapPoint` فيضع علامة الانطلاق/الوجهة على موضع السائق.
- أضفت `consumeTapEvents` لعلامات السائق، ومعه حارس زمني وإحداثي داخل الخريطة. الحارس يلتقط كلا ترتيبي الأحداث الممكنين ويختار السائق إن كانت النقرة ضمن 22 متراً من موقع علامته.
- المساحة الحرة فقط تمرر إحداثياتها إلى شاشة اختيار المسار. بذلك تبقى علامة السائق للتفاصيل والبطاقة ولا تصبح موقعاً في الرحلة.
- تحقق البناء والتثبيت على MEmu بنجاح؛ لا تغيير عقد API أو Postman.

## تتبع السائق الحي قبل الوصول إلى العميل — 2026-09-19

- تم تحديث خريطة انتظار السائق لعرض الطريق الحي بين موقع السائق المتغير ونقطة انطلاق العميل، مع تركيز يحافظ على الطرفين وخط القيادة ضمن الخريطة.
- أزيلت علامة العميل العامة المكررة. تظهر علامة الانطلاق الشخصية وحدها، بينما تستخدم علامة السائق صورته إن كانت موجودة في ملفه أو الدبوس البديل عند غيابها.
- تحقق: تحليل Flutter بلا أخطاء تنفيذية، وبناء APK debug وتثبيته وتشغيله على MEmu بنجاح. لا تعديل على API أو Postman.

## علامات التتبع الاتجاهية والمسار المتبقي — 2026-09-19

- أضفت علامتين مرسومتين محلياً لخريطة التتبع: سيارة مضيئة للسائق قبل بدء الرحلة، وعلامة اتجاه دائرية بيضاء بإطار أزرق عند تحرك الرحلة إلى الوجهة. تتجهان من بيانات bearing الحية.
- خط التتبع لا يعرض ما قطعه السائق: يحسب أقرب نقطة على مسار API لموقع السائق الحالي ثم يرسم الجزء القادم فقط.
- تحقق: بناء APK العميل debug وتثبيته وتشغيله على MEmu بلا عطل. لم يتغير API أو Postman.

## تحسين بصري لعلامات تتبع الرحلة — 2026-09-19

- استبدلت سيارة التتبع المرسومة يدوياً بأيقونة Flutter رسمية صغيرة داخل هالة وظل، وحسنت سهم الرحلة بحلقات إضاءة وتدرج وظل ليظهر كعلامة ملاحة فعلية.
- بنيت نسخة العميل الجديدة وثبتها وشغلتها على MEmu. التحليل لا يحتوي أخطاء تنفيذية؛ لا تغيير في API أو Postman.

## استقرار خريطة Google على المحاكي — 2026-09-20

- كشف سجل MEmu أن Map SDK يهبط إلى المحرّك القديم مع أخطاء EGL/OpenGL، لذا الخريطة السوداء ليست بديلاً تجريبياً من التطبيق.
- طلب التطبيق الآن renderer الحديث قبل إنشاء الخريطة ويحضره مسبقاً، مع حماية جميع تحريكات الكاميرا من استخدام controller تم التخلص منه.
- بُني APK العميل الجديد وثُبت وشُغّل على MEmu. لا تغيير API أو Postman.

## إصلاح تعطل MEmu عند إنشاء الخريطة — 2026-09-20

- السجل أثبت `eglChooseConfig failed` في GLThread الخاصة بـGoogle Maps، إضافة إلى تحريك كاميرا لمتحكم disposed داخل اختيار الموقع.
- فُعل Hybrid Composition للمحاكي، وحميت تحريك الكاميرا من المتحكمات المنتهية وأزلت الإتلاف المكرر للـcontroller من متحكم الموقع.
- تحقق على MEmu: تطبيق العميل بقي قيد التشغيل بعد تهيئة خريطة الموقع دون الخطأ القاتل، وتطبيق السائق اجتاز فحص التشغيل مع API. لا تغيير في API أو Postman.

## تصحيح اتجاه سيارة التتبع — 2026-09-20

- نقلت تعويض اتجاه سيارة Material من الرسم الداخلي إلى `Marker.rotation` كي تصطف السيارة مع bearing GPS الصحيح بدلاً من الظهور أفقياً.
- بنيت وثبتت نسخة العميل الجديدة وأعدت تشغيلها على MEmu. لا تغيير API أو Postman.

## منع تكرار قرار عجز الدفع النقدي — 2026-09-20

- عدلت نافذة الموافقة المالية لتغلق قبل انتظار الشبكة، مع حارس حالة يمنع النقرات والطلبات المكررة.
- بعد اكتمال القرار يعاد جلب تفاصيل الرحلة لتحديث الواجهة. بُني APK العميل وثُبت وشُغل على MEmu؛ لا تغيير API أو Postman.

## بدء مسار الإلغاء المحكوم والمراجعة الإدارية — 2026-09-19

- ثُبت شرط سبب الإلغاء على مستوى API وقاعدة البيانات، مع سجل مستقل غير مالي لطلب الإلغاء وقرار السائق والإدارة.
- عطلت طريق الإلغاء القديم حتى لا ينفذ استرداداً أو تغييراً للحالة خارج سير العمل الجديد.
- أضيفت آلية إيقاف الرحلة أثناء المراجعة والإحالة المباشرة للإدارة للرحلة التي بدأت، مع حفظ آخر إحداثية متاحة للسائق/الرحلة.
- أنشئت وطُبقت Migration `20260919192342_AddRideCancellationWorkflow`، ثم تحقق بناء الحل وأعيد تشغيل API. أضيفت طلبات Postman اللازمة للمسار الجديد؛ بقي ربط التطبيقات ولوحة الإدارة واختبارات التدفق قيد التنفيذ.
- وصل تطبيق العميل طلب الإلغاء إلى المودل الجديد من شاشة السبب الحالية، وحجبت الواجهة أزرار الاسترداد القديمة من الرحلات المكتملة. اجتازت اختبارات التكامل المالية بعد استبدال اختبار الاسترداد المباشر بسيناريو الإلغاء المحكوم. ما زالت واجهة قرار السائق وشاشة مراجعة الإدارة وتدفق الدفع النقدي الجديد قيد التنفيذ.
- أضيفت أزرار قرار السائق في مركز إشعارات تطبيق السائق مع ربط آمن بمعرف طلب الإلغاء. بقيت شاشة مراجعة الإدارة وتدفق الدفع النقدي الجديد قيد التنفيذ.

## قائمة مراجعة الإلغاء في لوحة الإدارة — 2026-09-19

- أنشأت تقرير قراءة إداري `RideCancellationReport` منفصلاً عن أمر تغيير الحالة، وأضفت له فلترة بالحالة أو الرحلة. يحمي التقرير نفسه بفحص دور الإدارة ولا يعيد أي سر أو بيانات مزود دفع.
- أضفت إلى لوحة التحكم صفحة طلبات الإلغاء وحالة السائق ومعلومات الرحلة والدفع المتاحة، وربطت زري اعتماد/رفض الإدارة بعمليات API الرسمية مع تأكيد قبل الإرسال وحماية من الإجراء على طلب ليس في مراجعة الإدارة.
- أعيد تشغيل API بعد بناء ناجح. شغلت اختبارات التكامل المالية على قاعدة مؤقتة ونجحت. لم يلزم تغيير Migration أو Postman لأن عملية قرار الإدارة كانت موجودة مسبقاً؛ أضيف التقرير وواجهة الإدارة فقط.
- القرار المالي المتبقي المقصود: بعد اعتماد طلب مدفوع، تنشئ الإدارة عملية استرداد/تسوية منفصلة عبر الأستاذ العام، ولا تعتمد الشاشة الحالية كإذن تلقائي لتحريك المحفظة أو عكس دفعة.
- بنيت APK debug للعميل والسائق وثبّت النسختان على MEmu، ثم شغلت كل تطبيق وتحققت من بقائهما في الواجهة. لا يوجد `FATAL EXCEPTION` في السجل المرتبط بالتطبيقين.

## فصل تحصيل النقد عن إنهاء الرحلة — 2026-09-19

- أضفت كياناً مستقلاً غير مالي `CashPaymentRequest` لحالة طلب العميل دفع النقد بعد إنهاء السائق للرحلة من دون تحصيل، مع فهرس يمنع أكثر من طلب مفتوح لنفس الرحلة ومفتاح منع تكرار للعميل.
- عدلت `DriverCashPayment` ليفصل القيد المالي عن حالة الرحلة. يقبل التحصيل أثناء الرحلة، أو بعد اكتمالها فقط عندما يكون طلب العميل مؤكداً من السائق؛ ويحوّل الطلب إلى Collected داخل المعاملة نفسها مع الدفعة والمحفظة والأستاذ العام.
- أضفت واجهة السائق: إنهاء الرحلة منفصل أثناء التشغيل، ومع الرحلة المكتملة زر تسجيل تحصيل نقدي بطلب العميل. أضفت قرار قبول/رفض من إشعار الدفع النقدي. في العميل، النقد الآن طلب تأكيد للسائق بدلاً من رسالة تحقق سلبية، وتصبح شاشة الدفع شاشة إنهاء مباشرة عند ظهور دفعة نقدية معتمدة.
- أضفت ثلاثة طلبات Postman جديدة وتحققت من صلاحية JSON. بُني API، طُبق الترحيل، واجتاز اختبار التكامل المالي المعزول. بقيت تحذيرات تنسيق قديمة و`CS1998` في مسار إلغاء قديم محجوب، وليست أخطاء تنفيذية.
- بُنيت نسختا APK debug. ثبتت نسخة العميل المحدثة على MEmu، وتحققت من أن API يعمل بعد تطبيق الترحيل. لم أطبع أي رموز جلسات أو أسرار أو إعدادات اتصال.
- حسنت استجابة السائق لطلب النقد: بعد قبول الإشعار تعاد تحميل القائمة ثم تفتح شاشة التحصيل المعروفة للرحلة المقصودة تلقائياً.

## خريطة السائق الداخلية ومحفظته — 2026-09-19

- أزلت فتح روابط Google الخارجية من توجيه السائق. المسار الآن صفحة داخلية تستدعي `MapRouteQuoteModel/report` عبر `ApiRoutesRepository`، وترسم الطريق بعلامتي موقع السائق والهدف فوق `AppGoogleMap`.
- أضفت مساراً داخلياً للمحفظة وربطته بمستودع السائق. يعرض الرصيد والحركات الموثقة من API فقط، ولا يستخدم `WalletController` القديم الذي كان يحتوي قيم عرض وشحن تجريبي.
- أضفت الحماية من تحميل المحفظة المتكرر في إعادة بناء الواجهة، وتحققت من تحليل Flutter ثم بنيت وثبتت APK السائق على MEmu. لا مفاتيح أو رموز أو بيانات اتصال مطبوعة.
- شغلت تطبيق السائق بعد التثبيت وتأكدت أن `MainActivity` بقيت مفتوحة؛ رسائل توافق Window القديمة من MEmu ليست استثناءً قاتلاً للتطبيق.

## تصحيح زر التحصيل النقدي للرحلات المكتملة — 2026-09-19

- كان الزر مرئياً لكل رحلة مكتملة لأن الواجهة استخدمت الحالة التشغيلية فقط. أضفت إلى `RideModel/list` حقلي طلب النقد المرتبطين بالرحلة، وقيدت الزر بالحالة المؤكدة من السائق وحدها.
- تحققت من بناء API وتحليل Flutter دون أخطاء تنفيذية، وبنيت وثبتت APK السائق المحدث على MEmu.
