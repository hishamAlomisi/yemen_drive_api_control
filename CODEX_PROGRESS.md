# YemenDrive codex://threads/01a09703-73c8-7043-a532-1dc155715fadProgress

آخر تحديث: 2026-09-15 (تجهيز موافقة العميل على عجز التحصيل النقدي)

> تم إنشاء سجل تفصيلي للعمل والاختبارات في `CODEX_WORK_LOG.md`. المشروع ما زال في مرحلة بناء واختبار تجريبية؛ لا توجد بيانات أو مفاتيح إنتاجية.

## حالة المشروع

المشروع في مرحلة البناء والاختبار التجريبي. الهدف هو بناء API بسيطة ذات مسار موحد، بحيث يرسل العميل اسم المودل والعملية والبيانات، ويقوم الخادم بتوجيه الطلب إلى الكلاس المناسب تلقائياً.

## آخر تقدم — موافقة عجز التحصيل النقدي

- أضيفت Migration رسمية `CashCollectionApproval` وطُبقت على قاعدة التطوير؛ لا تحوي أي بيانات اتصال أو أسرار.
- عندما يسجل السائق مبلغاً نقدياً أقل من الإجمالي، لا يخصم الخادم من محفظة العميل مباشرة. ينشئ طلب موافقة، ويظل التحصيل والرحلة بلا دفعة مالية حتى قرار العميل.
- يمكن للعميل قبول الطلب أو رفضه من تطبيق العميل. القبول يعيد التحقق من الرصيد ثم يتيح للسائق إعادة تسجيل التحصيل نفسه؛ الرفض أو عدم كفاية الرصيد لا ينشئ دفعة ولا يغير الرصيد.
- جهزت واجهة السائق لعرض حالة انتظار الموافقة، وواجهة العميل لاتخاذ القرار مع تحديث الرحلة. ما زال ربط قيود الأستاذ العام الفعلية بالتحصيل خطوة لاحقة مستقلة لضمان الذرية المحاسبية الكاملة.
- أضيفت طلبات Postman الآمنة لقراءة/قبول/رفض طلب العجز، واجتاز اختبار التكامل المالي المعزول سيناريو الطلب والموافقة ثم التحصيل، وسيناريو عدم كفاية الرصيد.

المسار الرئيسي الحالي:

```text
POST /api/execute
```

صيغة الطلب العامة:

```json
{
  "model": "UserModel",
  "operation": "add",
  "data": {}
}
```

## بنية الحل الحالية

- `YemenDrive.Api`: تشغيل ASP.NET Core، المسار الموحد، نقاط إعداد قاعدة البيانات، المصادقة، رفع الصور ولوحة التحكم.
- `YemenDrive.Application`: المودلات وكلاسات الوظائف الخاصة بالنظام.
- `YemenDrive.Services`: الخدمات الأساسية الثابتة التي يرث منها المطور:
  - `OperationsService<TModel>` للإضافة والتعديل والحذف والجلب.
  - `ReportsService<TModel>` للقوائم والتقارير والبحث.
  - `InitializationService<TModel>` للتهيئة والتجهيز.
- `YemenDrive.Database`: Entity Framework Core، الكيانات، `DbContext`، إعداد الاتصال ومزود SQL Server.
- `YemenDrive.Shared`: النتائج الموحدة، الأخطاء، أدوات الأمان المشتركة.
- `postman/YemenDrive.postman_collection.json`: مجموعة اختبار العقود بالترتيب.

يتم اكتشاف الكلاسات التي ترث خدمات النظام عبر Reflection وحقنها تلقائياً. لا يحتاج المطور إلى إنشاء Controller أو Route جديد عند إضافة وظيفة جديدة؛ يكفي إضافة Model وكلاس خدمة في `YemenDrive.Application`.

## ما تم إنجازه

### هوية المستخدم من Bearer token

- أضيف `ICurrentUserContext` و`HttpCurrentUserContext` لاستخراج معرف الحساب من access token.
- سجل الرحلات والأماكن المحفوظة وبيانات الحساب والمحفظة والدفع وإنشاء الرحلة لم تعد تعتمد على `customerId` أو `userId` المرسل من العميل.
- عند غياب التوكن في عملية شخصية يعيد API الخطأ `authentication_required`.
- عمليات الوصول إلى رحلة أو دفعة تتحقق من أن المستخدم الحالي هو صاحب المورد أو السائق المرتبط به.
- أضيف `SavedPlaceAdminModel` حتى تبقى قائمة جميع الأماكن في لوحة الإدارة منفصلة عن عقد المستخدم.
- تم تحديث Postman لاستخدام Bearer token على مستوى المجموعة وحذف معرف المستخدم من أجسام طلبات المستخدم.
- لوحة التحكم تستخدم عقود الإدارة عند عرض بيانات جميع المستخدمين. في عمليات الإدارة يمكن تحديد معرف المستخدم صراحة، بينما إذا وُجد Bearer token فإن هوية التوكن لها الأولوية ولا يمكن للعميل تجاوزها بمعرف داخل البيانات.
- أضيف حساب مدير تجريبي يُنشأ تلقائياً عند تجهيز قاعدة البيانات أو عند تشغيل API مع قاعدة مجهزة:
  - الهاتف: `700000001`
  - كلمة المرور: `Admin1234!`
  - الدور: `Admin`
- أضيف `POST /api/admin/login` لإصدار جلسة المدير، و`POST /api/admin/execute` لتنفيذ عقود لوحة التحكم بعد التحقق من دور المدير.
- لوحة التحكم لا تفتح العمليات قبل تسجيل الدخول. تحفظ التوكن في المتصفح، وتوفر تسجيل الخروج، وتعيد المستخدم إلى شاشة الدخول عند انتهاء الجلسة.
- تم منع استخدام حساب المدير كحساب سائق؛ محاولة إنشاء ملف سائق للمدير تعيد `admin_cannot_be_driver` بدلاً من تغيير دوره وفقدان صلاحية الدخول.
- عند تشغيل API أو تجهيز قاعدة البيانات تتم استعادة دور حساب المدير التجريبي إلى `Admin` وتفعيله إذا كان قد تغير في بيانات تجريبية سابقة.
- حقل حساب المستخدم في نموذج إضافة السائق داخل لوحة التحكم أصبح قائمة مستخدمين تستبعد المدير بدلاً من إدخال المعرف يدوياً.
- أضيفت عملية صريحة لقبول عرض السائق:
  - `POST /api/execute`
  - `model: RideOfferModel`
  - `operation: accept`
  - `data: { "id": 123 }`
  - يقبلها العميل صاحب الرحلة أو المدير، ويرفض العروض المنتهية أو المعالجة، ويثبت السائق والسعر وينقل الرحلة إلى `DriverAssigned`.
- تم تحديث Postman ليستخدم التسلسل: إضافة العرض، قبول العرض، وصول السائق (`4`)، بدء الرحلة (`5`).

### قاعدة البيانات وAPI

- إنشاء مخطط قاعدة البيانات باستخدام EF Core و`EnsureCreated`.
- تجهيز 22 جدولاً حالياً، من ضمنها:
  - `Users`
  - `DriverProfiles`
  - `SavedPlaces`
  - `Rides`
  - `RideOffers`
  - `Wallets`
  - `PricingRules`
  - `RideServiceCatalogItems`
  - `ServiceKinds`
  - جداول الدفع والإشعارات والمواقع والتسويات.
- المعرفات أرقام صحيحة متسلسلة تبدأ من 1 حسب SQL Server.
- إعداد قاعدة التجربة الحالية في:
  `src/YemenDrive.Api/data/database.settings.json`
- تم حل مشكلة `Invalid object name 'UserAccounts'` بإعادة إنشاء المخطط الصحيح.
- تم إضافة `SavedPlace.Kind` وعلاقة المكان بالمستخدم وفهرس `(UserId, Label)`.
- إضافة التحقق من وجود المستخدم عند إنشاء مكان محفوظ ومنع قراءة أو تعديل أو حذف مكان تابع لمستخدم آخر.

### سجل الرحلات

- إضافة:
  - `src/YemenDrive.Application/Rides/RideHistoryModel.cs`
  - `src/YemenDrive.Application/Rides/RideHistoryReport.cs`
- الطلب:

```json
{
  "model": "RideHistoryModel",
  "operation": "list",
  "data": { "customerId": 1 }
}
```

- النتائج تتضمن:
  - رقم الرحلة
  - العميل والسائق
  - الحالة
  - نوع الخدمة والخدمة
  - نقطة الانطلاق والوجهة والإحداثيات
  - السعر النهائي وسعر العميل وسعر الخادم
  - أوقات الإنشاء والبدء والإكمال.
- يتم التصفية حسب `customerId` وعدم إرجاع سجل عميل آخر.

### الأماكن المحفوظة

- إضافة `SavedPlaceModel` وخدمة العمليات وتقرير القائمة.
- العمليات المدعومة:
  - `add`
  - `list`
  - `get`
  - `update`
  - `delete`
- الطلبات تستخدم `userId`، والعمليات الخاصة بمكان محدد تستخدم `id` مع `userId`.
- الحقول الحالية:
  - `label`
  - `kind`
  - `address`
  - `latitude`
  - `longitude`
- تم اختبار إضافة مكان ثم عرضه بنجاح من API.

### Flutter

المشروع المرتبط:

```text
E:\Eqs\yemen_drive
```

- مستودع سجل الرحلات يستخدم `RideHistoryModel` ويرسل معرف المستخدم الحالي.
- مستودع الأماكن المفضلة يستخدم `SavedPlaceModel` بدلاً من `FavouriteModel`.
- تم ربط الحذف والإضافة والقائمة بالـAPI.
- شاشة المفضلة تحتوي على نموذج إضافة حقيقي يطلب الاسم والعنوان والنوع والإحداثيات.
- شاشة اختيار الموقع تحمل الأماكن المحفوظة من API وتعرضها كوجهات قابلة للاختيار.
- تم إزالة مستودعات البيانات التجريبية من مساري سجل الرحلات والمفضلة.
- تم إصلاح تكرار دالة `add` في مستودع الأماكن المفضلة.

### لوحة التحكم

- لوحة التحكم تستخدم المسار الموحد نفسه.
- لوحة التحكم تستخدم `/api/admin/execute` مع توكن المدير، بينما تبقى `/api/execute` لعقود تطبيق العميل.
- تقارير `RideModel/list` و`RideHistoryModel/list` تتجاوز فلتر المستخدم عند تنفيذها بتوكن مدير فعال، وتعرض جميع الرحلات وسجل الرحلات لجميع العملاء والسائقين.
- `SavedPlaceAdminModel/list` هو العقد الإداري للمفضلة/الأماكن المحفوظة ويعرض جميع المستخدمين افتراضياً، مع دعم التصفية الاختيارية بـ`userId`.
- يجب تشغيل النسخة المعدلة من API وإعادة تحميل لوحة التحكم بعد أي تغيير؛ تشغيل نسخة قديمة أو وجود بيانات اعتماد/قاعدة مختلفة قد يظهر القوائم فارغة رغم صحة العقد.
- تم التحقق فعلياً على النسخة المعدلة: `RideModel/list` و`RideHistoryModel/list` أعادا جميع الرحلات بتوكن المدير، و`SavedPlaceAdminModel/list` أعاد قائمة شاملة فارغة فقط لأن قاعدة الاختبار الحالية لا تحتوي أماكن محفوظة.
- أضيف استبعاد لمجلدات مخرجات الفحص `buildcheck-*` في `YemenDrive.Api.csproj` حتى لا تعطل البناء والنشر عند وجودها داخل مجلد المشروع.
- ظهرت قائمة فارغة في اختبار سابق على المنفذ `5080` بسبب عملية قديمة/إعداد قاعدة مختلف. بعد تشغيل المصدر الحالي وإعادة تسجيل الدخول بتوكن المدير، أعاد `RideModel/list` و`RideHistoryModel/list` رحلتين من قاعدة الاختبار نفسها؛ أما الأماكن المحفوظة فعددها صفر فعلياً في قاعدة الاختبار الحالية.
- شاشة الأماكن المحفوظة تعرض المستخدم والاسم والنوع والعنوان والإحداثيات والتاريخ.
- شاشة سجل الرحلات تعرض العميل والمسار والحالة والسعر والتاريخ.
- إضافة الصور للخدمات وأنواع الخدمات تتم عبر `/api/media/upload` وتعرض المعاينة والرابط.

### Postman

تم تحديث:

```text
postman/YemenDrive.postman_collection.json
```

بإضافة:

- طلب سجل رحلات العميل.
- إضافة مكان محفوظ.
- عرض الأماكن المحفوظة.
- تعديل مكان محفوظ.
- حذف مكان محفوظ.
- متغير `savedPlaceId` وحفظ المعرف تلقائياً من اختبارات Postman.

ترتيب المجلدات الحالي يبدأ من إعداد قاعدة البيانات ثم المصادقة والمستخدمين والسائقين والكتالوج والتسعير والرحلات والأماكن والمحفظة.

### توافق Flutter المضاف في API بتاريخ 2026-09-08

- أضيف `RideOfferActionModel` ليتوافق مع مستودع التفاوض الحالي في Flutter عند قبول أو رفض العرض.
- نتيجة `RideModel/get` تتضمن الآن اسم السائق ونموذج المركبة ورابط صورة السائق داخل العرض.
- نتيجة `PromotionModel/list` تتضمن الحقول النصية التي تحتاجها شاشة العروض: `discount`, `expiresAtUtc`, `description`.
- أضيفت عملية `reject` إلى `OperationsService` الأساسية.
- تم اختبار العقود الجديدة بعد تشغيل API ونجح البناء.

### تطبيق السائق - بدء التنفيذ بتاريخ 2026-09-09

- بدأ إنشاء تطبيق Flutter مستقل في `E:\Eqs\yemen_drive_driver`.
- التطبيق مستقل عن تطبيق العميل ويستخدم نفس المسار الموحد وعقود API.
- الوظائف المستهدفة للسائق فقط: تسجيل الدخول، ملف السائق، الرحلات المفتوحة المطابقة لخدمته، إرسال عرض، الرحلات المسندة، تحديث حالة الرحلة، وتحديث الموقع.
- تم تعديل تقرير `RideModel/list` بحيث يرى السائق الرحلات المفتوحة (`Searching` و`Negotiating`) المطابقة لنوع الخدمة والخدمة المسندة إلى ملفه، إضافة إلى الرحلات المسندة إليه.
- أضيفت شاشة دخول للسائق مع دعم جلسة API ورمز OTP عند طلبه.
- أضيفت لوحة سائق مرتبطة فعلياً بـ`DriverModel/list` و`RideModel/list`.
- أضيف إرسال عرض عبر `RideOfferModel/add`، وتحديث حالات الرحلة عبر `RideModel/update` من `DriverAssigned` إلى `DriverEnRoute` ثم `InProgress` ثم `Completed`.
- أضيف تحديث موقع السائق عبر `DriverLocationModel/update` باستخدام بيانات تجريبية قابلة للاستبدال بموقع الجهاز لاحقاً.
- نجح `flutter analyze --no-pub` دون أخطاء (بقيت رسائل lint معلوماتية)، ونجح `flutter test --no-pub`، ونجح `flutter build apk --debug`.
- ملف APK التجريبي الناتج: `E:\Eqs\yemen_drive_driver\build\app\outputs\flutter-apk\app-debug.apk`.
- خادم الـAPI يعمل حالياً على `http://localhost:5080`، وتم التحقق من استجابة لوحة التحكم بحالة HTTP 200.

### توحيد هيكل تطبيق السائق بتاريخ 2026-09-09

- تمت مزامنة تطبيق السائق مع هيكل تطبيق العميل بالكامل: `app` و`core` و`features` و`shared` و`assets` وملفات الاختبارات والمنصات.
- تم اعتماد GetX في تطبيق السائق بنفس تقسيم العميل: `bindings`, `controllers`, `providers`, `repositories`, `models`, `views` و`routes`.
- أضيفت Feature مستقلة للسائق في `lib/features/driver`، والاختلاف عن تطبيق العميل أصبح في الوظائف ومسارات التشغيل فقط.
- مسار التشغيل الابتدائي لتطبيق السائق هو `DriverRoutes.login`، بينما تبقى البنية المشتركة قابلة لإعادة الاستخدام.
- تم تحديث اسم الحزمة إلى `yemen_drive_driver` وتصحيح مراجع الحزمة بعد نسخ الهيكل.
- نجح `flutter test --no-pub` و`flutter build apk --debug` بعد التوحيد، ولا توجد أخطاء تحليل في ملفات السائق أو التطبيق.
- التحليل الكامل للمشروع الموروث يعرض رسائل lint/info قديمة من شاشات العميل المشتركة، دون أخطاء Dart أو أخطاء في Feature السائق.

### تنظيف واختبار السائق بتاريخ 2026-09-09

- أزيلت 41 مجلداً فارغاً من شجرة المصدر داخل `lib` و`test`.
- أزيل الملفان الجذريان الزائدان `main.dart` و`easy_ride_ar.iml` من تطبيق السائق.
- بقيت مجلدات المشروع المشتركة غير الفارغة للحفاظ على التطابق البنيوي مع تطبيق العميل؛ أما `build` و`.dart_tool` فهي مخرجات مولدة يحتاجها Flutter ولا تعتبر ملفات مصدر.
- تم اكتشاف السائق التجريبي: المستخدم `2`، ملف السائق `1`، الهاتف `775288883`، خدمة «نقل / سيارة».
- اختبار API كامل باستخدام جلسة تجريبية للسائق: جلب الملف والرحلات، تحديث الموقع، إرسال العرض، قبول العرض إدارياً، ثم تحديث الحالات `4` و`5` و`6` بنجاح.
- بعد التنظيف نجح `flutter test --no-pub` و`flutter build apk --debug`، والتحليل الموجه لم يظهر أخطاء.

## الاختبارات التي نجحت

- `dotnet build YemenDrive.sln --no-restore`
  - النتيجة: 0 أخطاء، 0 تحذيرات في آخر تشغيل.
- ملف Postman صالح بصيغة JSON.
- اختبار تسجيل دخول المدير ورفض الطلب الإداري دون توكن ونجاحه بتوكن المدير.
- بناء مشروع API المعزول بعد التعديلات نجح دون أخطاء. تعذر استبدال ملفات نسخة البناء الافتراضية أثناء بناء الحل الكامل لأن نسخة API قديمة كانت تعمل وتستخدم ملفات DLL؛ يجب إيقافها ثم البناء المعتاد.
- إنشاء قاعدة `YemenDrive1` بالمخطط الجديد نجح وأعاد 22 جدولاً.
- اختبار API فعلي نجح في:
  - إنشاء مستخدم.
  - إنشاء نوع خدمة وخدمة.
  - إنشاء رحلة.
  - إنشاء مكان محفوظ.
  - جلب سجل الرحلات.
  - جلب الأماكن المحفوظة.
- `flutter analyze lib/features/account lib/features/ride --no-pub`
  - لا توجد أخطاء بناء أو أخطاء syntax.
  - توجد رسائل lint/info قديمة وجديدة، أهمها تنسيق الأقواس والفواصل وأنواع `Map` الخام.

## ما تبقى من العمل

### أولوية مباشرة

1. تشغيل Flutter فعلياً على جهاز أو محاكي واختبار:
   - تسجيل الدخول والتسجيل.
   - فتح سجل الرحلات بعد وجود رحلة حقيقية.
   - إضافة مكان من شاشة المفضلة.
   - حذف المكان ثم التحقق من اختفائه.
   - اختيار مكان محفوظ أثناء إنشاء رحلة.
2. تشغيل تطبيق السائق الجديد على محاكي/جهاز فعلي، ثم اختبار تسجيل الدخول والعروض والحالات والموقع مع حساب سائق حقيقي من قاعدة التجربة.
3. إضافة زر حفظ الوجهة الحالية مباشرة داخل شاشة تأكيد الوجهة في مسار الحجز، وربطه بـ`LocationController.saveCurrentDestination()`.
4. التأكد من أن `AuthSessionService.currentUserId` يتم تعبئته بعد تسجيل الدخول والتسجيل، لأن السجل والمفضلة يعتمدان عليه.
5. اختبار الحالات الفارغة والأخطاء عند عدم إعداد قاعدة البيانات أو عدم وجود مستخدم مسجل.

### عناصر لم تكتمل بعد في التطبيق

- ما زالت تعريفات `Demo*` و`Preview*` موجودة ككود احتياطي، لكنها لم تعد مسارات التشغيل الافتراضية عند `USE_DEMO_DATA=false`.
- لا يزال مسار السائقين القريبين والتتبع الحقيقي بحاجة إلى عقد مواقع/تتبع مستقل؛ التتبع التجريبي لا يعمل في الوضع الافتراضي.
- تم حذف مستودعات `Demo*` و`Preview*` غير المستخدمة من مستودعات Flutter؛ الوضع التجريبي لا يعمل إلا إذا فُعّل صراحة للواجهات البصرية.
- يجب إضافة واجهات API للميزات التي لا يوجد لها عقد بعد، ثم تحديث Postman والتطبيق معها.
- يجب إضافة migrations لاحقاً إذا أصبح الحفاظ على قواعد البيانات الحالية مطلوباً؛ الوضع الحالي يستخدم `EnsureCreated` وإعادة إنشاء مخطط تجريبي.

### متابعة جلسة الربط

- تم منح صلاحية الكتابة وحفظ تعديلات Flutter فعلياً في `E:\Eqs\yemen_drive`.
- تم ربط الحساب والملف الشخصي وكلمة المرور والدعم والحذف والمحفظة والأماكن وسجل الرحلات والتفاوض مع API.
- تم توثيق التفاصيل والاختبارات في `CODEX_WORK_LOG.md`.
- نجح تحليل Flutter دون أخطاء، ونجحت اختبارات Flutter، وتم إنشاء APK تجريبي بنجاح.
- نجح بناء حل API دون أخطاء أو تحذيرات، وتم اختبار عقد تغيير كلمة المرور فعلياً.
- حُذفت مستودعات `Demo*` و`Preview*` غير المستخدمة من Flutter، وأعيد تشغيل `flutter test` ونجح الاختبار بالكامل.
- أُعيد بناء APK بعد التنظيف ونجح بنجاح.

### تحسينات جودة لاحقة

- معالجة رسائل lint في Flutter تدريجياً، خصوصاً `strict_raw_type` و`require_trailing_commas`.
- تحسين توحيد أسماء الحالات والرسائل بين API وFlutter.
- إضافة اختبارات آلية للخدمات والعقود الأساسية.
- إضافة مصادقة حقيقية وصلاحيات فعلية قبل استخدام النظام خارج بيئة التجربة.
- حساب المدير والتوكن الحاليان تجريبيان؛ يجب تغييرهما إلى هوية وصلاحيات حقيقية قبل الإنتاج.
- استبدال رموز الجلسة التجريبية الحالية بنظام JWT أو مزود هوية مناسب.

## التشغيل الحالي

من جذر مشروع API:

```powershell
dotnet run --project src/YemenDrive.Api --urls http://localhost:5080
```

الروابط:

- لوحة التحكم: `http://localhost:5080/admin`
- إعداد قاعدة البيانات: `http://localhost:5080/setup`
- API الموحد: `http://localhost:5080/api/execute`

عند تغيير مخطط الجداول في مرحلة التجربة، استخدم إعداد `recreateSchema: true` من شاشة إعداد قاعدة البيانات أو طلب Postman، مع الانتباه إلى أن ذلك يحذف قاعدة التجربة ويعيد إنشاءها.

## تنبيه البيئة التجريبية

كل العمل الحالي مرحلة بناء واختبار فقط. قواعد البيانات والبيانات والمستخدمون والمفاتيح والرموز وكلمات المرور وعناوين الاتصال ورموز OTP والجلسات الحالية تجريبية وغير حقيقية. لا ينبغي استخدام أي قيمة موجودة في هذا المشروع في بيئة إنتاجية. رمز OTP يطبع في Console مؤقتاً إلى أن تتم إضافة مزود الرسائل النصية. لا يتم تحميل أي مسؤولية عن استخدام هذه القيم أو الشفرات خارج بيئة الاختبار.

## طريقة متابعة العمل في جلسة جديدة

1. اقرأ هذا الملف أولاً.
2. راجع قسم `ما تبقى من العمل`.
3. افحص حالة قاعدة البيانات من `/api/setup/database`.
4. شغّل بناء API قبل التعديلات.
5. بعد كل مرحلة، حدّث هذا الملف بتاريخ اليوم، وانقل البنود المنجزة إلى `ما تم إنجازه` وأضف الأخطاء أو القرارات الجديدة.

### طلبات العملاء والتفاوض في تطبيق السائق - 2026-09-09

- أضيف تحديث دوري لقائمة رحلات السائق كل 5 ثوانٍ في `DriverController`، مع منع تداخل الطلبات وإبقاء آخر قائمة ظاهرة عند فشل مؤقت في الشبكة.
- أصبحت لوحة السائق تعرض الرحلات المفتوحة المطابقة لخدمة السائق أثناء التشغيل، وتحدث القائمة تلقائياً دون الضغط على زر التحديث.
- أضيفت `driverOfferAmount` و`driverOfferExpiresAtUtc` إلى نتيجة `RideModel/list` للسائق، لعرض العرض المعلق الحالي.
- زر العرض يعرض «إرسال عرض» أو «تعديل العرض»، والسعر الابتدائي هو سعر العميل ويمكن تغييره.
- عدل `RideOfferModel/add` لإعادة استخدام العرض المعلق للسائق نفسه وتحديث المبلغ ومدة الصلاحية بدلاً من إنشاء عرض مكرر.
- ربط إرسال العرض بهوية السائق في التوكن، مع السماح للإدارة بتحديد سائق صراحة للاختبار.
- الاختبار الفعلي: ظهرت الرحلة `2` للسائق، أُرسل عرض `1200` ثم عُدّل إلى `1300` مع بقاء نفس معرف العرض `2`.
- نجح بناء الـAPI، واختبار Flutter، وبناء APK التجريبي. لم يوجد جهاز Android متصل عند الفحص.

### المتبقي

- تشغيل تطبيق السائق على محاكي أو هاتف للتأكد بصرياً من التحديث الدوري.
- إضافة SignalR فعلي لاحقاً إذا أضيف Hub إلى API؛ حالياً polling هو الآلية الفعالة.
- استبدال إحداثيات الموقع التجريبية بقراءة `geolocator`.

### استكمال وظائف تطبيق السائق الأساسية - 2026-09-12

- أضيف عقد `RideMessageModel` في API الرئيسي للرسائل، مع `list` لقراءة محادثة الرحلة و`add` لإرسال رسالة و`update` لتعليم الرسالة كمقروءة.
- قُيّدت المحادثة بحيث لا تعمل إلا بين العميل والسائق المسند بعد الاتفاق، ولا تعمل للرحلات المفتوحة أو المكتملة أو الملغاة.
- أضيفت إشعارات داخل قاعدة البيانات عند إنشاء رحلة جديدة للسائقين المتاحين المطابقين لنوع الخدمة والخدمة، وعند وصول رسالة جديدة.
- أضيف عقد `DriverCashPaymentModel` و`DriverCashPayment` للدفع النقدي من السائق.
- عند تسجيل الدفع النقدي، يقارن API المبلغ المقبوض بالأجرة المتفق عليها؛ يضيف الزيادة لمحفظة العميل، أو يخصم النقص من المحفظة عند توفر الرصيد، ويرفض العملية إذا لم يكفِ الرصيد، ثم يكمل الرحلة في معاملة واحدة منطقياً.
- قُيّدت حالات `DriverEnRoute` و`InProgress` و`Completed` بحيث لا يغيرها إلا السائق المسند أو الإدارة.
- أضيفت شاشة دفع منفصلة في تطبيق السائق، وزر محادثة وزر اتصال بالعميل داخل بطاقة الرحلة.
- أضيف تحديث دوري لمحادثة الرحلة كل 4 ثوانٍ عند عدم توفر SignalR، مع منع تكرار الرسائل.
- أضيف عرض الإشعارات غير المقروءة في لوحة السائق، مع جلبها عبر `NotificationModel/list`.
- تم تضمين رقم هاتف العميل في تقرير الرحلات لتمكين الاتصال من التطبيق.

### تحقق هذه المرحلة

- `dotnet build YemenDrive.sln --no-restore`: نجح بلا أخطاء أو تحذيرات.
- اختبار `RideMessageModel/list` للسائق على رحلة مسندة: نجح وأعاد قائمة الرسائل.
- اختبار إرسال رسالة على رحلة مكتملة: رُفض بشكل صحيح بالرمز `chat_not_available`.
- تم تحديث قسم Postman الخاص بالسائق ليستخدم أسماء العقود الفعلية في API الرئيسي: `DriverModel` و`RideModel` و`RideMessageModel` و`DriverCashPaymentModel` بدلاً من أسماء عقود غير موجودة.
- اختبار دورة دفع نقدي ناجحة على الرحلة `3`: أجرة `1500`، قبض `1600`، أضيف الفرق `100` إلى محفظة العميل، وانتقلت الرحلة إلى `Completed`.
- اختبار نقص غير كافٍ على الرحلة `4`: أجرة `1500`، قبض `1000`، ورصيد العميل غير كافٍ؛ أعاد API `insufficient_wallet_balance` وبقيت الرحلة `InProgress` دون تسجيل دفع.
- أضيفت شاشة مستقلة `DriverHistoryView` ومسار `/driver/history` لعرض الرحلات المكتملة والملغاة الخاصة بالسائق.
- `flutter analyze lib/features/driver --no-pub`: لا أخطاء برمجية، والملاحظات المتبقية تنسيقية فقط.
- `flutter test --no-pub`: نجح.
- `flutter build apk --debug`: نجح، والناتج في `E:\Eqs\yemen_drive_driver\build\app\outputs\flutter-apk\app-debug.apk`.

### المتبقي قبل اعتبار تطبيق السائق مكتملاً

- اختبار دورة دفع نقدي كاملة على رحلة تجريبية في الحالات `InProgress` و`Completed` مع حالات الزيادة والنقص والعجز.
- استبدال polling لإشعارات الطلبات وPolling المحادثة بـSignalR فعلي أو خدمة إشعارات مناسبة عند اعتمادها في API الرئيسي.
- إضافة صلاحيات الهاتف والإشعارات واختبارها على Android فعلي.
- اختبار التطبيق على محاكي أو هاتف، لأن الأجهزة المتصلة حالياً لا تتضمن Android.
- مراجعة وتنظيف ملفات الهيكل المشتركة غير اللازمة بعد تثبيت المسارات النهائية، دون كسر تطابق هيكل تطبيق العميل.

## المرحلة 3.1: حماية قاعدة البيانات قبل التعديلات - 2026-09-12

- تم إنشاء نسخة احتياطية `COPY_ONLY` من قاعدة الاختبار `YemenDrive_Integration_20260908` قبل بدء تعديل المخطط.
- مسار النسخة على خادم SQL Server هو:
  `C:\Program Files\Microsoft SQL Server\MSSQL15.SQL2019\MSSQL\Backup\YemenDrive_Integration_20260908_before_stage3_20260912.bak`
- لم يتم حذف أو إعادة إنشاء أي جدول أو بيانات في هذه الخطوة.
- تعذر نسخ ملف النسخة من مجلد SQL Server المحمي إلى مجلد المشروع بسبب صلاحيات نظام التشغيل؛ النسخة الأصلية موجودة بنجاح في مسار خادم SQL المذكور.

## المرحلة 3.2: تثبيت Migrations للمخطط - 2026-09-12

- أضيفت Migration رسمية باسم `20260912183308_Stage3SchemaBaseline` داخل `src/YemenDrive.Database/Migrations`.
- أضيف `DesignTimeYemenDriveDbContextFactory` حتى يستطيع المطور إنشاء migrations لاحقة من أي بيئة تطوير.
- تم تثبيت أداة `dotnet-ef` بالإصدار `9.0.9` والتحقق من ظهور Migration بحالة `Pending`.
- تم توليد Script SQL للمخطط واختباره على قاعدة منفصلة باسم `YemenDrive_Migrations_Test_20260912`.
- نجح تطبيق الـMigration من الصفر، ونتجت 25 جدولاً وسجل Migration واحد في `__EFMigrationsHistory`.
- لم يتم تطبيق Migration أو حذف أي بيانات من قاعدة العمل الحالية بعد؛ الاختبار تم على قاعدة منفصلة آمنة.

## المرحلة 3.3: سلامة البيانات ومنع تكرار الدفع - 2026-09-12

- كانت Migration `20260912184039_Stage3IntegrityIndexes` موجودة في المشروع دون توثيق سابق. تضيف فهارس للرحلات والعروض والمحفظة والإشعارات والرسائل، وتمنع وجود عرض معلّق مكرر من السائق نفسه للرحلة نفسها.
- أضيفت حماية تزامن للرحلات عبر `RowVersion`، وفهرس فريد لمعرّف مرجع الدفع عند وجوده.
- أضيفت Migration جديدة باسم `20260912191548_Stage3PaymentIdempotency`.
- تضيف الـMigration حقل `IdempotencyKey` اختياريًا لعمليات الدفع، وفهرسًا فريدًا مركبًا لكل مستخدم ومفتاح؛ لذلك تعاد نتيجة العملية السابقة عند إعادة إرسال الطلب نفسه بدلاً من إنشاء عملية دفع ثانية.
- أصبح الدفع النقدي للسائق يستخدم مرجعًا ثابتًا لكل رحلة وسائق، ويتعامل مع الطلب المتزامن أو المعاد إرساله بإرجاع العملية المسجلة بدلاً من تكرار الخصم أو الإضافة أو إكمال الرحلة مرة ثانية.
- تم توليد script migrations قابل للتطبيق المتكرر ومراجعته. لم تُطبّق أي migration على قاعدة العمل الحالية ولم تُحذف بيانات.
- `dotnet build YemenDrive.sln --no-restore`: نجح بلا أخطاء أو تحذيرات.
- حُدّث طلب `DriverCashPaymentModel / add` في Postman ليقبل المتغير `paymentIdempotencyKey`؛ أعد إرسال الطلب بالمفتاح نفسه للتحقق من عدم تكرار العملية.

## المرحلة 3.4: تطبيق الترقية على قاعدة العمل — 2026-09-12

- أُنشئت نسخة احتياطية جديدة من قاعدة العمل قبل تطبيق أي migration.
- تبيّن أن قاعدة العمل أُنشئت سابقاً دون سجل EF Core وتفتقد جدولَي `SupportTickets` و`ReferralRedemptions`.
- جرى فحص التعارضات قبل الترقية: لا توجد عروض معلّقة مكررة ولا مراجع دفع مكررة.
- أُضيفت Migration `20260912192135_Stage3ExistingDatabaseUpgrade` لإنشاء الجدولين الناقصين فقط عند غيابهما، لذلك تبقى آمنة لقواعد البيانات القائمة والجديدة.
- تم اعتماد الـbaseline للمخطط القائم في سجل EF Core، ثم تطبيق مراحل الفهارس ومنع تكرار الدفع وترقية الجداول بنجاح.
- التحقق النهائي: 25 جدولاً، و4 migrations مطبقة، ووجود الفهارس المطلوبة والجدولين الناقصين.
- لم تُحذف أو تُعاد إنشاء أي بيانات أو جداول قائمة.

## المرحلة 3.5: سلامة المعاملات المالية والتحويل الكامل إلى Migrations — 2026-09-12

- أصبحت تهيئة API وشاشة إعداد قاعدة البيانات تستخدم `MigrateAsync` بدلاً من `EnsureCreatedAsync`، لتبقى قواعد البيانات الجديدة والقائمة قابلة للترقية رسمياً عبر EF Core.
- أضيفت معاملة SQL متسلسلة لعمليات قبول العرض، والدفع العادي، والتحصيل النقدي من السائق، وتعديل المحفظة؛ لذلك لا يمكن حفظ نصف عملية عند وصول طلبين متزامنين.
- أضيف `RowVersion` للمحفظة، وقيد يمنع الرصيد السالب، وقيود تفرض أن قيم حركات المحفظة والمدفوعات موجبة وأن أسعار الرحلات غير سالبة.
- أصبح لكل رحلة سجل دفع نهائي واحد فقط على مستوى قاعدة البيانات، كما لا تسمح خدمة الدفع بتعديل عملية نهائية أو ملغاة أو مستردة.
- أُنشئت migration `20260912193419_Stage3FinancialIntegrity` بصورة متوافقة مع قواعد التطوير التي أُنشئت سابقاً؛ فهي تضيف ما ينقص فقط عند اعتماد مخطط قائم.
- أُخذت نسخة احتياطية قبل اعتماد سجل migrations، واجتازت النسخة الأحدث فحص السلامة باستخدام CHECKSUM.
- بعد فحص البيانات القائمة (لا تكرار دفع نهائي، ولا رصيد سالب، ولا قيم مالية مخالفة)، تم اعتماد المخطط القديم بأمان وتطبيق الترقية. التحقق النهائي: 5 migrations، و5 قيود مالية، و`RowVersion` للمحفظة، وفهرس الدفع النهائي الفريد.
- حُدث Postman بطلب `DriverCashPaymentModel / retry with same key` لاختبار إعادة الطلب بالمفتاح نفسه من دون إنشاء تحصيل جديد.
- لم تُطبع أو تُسجل أي كلمات مرور أو مفاتيح أو نصوص اتصال ضمن التنفيذ أو هذه الوثائق.

## المرحلة 3.1: جرد المخطط والاستخدام الفعلي — 2026-09-12

- تم جرد 24 جدول تشغيل ومطابقتها مع API ولوحة الإدارة وتطبيق العميل وتطبيق السائق، دون تعديل أو حذف بيانات.
- وثقت النتيجة في `DATABASE_SCHEMA_AUDIT.md`: 17 جدولاً مستخدماً مباشرة، و7 جداول محفوظة لقرارات الدفع المتقدم أو السلامة أو تتبع المسار.
- لا توجد حالياً خدمة API أو شاشة مباشرة لجداول البطاقات والدفتر المحاسبي وتسويات السائق وتسجيلات الطوارئ ونقاط المسار؛ لذلك تم تصنيفها «مؤجلة القرار» وليس «غير لازمة».

### قرارات المنتج المضافة

- جداول الدفتر المحاسبي والتسويات ستُربط بتحصيل السائق النقدي، وفروق الزيادة والنقص، وعمولة المنصة، ومستحقات السائق.
- `EmergencyRecordings` جزء معتمد من إجراءات أمان الرحلة، إلى جانب مشاركة الرحلة وأرقام الطوارئ والتسجيل الصوتي.
- `LocationUpdates` سيحفظ السجل التاريخي لمواقع العميل والسائق والمسار؛ أما `DriverLiveLocations` فيبقى للموقع الحي الأخير على الخريطة.

## تكامل مسار الرحلة: الدفعة 1 — 2026-09-12

- ثُبّت تسلسل Flutter القائم من اختيار الخدمة والمواقع والسعر حتى عروض السائق والقبول والتتبع والمحادثة والوصول إلى شاشة الدفع؛ لم يتغير ترتيب أي شاشة ولم يُنفذ الدفع.
- أضيف عقد `RideLocationModel` عبر `/api/execute` لحفظ نقاط موقع العميل أو السائق المرتبطة بالرحلة، مع التحقق من هوية طرف الرحلة ومنع التحديث بعد الإنهاء أو الإلغاء، وإتاحة قراءة السجل للطرفين فقط.
- أصبح `RideModel/get` يعيد بيانات السائق المعين وموقعه الحي، إضافة إلى الرحلة والعروض؛ وحُمي تحديث موقع السائق بحيث لا يحدّثه إلا السائق نفسه أو الإدارة.
- أضيفت إشعارات فعلية عند وصول عرض جديد للعميل، وقبول العرض للسائق، وتغير حالة الرحلة أو إلغائها للطرف الآخر.
- رُبط تطبيق العميل بـ `RideMessageModel` و`NotificationModel` بدلاً من الرسائل والإشعارات الثابتة، وبـ`RideLocationModel` لإرسال موقع العميل أثناء الرحلة، وبـ`RideModel/get` لجلب موقع السائق الحقيقي دورياً لأن API لا يحتوي Hub SignalR فعّالاً حالياً.
- أصبحت أخطاء قبول العرض ورفضه وإلغاء الرحلة تُعاد للواجهة بدلاً من الاستمرار محلياً كأن العملية نجحت.
- التحقق: `dotnet build YemenDrive.sln --no-restore` نجح؛ و`flutter analyze --no-pub` بلا أخطاء تجميع، مع ملاحظات lint قديمة وموجودة مسبقاً.

## تكامل مسار الرحلة: الدفعة 2 — 2026-09-12

- أضيف `IdempotencyKey` لطلبات الرحلة في `RideModel` و`Rides` مع فهرس فريد لكل عميل؛ إعادة إرسال طلب العميل نفسه تعيد الرحلة القائمة بدلاً من إنشاء طلب جديد.
- أُنشئت وطُبقت migration `RideRequestIdempotency` على قاعدة العمل بنجاح.
- أصبح العميل يمرر مفتاحه الموجود بالفعل إلى API، وأصبحت استجابات رفض/قبول العرض أو الإلغاء تُعامل كفشل واضح في التطبيق.
- تُحدّث المحادثة كل 4 ثوان والإشعارات كل 10 ثوان ببيانات API الفعلية؛ هذا بديل متوافق ومحدود لحين تفعيل Hub لحظي في الخادم.

## تكامل مسار الرحلة: الدفعة 3 — 2026-09-12

- أصبح ملخص المسار يعرض عنوانَي الانطلاق والوجهة الفعليين، والمسافة المحسوبة من خط القيادة، والسعر الفعلي المتفق عليه؛ أزيلت قيم الوجهة والمسافة والتكلفة الثابتة من هذه الواجهة.
- تحفظ عملية إنشاء الرحلة مسافة المسار في الطلب، ويستخدم زر الاتصال رقم السائق المعاد من API بدلاً من شاشة اتصال تجريبية.
- أزيلت سجلات تشخيص استجابات خدمة الخرائط كي لا تظهر تفاصيل غير لازمة في سجل التطبيق.
- حُدثت مجموعة Postman بطلب إرسال موقع العميل المرتبط بالرحلة، وطلب إتمام الرحلة قبل شاشة الدفع، مع تضمين مفتاح منع التكرار في إنشاء الرحلة. تم التحقق من صحة ملف المجموعة.

## مسار السلامة: تسجيل صوتي مباشر — 2026-09-12

- استُبدل زر التسجيل الشكلي بتسجيل صوت سلامة حقيقي مرتبط برحلة نشطة، ولا يبدأ إلا بعد نافذة موافقة صريحة في كل رحلة وصلاحية الميكروفون من نظام التشغيل.
- يرسل التطبيق الصوت كمقاطع PCM صغيرة ومصادق عليها إلى API أثناء التسجيل. لا تُحفظ المقاطع ضمن الملفات العامة ولا ينتج عنها رابط تنزيل مباشر.
- ينشئ API جلسة تسجيل مملوكة لصاحب الرحلة فقط، ويمنع البدء للرحلات المنتهية أو الملغاة أو من طرف غير مشارك. عند الإغلاق يحفظ المدة وبصمة SHA-256 لمحتوى المقاطع.
- أضيفت حقول الموافقة وحجم البيانات وآخر مقطع إلى `EmergencyRecordings`، وفهارس جلسات التسجيل، عبر migration `20260912204456_SafetyRecordingStreaming` التي طُبقت على قاعدة التطوير.
- عند تعذر رفع مقطع يتوقف التسجيل ويظهر خطأ صريح؛ لا يدّعي التطبيق أن التسجيل حُفظ. أي جلسة قديمة غير مغلقة تُختم قبل بدء جلسة جديدة كي لا تختلط المقاطع.
- أضيفت صلاحية الميكروفون ووصفها على Android وiOS، واعتمد Android API 23 كحد أدنى لتوافق مكتبة التسجيل.
- حُدث Postman بطلب بدء جلسة بموافقة صريحة وطلب إغلاقها، واجتاز ملف المجموعة فحص JSON.
- التحقق: نجح `dotnet build YemenDrive.sln --no-restore` بلا أخطاء أو تحذيرات، و`flutter analyze --no-pub` بلا أخطاء تجميع. لم تُطبع أو تُسجل أي مفاتيح أو كلمات مرور أو إعدادات اتصال.

## حالة الإغلاق قبل اختبار دورة الرحلة — 2026-09-12

- الربط البرمجي بين API وتطبيق العميل مكتمل حتى شاشة الدفع: إنشاء الرحلة، العروض والقبول، تعيين السائق، تحديث الحالة، الموقع، المحادثة والإشعارات. الدفع نفسه خارج هذا المسار.
- لم يُنفذ بعد اختبار تشغيلي كامل بحساب عميل مستقل وحساب سائق مستقل، ولم تُنفذ اختبارات التزامن الفعلية ضد API؛ هذه هي خطوة الإغلاق التالية وليست نتيجة منجزة.
- جهزت Postman طلب إعادة إنشاء الرحلة بالمفتاح نفسه `rideIdempotencyKey` للتحقق من عدم إنشاء رحلة ثانية. تبقى اختبارات عرضين متزامنين وتغيير حالة غير مسموح ضمن تنفيذ الاختبار القادم.
- مسار السلامة: التسجيل الصوتي مرتبط بالخادم، أما جهات الطوارئ وبلاغ الطوارئ ورابط المشاركة الحي المحدود المدة فلا تزال أعمالاً لاحقة منفصلة.

## اختبار تكامل API لدورة الرحلة — 2026-09-13

- أُجري اختبار تشغيلي معزول على قاعدة التطوير بحساب عميل وحسابَي سائق وبيانات خدمة موسومة للاختبار؛ لم تُحذف أي بيانات قائمة ولم تُطبع رموز دخول أو كلمات مرور أو إعدادات اتصال.
- نجح إنشاء رحلة وإعادة إرسال الطلب بالمفتاح نفسه، وتأكد أن الإعادة أعادت الرحلة نفسها بدلاً من إنشاء رحلة ثانية.
- نجح إرسال عرضين من سائقين متوازيين، ثم تشغيل طلبي قبول متزامنين. كانت النتيجة قبول عرض واحد فقط ورفض المنافس، كما هو مطلوب.
- نجحت حماية الحالة: رُفضت محاولة العميل تغيير الحالة التشغيلية، ونجحت انتقالات السائق المعين: في الطريق ثم بدأت ثم مكتملة.
- نجحت في الاختبار بيانات موقع السائق والمحادثة واسترجاع تفاصيل الرحلة بعد اكتمالها، ووصلت الرحلة إلى حد شاشة الدفع من دون تنفيذ دفع.
- هذا اختبار API فعلي، وليس بديلاً عن اختبار واجهة تطبيق العميل وتطبيق السائق على جهازين؛ الاختبار الواجهاتي والأذونات الفعلية للموقع والميكروفون ما زال مطلوباً.

### فحص المحادثة والإشعارات

- تأكد أن API يمنع المحادثة قبل قبول العرض وتعيين السائق، ثم يسمح بها بعد التعيين فقط.
- اجتاز الاختبار إشعار العرض للعميل، وإشعار قبول العرض للسائق، واسترجاع رسالة المحادثة للطرف الآخر بعد التعيين.
- فُحص تطبيق العميل وتطبيق السائق بتحليل Flutter؛ لا توجد أخطاء تجميع في أي منهما.
- صُححت مجموعة Postman بحيث تستخدم طلبات إرسال العرض وحالات السائق رمز السائق، بينما يبقى القبول وموقع العميل برمز العميل. أضيف طلب يثبت رفض حالة السائق من العميل، ثم اجتاز الملف فحص JSON.

## Seeder لبيانات التطوير — 2026-09-13

- أضيف `DevelopmentDataSeeder` وendpoint إداري خاص ببيئة التطوير فقط لتعبئة قاعدة التطوير ببيانات مترابطة؛ لا يتاح خارج بيئة التطوير ويتطلب حساب إدارة.
- الـSeeder غير هدمي وقابل للتكرار: لا يحذف أو يستبدل سجلات موجودة، ويستخدم علامات `DEV-SEED` ومفاتيح ثابتة لتجنب تكرار بياناته.
- يضيف أنواع خدمات وخدمات وتسعيراً وحساب عميل وسائقين وملفات سائق ومواقع حية وأماكن مفضلة ورحلات بحالات البحث والتفاوض والنشاط والاكتمال، إضافة إلى عروض ورسائل وإشعارات تجريبية.
- شُغّل على قاعدة التطوير بنجاح ثم شُغّل مرة ثانية للتحقق من idempotency بنجاح. لا يتضمن التقرير أي بيانات دخول أو إعدادات اتصال.
- حُدث Postman بطلب `Seed Development Data (development only)` باستخدام رمز الإدارة، وتحقق JSON للملف بنجاح.

## حالة التوقف والخطوة التالية — 2026-09-13

- اختبار تكامل API لدورة الرحلة مكتمل وناجح، وتحليل Flutter لتطبيقي العميل والسائق بلا أخطاء تجميع.
- **لم يُنفذ اختبار واجهات التطبيقين فعلياً بعد**؛ لا ينبغي اعتباره مكتملًا لمجرد نجاح API أو التحليل الساكن.
- عند استئناف العمل: شغّل تطبيق العميل وتطبيق السائق بحسابين مختلفين، ثم اختبر من الواجهات إنشاء الرحلة، ظهورها للسائق، العرض والقبول، الموقع والخرائط، المحادثة والإشعارات، انتقال الحالات حتى شاشة الدفع، وصلاحية التسجيل الصوتي على جهاز فعلي.
- لا تنفذ الدفع ضمن هذا الاختبار. استخدم Seeder للتطوير عند الحاجة، فهو غير هدمي وقابل للتكرار.

## بدء اختبار واجهات Android — 2026-09-13

- بدأ تجهيز اختبار الواجهتين على محاكي Android، وتأكد اتصال المحاكي وبدء تشغيل API التطوير.
- عُثر على تعارض يمنع اختبار العميل والسائق معاً: كان التطبيقان يستعملان معرّف Android نفسه. عُدل معرّف تطبيق السائق في إعداد تطويره إلى معرّف مستقل كي يمكن تثبيت التطبيقين بالتوازي؛ لا أثر لذلك على API أو بيانات الإنتاج.
- اجتاز تحليل Flutter للعميل والسائق بعد التعديل بلا أخطاء تجميع.
- بناء APK للعميل بدأ لكنه لم يكتمل بعد بسبب وقت Gradle على الجهاز؛ لذلك لم يُسجل حتى الآن أي اختبار واجهة أو قبول أذونات أو تسجيل دخول. الاستئناف: انتظار اكتمال البناء، تثبيت التطبيقين، ثم يتولى المستخدم تسجيل الدخول يدوياً قبل متابعة الاختبار المرئي.

### تقدم اختبار المحاكي — 2026-09-13

- تأكد اتصال المحاكي، وثُبت APK العميل وفتحت شاشة تسجيل الدخول بنجاح؛ لا يوجد تعطل في الإقلاع.
- ظهر أن APK السائق القديم، الذي بُني قبل فصل المعرّف، كان يستبدل نسخة العميل عند تثبيته. هذا أثر بناء قديم وليس نتيجة لاختبار مسار التطبيق؛ بدأ إعادة بناء نظيفة للعميل والسائق قبل إعادة تثبيتهما معاً.
- لم تُدخل أي بيانات تسجيل دخول ولم تُقبل أي صلاحية موقع أو ميكروفون. يبقى تسجيل الدخول والأذونات الحساسة للمستخدم أو لموافقة صريحة منه.
- اكتمل البناء النظيف وثُبت التطبيقان معاً على المحاكي بمعرّفين منفصلين. فُحص إقلاع كل تطبيق ووصوله إلى نشاطه الرئيسي بنجاح.
- صُحح مسار إقلاع Android لتطبيق السائق ليتوافق مع معرّفه الجديد؛ كان التعديل السابق للمعرّف كافياً للبناء لكنه لم يحدّث فئة النشاط الرئيسي، فكانت نسخة السائق لا تقلع. لا توجد بيانات حساسة أو إعدادات اتصال ضمن هذا التعديل.
- لم يبدأ اختبار دورة الرحلة الواجهاتي بعد لأنه يتطلب تسجيل دخول يدوي بحسابي العميل والسائق؛ لا تدخل الأداة بيانات اعتماد ولا تقبل أي أذونات حساسة تلقائياً.

### تصحيح رسالة تسجيل الدخول — 2026-09-13

- عُولجت استجابة تسجيل الدخول الفاشلة في تطبيق العميل: كان التطبيق يحاول قراءة رموز جلسة من رد API الفاشل، فيعرض رسالة تقنية مضللة بدلاً من رسالة سبب الرفض.
- أصبح التطبيق يعرض رسالة API الآمنة للمستخدم (مثل خطأ بيانات الدخول) مع الإبقاء على رد OTP أو رمز الجلسة الصحيحين كما هما.
- بُني APK العميل المحدّث وثُبت وشُغل على المحاكي بنجاح. لم يلزم تعديل API أو Postman.

### توحيد صيغة رقم الهاتف — 2026-09-13

- تأكد وجود اختلاف بين لوحة التحكم وتطبيق العميل: اللوحة وبيانات التطوير تحفظ الرقم المحلي، فيما كان العميل يضيف مفتاح اليمن عند الإرسال.
- وحّدت تطبيق العميل على الرقم المحلي في تسجيل الدخول وإنشاء الحساب واستعادة كلمة المرور، مع بقاء مفتاح الدولة في الواجهة للعرض والإدخال الطبيعي.
- بُني APK العميل وثُبت وشُغل على المحاكي بعد التعديل. لا يلزم تعديل API أو Postman لهذه المعالجة.

### تصحيح قائمة الجنس في الملف الشخصي — 2026-09-13

- عولج تعطل شاشة تعديل الملف الشخصي عند غياب قيمة الجنس أو وصول قيمة غير معروفة من API؛ كانت القائمة تستقبل قيمة فارغة لا تقابل أي خيار.
- أصبحت القيمة غير المعروفة تظهر كحالة غير محددة، ويستطيع المستخدم اختيار قيمة صالحة لاحقاً وحفظها.
- بُني APK العميل وثُبت وشُغل على المحاكي بعد التعديل. لا يلزم تعديل API أو Postman.

### تصحيح الانتقال بعد اختيار الخدمة — 2026-09-13

- عولج خطأ Flutter عند الضغط على التالي بعد اختيار نوع الخدمة؛ كانت شاشة اختيار الموقع تحدّث حالة مراقبة أثناء بناء المسار الجديد.
- أُجل تحديث حالة البدء التلقائي إلى ما بعد اكتمال الإطار مع التحقق من بقاء الشاشة مركبة، لمنع استدعاء بناء متداخل.
- بُني APK العميل وثُبت وشُغل على المحاكي بعد التعديل. لم يُفتح طلب إذن موقع ضمن الاختبار الآلي.

### سياسة ربط Controllers لمسارات الحساب — 2026-09-13

- عولج تعطل `ProfileController not found` بعد تسجيل الدخول: كان مسار الملف الشخصي يستخدم الربط العام بدلاً من `ProfileBinding` المالك للـController.
- صُحح أيضاً ربط مسار العروض، وجُعل `ProfileBinding` قابلاً لإعادة إنشاء Controller ومستودعه بأمان عند العودة إلى المسار.
- أضيفت سياسة مكتوبة لربط Controllers واختبار آلي يحرس ربط مساري الملف الشخصي والعروض؛ اجتاز الاختبار بنجاح.
- بُني APK العميل وثُبت وشُغل على المحاكي. لا يلزم تعديل API أو Postman.

### استكمال حماية Controller داخل الشاشة الرئيسية — 2026-09-13

- بعد إعادة التحقق ظهر أن أيقونة حسابي تعرض الملف الشخصي داخل `MainShell`، فلا تمر عبر مسار الملف الشخصي المنفصل.
- أضيف `ProfileBinding` إلى ربط الشاشتين الرئيسيتين، لذلك يتوفر `ProfileController` قبل بناء الصفحة المدمجة.
- وسّع اختبار ربط المسارات ليغطي الـMain Shell، واجتاز بنجاح. بُني APK العميل وثُبت وشُغل على المحاكي.

### مسار تقديري لاختبار الحجز دون مزود الخرائط — 2026-09-13

- أضيف بديل مسار محصور ببيئة غير الإنتاج وعند غياب إعداد مسارات الخرائط: يرسم خطاً بين نقطتي الانطلاق والوجهة اللتين اختارهما المستخدم، فيحسب التدفق المسافة منه ويُفعّل الانتقال لبقية خطوات الحجز.
- يظهر تنبيه صريح بأن المسار تقديري للتطوير وليس مسار قيادة فعلياً.
- يتعطل البديل تلقائياً عند الإنتاج أو عند إعداد مزود مسارات حقيقي؛ لم يُغيّر API أو Postman.
- بُني APK العميل وثُبت وشُغل على المحاكي.

### تصحيح استلام عروض السائقين في المفاوضة — 2026-09-13

- قُوي قارئ العروض الدورية في تطبيق العميل ليقرأ أي كائن JSON صالح صادر من API بدلاً من الاعتماد على نوع خريطة مقيد قد يمنع ظهور العرض.
- أصبح فشل استعلام التحديث يعرض رسالة واضحة بدلاً من إبقاء شاشة العميل في حالة بحث صامتة.
- بُني APK العميل وثُبت وشُغل على المحاكي؛ لا يلزم تعديل API أو Postman.

### منع وميض العروض المنتهية وتصحيح العدّ — 2026-09-13

- تبين أن واجهة العميل كانت تتلقى سجل العروض كاملاً، بما فيه العرض المنتهي أو الذي تمت معالجته، ثم تحذفه فوراً عند مقارنة تاريخ انتهاء الصلاحية؛ لذلك كان قد يظهر للحظة عند العودة إلى المفاوضة.
- أصبح العميل يتجاهل قبل العرض أي عنصر غير معلّق أو منتهي الصلاحية، ويقرأ حالة العرض وتاريخ إنشائه من API.
- عُدّل شريط العدّ ليعرض الوقت المتبقي الحقيقي للعرض بدلاً من عدّ ثابت مدته 20 ثانية. ولا يوجد رفض تلقائي للعرض السليم قبل موعد انتهائه.
- أضيف تاريخ إنشاء العرض إلى استجابة الرحلة من API. لا يلزم تعديل Postman.
- اجتاز مشروع طبقة التطبيق بناء .NET بنجاح، وبُني APK العميل وثُبت وشُغل على المحاكي. تعذر استبدال ملفات عملية API وهي تعمل، لذا يتطلب نشر إضافة وقت الإنشاء إعادة تشغيل نافذة API لاحقاً؛ أما منع وميض العرض فيعمل من نسخة العميل الحالية.

### تصحيح تفسير توقيت صلاحية العرض — 2026-09-13

- ظهر أن بعض تواريخ UTC الصادرة من قاعدة البيانات لا تتضمن علامة المنطقة الزمنية؛ كان Flutter يفسرها كتوقيت الجهاز، فيعد العرض صالحاً منتهياً عند تقدم توقيت الجهاز على UTC.
- أصبح قارئ عروض العميل يعامل التاريخ غير الموسوم صراحةً كتوقيت UTC قبل مقارنة الصلاحية، مع الحفاظ على رفض العروض المنتهية فعلياً.
- بُني APK العميل وثُبت وشُغل على المحاكي. بقيت ملاحظات التحليل المعلوماتية السابقة فقط، بلا أخطاء بناء. لا يلزم تعديل API أو Postman.

### منع تعطل MEmu بعد قبول عرض — 2026-09-13

- فُحص سجل المحاكي بعد الإغلاق المفاجئ: التعطل حدث في خيط الرسوم لـGoogle Maps (`eglChooseConfig failed`) عند فتح متابعة السائق، وليس في قبول العرض أو API.
- أضيف عرض متابعة بديل مرئي في بيئة التطوير التجريبية فقط، يمنع تهيئة Google Maps غير المتوافقة مع MEmu ويتيح متابعة مسار الرحلة واختبار الحالات والدفع.
- تبقى الخريطة الحية مستخدمة في الإنتاج أو عند إيقاف بيانات التطوير. بُني APK العميل وثُبت وشُغل على المحاكي بنجاح؛ لا تغيير مطلوب في API أو Postman.

### تعميم بديل الخرائط الآمن على MEmu — 2026-09-13

- أكد تعطل ثانٍ بعد تأكيد المسار أن MEmu يفشل في كل محاولة لإنشاء Google Maps أصلية، لا في شاشة متابعة السائق وحدها.
- نُقل بديل الخريطة إلى المكوّن المشترك؛ في بيئة تطوير تجريبية يعرض خريطة تقديرية قابلة للنقر لتعيين نقطتي الانطلاق والوجهة، دون تشغيل Google Maps الأصلية.
- لا ينطبق البديل على الويب أو الإنتاج، فتظل الخريطة الحية هي المسار التشغيلي. بُني APK العميل وثُبت وشُغل على المحاكي؛ لم يُعدّل API أو Postman.

### تصحيح شرط خريطة التطوير مع API محلي — 2026-09-13

- اتضح أن التطبيق يعمل ببيئة `dev` لكنه متصل فعلياً بـAPI المحلي، لذلك كانت بيانات العرض التجريبية معطلة ولم يكن البديل السابق للخرائط يتفعّل.
- أصبح بديل الخريطة الآمن مرتبطاً ببيئة التطبيق غير الإنتاجية، لا بخيار بيانات العرض؛ وعليه لا تنشأ Google Maps الأصلية في MEmu طوال اختبار مسار الحجز المتصل بالـAPI.
- بُني APK العميل وثُبت وشُغل على المحاكي. لا تعديل في API أو Postman.

### حزمة التسعير والنوع الافتراضي — قيد التنفيذ 2026-09-13

- تأكد أن شاشة الدفع الحالية تعرض قيماً ثابتة ولا تستخدم عرض السائق المقبول أو بيانات التسعير؛ ستستبدل ببيانات رحلة مثبتة من الخادم.
- النطاق المصحح: تحدد الإدارة **نوع خدمة افتراضياً واحداً نشطاً**، وليس خدمة تفصيلية افتراضية. عند تفعيل خيار العميل يختار التطبيق ذلك النوع أولاً، ثم يبقى اختيار الخدمة التفصيلية ضمنه جزءاً من الحجز.
- ستضاف رسوم خدمة ثابتة لكل قاعدة تسعير (نوع الخدمة + الخدمة التفصيلية)، وتثبت مع السعر المقبول على الرحلة حتى لا تؤثر تعديلات الإدارة اللاحقة في رحلة قائمة أو مكتملة.
- التنفيذ يتضمن Migration رسمية، API ولوحة الإدارة وتطبيق العميل واختبارات وتحديث Postman عند تغير العقود.

### حزمة التسعير والنوع الافتراضي — مكتملة تقنياً 2026-09-13

- أُنشئت Migration رسمية `DefaultServiceKindAndServiceFee` لإضافة النوع الافتراضي، ورسم الخدمة، وإجمالي الرحلة المثبت مع القيود اللازمة.
- أضيفت حقول النوع الافتراضي ورسوم الخدمة في لوحة الإدارة وعقود API. يضمن الخادم مسح العلامة السابقة عند تعيين نوع نشط جديد افتراضياً.
- عند قبول عرض السائق يثبت الخادم السعر المقبول ورسم الخدمة وإجمالي الرحلة؛ تعرض شاشة الدفع السعر الحقيقي والرسم والإجمالي بدلاً من القيم الثابتة.
- بُني APK العميل وثُبت وشُغل على MEmu بنجاح. لم يتغير شكل طلبات Postman؛ الحقول الجديدة في الاستجابة فقط، لذا لم يلزم تعديل طلباتها.

### زر تعبئة بيانات التطوير — 2026-09-13

- أضيف زر «تعبئة بيانات التطوير» في الإجراءات السريعة للوحة التحكم، مع نافذة تأكيد صريحة بأنه يضيف بيانات تجريبية ولا يحذف البيانات الحالية.
- يستخدم الزر مسار التطوير المحمي الموجود مسبقاً؛ يتطلب جلسة إدارة ولا يتاح خارج بيئة التطوير.
- تم بناء API بنجاح. لا يلزم تعديل Postman لأن المسار كان موثقاً وموجوداً مسبقاً.

### إصلاح تبديل نوع الخدمة الافتراضي — 2026-09-13

- كان زر «تعيين افتراضي» يرسل التعديل لكن القيد الفريد في قاعدة البيانات قد يمنع نقل العلامة من النوع الحالي إلى النوع الجديد عند حفظ التعديلين معاً.
- أصبح الخادم يلغي العلامة من أي نوع افتراضي نشط سابق أولاً، ثم يعيّن النوع المطلوب، وكل ذلك ضمن عملية قاعدة بيانات واحدة قابلة للتراجع عند الفشل.
- أُعيد بناء API بنجاح دون تحذيرات أو أخطاء وأعيد تشغيله. لا يتطلب هذا التعديل تغييراً في Postman أو تطبيق العميل.

### تصحيح إعادة البحث وحالة عرض السائق — 2026-09-13

- أصبحت «إعادة البحث عن سائق» تعيد الاستماع إلى عروض **الرحلة نفسها** فقط؛ لا تنشئ طلب رحلة أو سجلاً جديداً في لوحة الإدارة.
- أضاف تقرير الرحلات حالة آخر عرض خاص بالسائق، فيعرض تطبيق السائق «تم رفض عرضك» أو «انتهت صلاحية عرضك»، ولا يتيح إرسال أو تعديل عرض بعد رفضه أو انتهائه أو إلغاء الرحلة.
- بُني API وتطبيقا العميل والسائق بنجاح. تعذر التثبيت على MEmu لأن المحاكي غير ظاهر كجهاز متصل حالياً؛ ملفات APK جاهزة للتثبيت عند تشغيله. لا تغيير في أجسام طلبات Postman.

### استئناف الرحلة عند إعادة فتح تطبيق العميل — 2026-09-13

- عند تشغيل التطبيق بحساب مسجل، يستعلم العميل من الخادم عن أحدث رحلة غير مكتملة لهذا الحساب بدلاً من الاعتماد على الذاكرة المؤقتة للجهاز.
- تعود الرحلة الباحثة عن سائق إلى شاشة المفاوضة نفسها وتستأنف متابعة عروضها؛ أما الرحلة ذات السائق المعيّن أو الجارية فتفتح شاشة المسار وتستعيد نقاط الرحلة وبيانات السائق وموقعه المتاح.
- لا يستأنف التطبيق الرحلات المكتملة أو الملغاة، ولا ينشئ أي رحلة جديدة أثناء الاستئناف. بُني APK العميل بنجاح؛ لم يتطلب الأمر تعديل API أو Postman.

### المرحلة التالية المتفق عليها: الدفع والمحفظة والتسوية — 2026-09-13

- الدفع النقدي: يستلم السائق إجمالي المبلغ، وتسجل عمولة المنصة مديونية على حسابه. يضاف الدفع الزائد إلى محفظة العميل، ويخصم العجز من محفظته عند كفاية الرصيد.
- الدفع من محفظة العميل أو وسيلة خارجية لاحقاً: يودع صافي استحقاق السائق بعد عمولته، وتسجل رسوم الخدمة وعمولة السائق لمصلحة المنصة.
- العمولة تضبطها الإدارة حسب الخدمة، ويجب أن تدعم نسبة أو مبلغاً ثابتاً أو كليهما. الدفع الجزئي يجب أن يتيح إكمال المتبقي نقداً بشكل واضح، ولا تغلق الرحلة قبل تسويتها.
- الإلغاء بعد الدفع يعيد المتبقي فقط بعد خصم الرسوم المحددة في سياسة الإلغاء. التنفيذ مؤجل بطلب المستخدم إلى الجلسة التالية، ويتضمن Migration وقيود التكرار والمعاملات والاختبارات وتحديث Postman عند تغير العقود.

### الدفع والمحفظة والتسوية — بدء التنفيذ 2026-09-14

- روجع الأساس المالي القائم: يمنع التكرار ويغطي النقدي والزيادة والنقص، لكنه كان يحسب أجرة الرحلة وحدها ولا يثبت عمولة حسب الخدمة أو مديونية المنصة كاملة.
- أضيفت Migration رسمية `FinancialSettlementRules` لعمولة السائق النسبية والثابتة، ورسم الإلغاء، وعمولة السائق المثبتة على الرحلة. لم تطبق على أي قاعدة بيانات ضمن هذه الخطوة.
- عند قبول عرض، تثبت الرحلة الآن رسم الخدمة وعمولة السائق وصافي السائق ومستحق المنصة؛ لا تتأثر هذه القيم بتعديل قاعدة التسعير لاحقاً.
- عدّل التحصيل النقدي ليعتمد إجمالي العميل (الأجرة + رسم الخدمة)، ويقيد مستحق المنصة مديونية تسوية على السائق بدلاً من تجاهله. الزيادة والنقص يظلان مرتبطين بمحفظة العميل داخل المعاملة نفسها.
- بُني API بنجاح دون أخطاء أو تحذيرات. الخطوة التالية ضمن المرحلة: دفع المحفظة والتوزيع غير النقدي والاختبارات.

### الدفع والمحفظة والتسوية — دفعة المحفظة 2026-09-14

- أصبح `PaymentModel` يطبق دفع محفظة يمن درايف فقط بصورة ذرية: يخصم إجمالي العميل المثبت، ويضيف صافي مستحق السائق إلى محفظته، ويسجل عمليتي المحفظة والدفعة وتسوية السائق في معاملة واحدة.
- يتحقق المسار من العميل والسائق والعملة والرصيد وإجمالي الرحلة، ويستفيد من مفاتيح منع التكرار والقيد الفريد للدفعات المعتمدة لمنع الخصم أو الإضافة مرتين.
- أُوقف قبول مزودي الدفع الخارجيين مؤقتاً برسالة واضحة حتى يربطوا بتحقق حقيقي؛ لا يمكن أن تصبح دفعة خارجية معتمدة بطلب من العميل وحده.
- تم التحقق ببناء API بنجاح. ما يزال مطلوباً ربط واجهات العميل والسائق، وإدارة سياسة الإلغاء، واختبارات التكامل والتزامن قبل إعلان المرحلة مكتملة.

### تطبيق Migration وربط العميل بالمحفظة — 2026-09-14

- طُبقت Migration `FinancialSettlementRules` بنجاح على قاعدة التطوير، ثم شُغّل API بالنسخة الجديدة.
- أصبحت شاشة الدفع في العميل تقرأ إجمالي الرحلة ورصيد المحفظة من API بدلاً من القيم الثابتة، وترسل دفع محفظة يمن درايف الفعلي بمفتاح منع تكرار.
- النقدي يبقى مسار تحصيل السائق، وتظهر الطرق الخارجية كغير متاحة حتى ربطها بمزود معتمد.
- نجح بناء API؛ فحص Flutter للشاشات المعدلة أظهر ملاحظات تنسيق وتحذيراً قديماً فقط بلا أخطاء تنفيذية. يتبقى ربط شاشة السائق وسياسة الإلغاء واختبارات التكامل.

### ربط شاشة تحصيل السائق بالتسوية — 2026-09-14

- وسعت استجابة تقرير الرحلات بالحقول المالية المثبتة اللازمة لتطبيق السائق.
- تعرض شاشة التحصيل الآن أجرة الرحلة، رسم الخدمة، إجمالي المطلوب من العميل، عمولة السائق، مديونية المنصة، وصافي مستحق السائق قبل التأكيد.
- يوضح النص أن مديونية المنصة تسجل للتسوية ولا تخصم مباشرة من رصيد محفظة السائق.
- نجح بناء API، وفحص ملف شاشة السائق بلا أخطاء تنفيذية؛ أعيد تشغيل API بالنسخة الجديدة.

### استرداد إلغاء الرحلة المدفوعة من المحفظة — 2026-09-14

- يثبت رسم الإلغاء على الرحلة عند قبول العرض، وطُبقت Migration مستقلة `RideCancellationFeeSnapshot` بنجاح على قاعدة التطوير.
- عند إلغاء رحلة مكتملة ومدفوعة من محفظة يمن درايف، يعيد الخادم للعميل الإجمالي ناقص رسم الإلغاء، وينشئ قيد محفظة استرداد ودفعة عكسية وسجل تسوية عكسي للسائق ضمن معاملة واحدة.
- لا تُعدّل أو تحذف الدفعة الأصلية. استرداد النقد أو أي مزود خارجي يطلب معالجة يدوية موثقة، كما يمنع الاسترداد إذا لم يكن صافي السائق قابلاً للعكس في محفظته.
- بُني API بنجاح وأعيد تشغيله؛ قاعدة البيانات محدثة بلا migrations معلقة.

### اختبارات التكامل المالي وPostman — 2026-09-14

- أضيف مشروع اختبار تكامل مستقل `tests/YemenDrive.FinancialIntegrationTests` يعمل على قاعدة LocalDB مؤقتة فريدة، ولا يقرأ من قاعدة التطوير أو يغيرها، ثم يحذف قاعدة الاختبار عند النهاية.
- نُفذت الاختبارات بنجاح: دفع محفظة يمن درايف، إعادة الإرسال بالمفتاح نفسه من دون خصم ثانٍ، استرداد الإلغاء بعد خصم الرسم، التحصيل النقدي عند الزيادة والعجز، ورفض العجز حين لا يغطيه رصيد المحفظة مع التحقق من بقاء الرحلة والتحصيل دون تغيير.
- أضيف قسم `09 - Financial Payment And Refund` إلى Postman: دفع المحفظة، إعادة الدفع بالمفتاح نفسه، وإلغاء رحلة مكتملة مدفوعة من المحفظة لاسترداد المتبقي. لا يحتوي القسم على رموز أو بيانات اتصال أو كلمات مرور.
- تحقق البناء والتشغيل الفعلي لاختبار التكامل بنجاح. تبقى تجربة واجهة المستخدم العملية على المحاكي، ثم تصميم آلية تسوية مديونيات السائق في لوحة الإدارة قبل اعتبار المسار المالي مغلقاً.

### إصلاح تبديل نوع الخدمة في مسار الحجز — 2026-09-14

- عولجت حالة بقاء خدمات النوع السابق ظاهرة بعد الرجوع واختيار نوع خدمة جديد. كان اختيار الشريحة يغيّر شكل الواجهة فقط ويؤجل تغيير معرف النوع الفعلي إلى زر البدء.
- أصبح اختيار النوع يثبت معرفه فوراً، ويلغي اختيار الخدمة السابق، ويفرغ القائمة المخزنة، ويلغي أي طلب تحميل قديم ثم يحمّل خدمات النوع الجديد فقط.
- اجتاز فحص Flutter بلا أخطاء تنفيذية؛ بقيت ملاحظات lint تنسيقية فقط. بُني تطبيق العميل وثُبتت النسخة المعدلة على MEmu للتجربة اليدوية.

### تحصين اختيار نوع الخدمة بعد تجربة المحاكي — 2026-09-14

- تأكد بالاستدعاء المباشر أن API يفصل الكتالوج فعلياً: نوع النقل يعيد خدمات النقل فقط، ونوع التوصيل يعيد خدمة التوصيل فقط.
- عولج سباق محتمل كان يسمح لاستجابة النوع الافتراضي المتأخرة بالكتابة فوق اختيار العميل اليدوي.
- أضيفت فلترة دفاعية في العميل قبل العرض؛ لا تقبل القائمة أي خدمة لا يطابق `serviceKindId` فيها النوع المحدد حالياً، حتى لو وصلت استجابة قديمة أو خاطئة.
- أصبح الدخول من شريط الموقع يمر عبر اختيار النوع المعتمد بدلاً من تجاوز التحقق. بُني التطبيق وثُبتت النسخة الجديدة على MEmu بنجاح.

### إنهاء طلب التسعير عند الفشل أو الرجوع — 2026-09-14

- عولجت حالة دوران مؤشر التسعير بلا نهاية عند عدم وجود قاعدة تسعير أو فشل الطلب؛ صار الطلب يخرج دائماً إلى حالة جاهزة أو فشل واضح.
- تعرض شاشة التسعير رسالة مفهومة وزر عودة لاختيار الخدمة عند غياب القاعدة بدلاً من مؤشر مستمر.
- عند الرجوع من شاشة التسعير يُلغى الطلب الجاري منطقياً، فلا تستطيع نتيجة شبكة متأخرة إعادة تشغيل المؤشر أو تعديل شاشة سابقة.
- فحص Flutter بلا أخطاء تنفيذية (ملاحظات lint تنسيقية فقط)، وبُني التطبيق وثُبتت النسخة المعدلة على MEmu.

### تسوية مديونيات السائقين النقدية من لوحة الإدارة — 2026-09-14

- أضيفت Migration رسمية `DriverSettlementPaymentCollection` لإنشاء سجل تحصيل مستقل، مرتبط بمديونية السائق، مع مرجع فريد وقيد يمنع المبلغ الصفري أو السالب.
- لا تعدّل عملية التحصيل `DriverSettlement` أو الدفعة النقدية الأصلية: تسجل فقط مبلغ التحصيل، والمشرف الذي سجله، والطريقة والمرجع والملاحظة. يدعم التحصيل الجزئي ويرفض المبلغ الأكبر من المتبقي أو المرجع المتكرر.
- أضيفت شاشة «مديونيات السائقين» إلى لوحة الإدارة، وتعرض المستحق والمحصل والمتبقي لكل رحلة نقدية، مع زر تسجيل تحصيل للمديونيات المفتوحة.
- طُبقت الـMigration على قاعدة التطوير، ونجح بناء API، ونجح اختبار التكامل المالي على قاعدة اختبار مستقلة بما فيه تحصيل الدين ومنع تكرار المرجع وحماية القيد الأصلي.
- حدثت مجموعة Postman بقسم `10 - Driver Debt Collection` لعرض المديونيات وتسجيل التحصيل برمز الإدارة. لا تحتوي الإضافة على أي أسرار أو بيانات اتصال.

### تجربة يدوية لمسار الدفع على المحاكي — 2026-09-15

- بدأت تجربة عملية من تطبيق العميل والسائق على MEmu بحسابات التطوير المسجلة، دون عرض كلمات مرور أو رموز أو إعدادات اتصال.
- اجتاز العميل اختيار نوع النقل، إدخال المسار التجريبي، اختيار «سيارة اقتصادية»، واحتساب السعر ثم الوصول إلى «بانتظار عروض السائقين».
- لم تصل الرحلة الجديدة إلى تطبيق السائق حتى بعد إعادة تشغيله؛ ظل يعرض الرحلات المفتوحة القديمة فقط. لذلك لم نتابع القبول وتشغيل الرحلة والدفع، ولا تعد تجربة النقد أو المحفظة مكتملة بعد.
- الخطوة التالية المطلوبة: تشخيص تطابق نوع الخدمة والخدمة الداخلية ومرشح رحلة السائق، ثم إعادة التجربة من نقطة إرسال العرض.

### استئناف تجربة المحاكي ومعالجة الرحلة المفتوحة — 2026-09-15

- اتضح أن الوصول إلى شاشة التسعير لا يرسل الطلب تلقائياً؛ الإرسال خطوة مستقلة. كما كانت رحلة تطوير قديمة مفتوحة تعيد التطبيق إليها وتمنع اختبار طلب جديد بصورة سليمة.
- أغلقت رحلتي تطوير قديمتين غير مدفوعتين فقط بعد التحقق من عدم ارتباطهما بأي دفعة، بهدف بدء اختبار نظيف؛ لم تمس أي قيود مالية.
- عولج العميل بحيث يمنع إنشاء رحلة جديدة عند وجود رحلة مفتوحة ويوجه المستخدم لمسارها الحالي، كما أضيف زر «إلغاء البحث» إلى شاشة انتظار العروض وتصفير حالة الرحلة المحلية بعد الإلغاء.
- اجتاز فحص الملفات المعدلة بلا أخطاء تنفيذية (بقيت ملاحظات تنسيق فقط)، وبُني APK العميل وثُبت على MEmu. تأكدت شاشة المحفظة من قراءة الرصيد عبر API.
- ما زال مطلوباً تكرار الدورة النظيفة من إرسال الطلب ثم عرضه في تطبيق السائق وقبوله وصولاً إلى الدفع؛ لم تسجل نتيجة نجاح غير مثبتة لهذه المراحل.

### تشخيص عدم وصول طلب العميل إلى السائق — 2026-09-15

- تأكد أن مرشح السائق لا يستخدم القرب الجغرافي حالياً؛ يعتمد فقط على أن تكون الرحلة بحالة بحث/تفاوض، بلا سائق مسند، وأن يطابق كل من `ServiceKindId` و`ServiceCatalogItemId` ملف سائق متاح.
- راجعت قاعدة التطوير بعد تجربة المحاكي: لم يُنشأ سجل رحلة جديد؛ السجلات المفتوحة السابقة فقط هي الموجودة. لذلك لم يصل الأمر بعد إلى مرحلة مطابقة السائقين أو النوع الافتراضي.
- كشف سجل المحاكي أن العميل كان يخفي رفض الخادم برسالة عامة `Invalid ride response` ويترك واجهة البحث في حالة مضللة.
- أصبح العميل يعرض سبب الرفض القادم من API، ولا يبدأ حالة البحث إلا بعد استلام رقم رحلة حقيقي. سجل التطوير يطبع معرّفي النوع والخدمة وسبب الرفض فقط، من دون رمز أو عنوان أو إحداثيات أو بيانات اتصال.
- عند غياب عنوان نصي من مزود الخرائط التجريبي، يرسل العميل تسميتين عامتين آمنتين لنقطتي الانطلاق والوجهة، مع بقاء الإحداثيات المحددة؛ وذلك لأن API يطلب حقلي العنوان.
- اجتاز `flutter analyze` بلا أخطاء تنفيذية (ملاحظات تنسيق فقط)، وبُني APK debug وثُبت على MEmu. لا تغيير في عقد API أو Postman.

### معالجة جلسة العميل التالفة على المحاكي — 2026-09-15

- أظهر سجل MEmu فشل فك تشفير مخزن جلسة Android؛ لذلك لم يكن طلب الرحلة يحمل تفويضاً صالحاً، وهو سبب مستقل عن مطابقة السائق أو النوع الافتراضي.
- أصبح مخزن العميل يتعامل مع مفاتيح Android Keystore غير القابلة للفك بأمان: يحذف الجلسة غير الصالحة ويعيد التطبيق إلى تسجيل الدخول، ولا يرمي استثناءً أو يبقي حالة دخول وهمية.
- بُني APK debug وثُبت، ثم أزيلت بيانات العميل المحلية في MEmu فقط لرفع الجلسة التالفة. لا تغيير في حسابات أو بيانات الخادم أو السائق. يلزم تسجيل دخول العميل من جديد قبل استئناف الاختبار.

### التحقق الفعلي من الجلسة عند بدء تطبيق العميل — 2026-09-15

- أصبح بدء التطبيق يراجع الجلسة المحفوظة لدى API عبر عملية الملف الشخصي المحمية؛ وجود نص في المخزن لم يعد كافياً لاعتبار المستخدم مسجلاً.
- إذا رفض الخادم الجلسة أو لم يعد الحساب صالحاً، تمسح الرموز المحلية وتتحول حالة التطبيق إلى تسجيل الدخول تلقائياً. أما انقطاع الشبكة أو الاستجابة غير القابلة للقراءة فلا يسجلان خروجاً قسرياً.
- أعيد ترتيب تهيئة الخدمات لتصبح شبكة API جاهزة قبل فحص الجلسة. اجتاز تحليل الملفات بلا ملاحظات، وبُني APK debug وثُبت على MEmu. لا تغير عقد API أو Postman.

### إصلاح محادثة وبطاقة الرحلة في تطبيق السائق — 2026-09-15

- أصبحت فقاعة المحادثة تعتمد على `senderId` القادم من الخادم ومطابقته بحساب السائق: رسالة السائق في يمين واجهة RTL ورسالة العميل في الجهة المقابلة.
- أزيل التكرار في زر «اتصال بالعميل» أثناء حالة الرحلة الجارية؛ يبقى زر واحد فقط مع زر المحادثة والتحصيل.
- اجتاز فحص Flutter بلا أخطاء تنفيذية (ملاحظات تنسيق قائمة فقط)، وبُني APK debug لتطبيق السائق وثُبت على MEmu. لا تغيير في API أو Postman.

### تحصين إغلاق الدفع النقدي وتوفر السائق — 2026-09-15

- عولج ظهور «تأكيد ودفع 0»: شاشة العميل تستخدم الآن أجرة الرحلة المثبتة مع رسم الخدمة كبديل عندما تكون `TotalAmount` فارغة في رحلات تطوير قديمة.
- زر الدفع النقدي لم يعد رسالة إرشادية فقط؛ يتحقق من قيد دفع نقدي معتمد سجله السائق للرحلة. عند وجوده يؤكد الإكمال، وعند غيابه يوضح أن السائق لم يسجل التحصيل بعد.
- عند تحصيل نقدي زائد أو ناقص، ينشئ الخادم إشعار دفع للعميل بالتزامن مع قيد المحفظة. تحققنا من الرحلة التجريبية الأخيرة: أُضيفت الزيادة 100 فعلياً إلى محفظة العميل؛ لم يكن ينقصها قيد مالي بل إشعار وقراءة متجددة في الواجهة.
- لا تظهر مديونية سائق إن كانت رسوم الخدمة وعمولة السائق في قاعدة التسعير صفراً. القاعدة النشطة لخدمة السيارة الاقتصادية الحالية مضبوطة بصفر في الحقلين، ولذلك لا يمثل غيابها من شاشة الإدارة خللاً؛ الزيادة النقدية تعود للعميل وليست مديونية.
- أصبح السائق الذي لديه رحلة مسندة أو في الطريق أو جارية مستبعداً من قائمة الطلبات الجديدة وإشعاراتها، ويُرفض عرضه أيضاً كحماية إضافية.
- نجح بناء API بلا تحذيرات أو أخطاء. اجتاز فحص Flutter بلا أخطاء تنفيذية (ملاحظات تنسيق فقط)، وبُني APK العميل وثُبت على MEmu؛ أعيد تشغيل API في نافذة Console ظاهرة. لا حاجة لتحديث Postman لأن عقد API لم يتغير.

### تصحيح محاسبي مطلوب قبل اعتماد النقد — 2026-09-15

- **تصحيح مهم:** معالجة الزيادة النقدية الحالية غير مكتملة محاسبياً. فهي تضيف الزيادة إلى محفظة العميل فقط، بينما يجب أن تسجل أيضاً التزاماً/مديونية مقابلة على السائق لصالح العميل، باعتبارها تحويل قيمة من حساب السائق إلى حساب العميل مرتبطاً بالرحلة.
- لا تعتبر قاعدة «الزيادة ليست مديونية» صحيحة بعد هذا التصحيح؛ المقصود أنها ليست عمولة منصة، لكنها تبقى مديونية على السائق للعميل حتى التسوية.
- أوقف اعتماد هذا الجزء من التدفق المالي مؤقتاً إلى أن يقدّم مالك النظام دورة التحصيل والتوريد المطلوبة. ستعاد نمذجة القيود والحسابات وسجل التسويات بصورة متوازنة قبل أي تعديل لاحق.

### دراسة تصميم الأستاذ العام المالي — 2026-09-15

- اكتملت دراسة فقط، بلا تعديل كود أو قاعدة أو بيانات. توجد نواة قابلة لإعادة الاستخدام في المشروع: `LedgerAccounts` و`JournalEntries` و`JournalLines`، لكنها غير مستخدمة فعلياً في التدفقات المالية الحالية.
- قاعدة التطوير تؤكد أن جداول الأستاذ العام خالية حالياً، بينما توجد محافظ ودفعات وحركات محفظة وسجلات تسوية سائق منفصلة. لذلك يمكن بناء الأستاذ العام فوق النواة القائمة من دون ترحيل أرصدة فعلية الآن أو إنشاء بديل مكرر.
- التصميم المقترح قيد المراجعة مع مالك النظام: جعل القيد المزدوج المنشور مصدراً محاسبياً غير قابل للتعديل، وربط المحافظ والدفعات والتسويات كمستندات تشغيلية/إسقاطات مرتبطة به. لن يبدأ التنفيذ قبل اعتماد دورة التحصيل والتوريد وقواعد الحسابات النهائية.

### اعتماد أسلوب قيود الأستاذ العام — 2026-09-15

- اعتمد مالك النظام تمثيل كل سطر محاسبي بحقلين صريحين: **Debit (مدين)** و**Credit (دائن)**، بدلاً من تخزين مبلغ واحد بإشارة موجبة أو سالبة.
- قاعدة النشر الملزمة: في كل قيد منشور، يساوي مجموع المدين مجموع الدائن بالعملة نفسها، ولا يجوز أن يحمل السطر الواحد مبلغاً مديناً ودائناً في الوقت نفسه أو أن يكون صفراً.
- هذا قرار تصميم فقط؛ لا تغيير في الحركات أو الأرصدة أو الجداول التشغيلية قبل اعتماد دورة التحصيل والتوريد التفصيلية.

### متطلب كشف الحساب في لوحة الإدارة — 2026-09-15

- ستضاف ضمن حزمة الأستاذ العام شاشة إدارة مستقلة اسمها «كشف الحساب»، تستعلم من القيود المنشورة فقط ولا تتيح تعديلها أو حذفها.
- يدعم الاستعلام اختيار حساب واحد أو جميع الحسابات، ونطاقاً زمنياً مرناً: يوم محدد، من تاريخ إلى تاريخ، شهر، أو سنة؛ مع اختيار نوع عملية محدد أو كل الأنواع.
- تعرض النتيجة رصيد البداية، ثم التاريخ والرقم والبيان والمرجع والحساب والعملة والمدين والدائن والرصيد الجاري، مع الإجماليات في النهاية. عند اختيار كل الحسابات تعرض النتيجة مجمعة وقابلة للتصفية بالحساب.
- ستُبنى الفهارس وواجهة API اللازمة للكشف ضمن تنفيذ الأستاذ العام بعد اعتماد دورات التحصيل والتوريد، مع صلاحية مشاهدة الإدارة فقط وسجل تدقيق للعمليات الحساسة.

### دورة النقد والمحفظة المعتمدة مبدئياً — 2026-09-15

- حدّد مالك النظام حسابات العملية الأساسية: حساب السائق، رسوم الخدمة، تحصيل الخدمة، عمولات السائقين، وحساب محفظة العميل؛ ويُضاف حساب وسيلة الدفع الخارجية عند تغذية المحفظة.
- نقد، إجمالي 1800 (أجرة 1500 + رسم خدمة 300) وعمولة سائق 10% من الأجرة = 150: مدين حساب السائق 1800، دائن رسوم الخدمة 300، دائن تحصيل الخدمة 1500، مدين عمولات السائقين 150، دائن حساب السائق 150. صافي أثر حساب السائق مدين 1650.
- نقد زائد 2000 لنفس العملية: القيد السابق مع مدين حساب السائق 2000 وإضافة دائن محفظة العميل 200؛ فيكون صافي أثر حساب السائق مدين 1850. الزيادة حق للعميل والتزام على السائق، وليست إيراد منصة.
- نقد ناقص 1500 مع رصيد محفظة كافٍ: مدين حساب السائق 1500، ومدين محفظة العميل 300، ودائن رسوم الخدمة 300، ودائن تحصيل الخدمة 1500، ثم قيد العمولة (مدين عمولات السائقين 150، دائن حساب السائق 150).
- دفع كامل من محفظة يمن درايف: مدين محفظة العميل 1800، ودائن رسوم الخدمة 300، ودائن تحصيل الخدمة 1500، ثم قيد العمولة نفسه. لا ينشأ تحصيل نقدي على السائق في هذه الحالة.
- تغذية محفظة العميل 500 بواسطة وسيلة خارجية: مدين حساب الوسيلة الخارجية 500، دائن محفظة العميل 500.
- إلغاء بعد دفع: يعكس فقط ما أضيف للسائق من قيد العمولة، ويعاد مبلغ تحصيل الخدمة من حساب تحصيل الخدمة إلى محفظة العميل؛ رسم الخدمة يبقى للمنصة وفق سياسة الإلغاء الحالية.
- هذه قيود عمل معتمدة مبدئياً للتصميم. لا تعديل برمجي أو ترحيل تاريخي قبل تثبيت حالات التشغيل وتوقيت النشر والاستثناءات المتبقية.

### اعتماد حالات النقد وعجز المحفظة وإلغاء الرحلة — 2026-09-15

- التحصيل النقدي المتساوي ينشر قيده فور أن يسجله السائق. لا ينتظر تأكيداً إضافياً من العميل.
- إن غطى العميل جزءاً ناقصاً من الأجرة بمحفظته، ينشأ طلب موافقة وإشعار للعميل أولاً. لا ينشر قيد التحصيل ولا يكتمل الطلب إلا بعد موافقته؛ الرفض يعيد العملية بلا أثر مالي ويظهر للسائق سبب الرفض.
- إن لم يكف رصيد المحفظة، ترفض العملية ولا يسمح بإنشاء رصيد عميل سالب في المرحلة الحالية، ويصل إشعار واضح للعميل. سيضاف لاحقاً، عند اعتماد سياسة مستقلة، دعم العملاء المميزين أو الكوبونات أو الائتمان المسموح.
- عند إلغاء رحلة مدفوعة نقداً يختار العميل أحد مسارين: (1) استرداد المبلغ من السائق نقداً: مدين تحصيل الخدمة ودائن حساب السائق، فتقل المديونية على السائق؛ أو (2) تحويله إلى محفظته: مدين تحصيل الخدمة ودائن محفظة العميل، ويبقى ما قبضه السائق مستحقاً عليه. يعكس قيد عمولة السائق فقط إن كان قد نُشر، وتبقى رسوم الخدمة وفق سياسة الإلغاء.
- لا يبدأ تصميم بوابة الدفع الخارجية الآن؛ ستضاف لها لاحقاً حالات معلّق/ناجح/فاشل/مسترجع وقواعد تسوية مستقلة.

### ربط الحسابات المالي المرن — 2026-09-15

- لن يرتبط جدول الحسابات مباشرة بجداول العملاء أو السائقين فقط. سيكون `LedgerAccount` مستقلاً وقابلاً لإنشاء حسابات النظام، الرسوم، الإيرادات، المصروفات، الصناديق، البنوك، المحافظ الخارجية، أو أي حساب مستقبلي.
- سيضاف كيان ربط عام اختياري (مثل `FinancialParty` وارتباط الحساب به) يحدد نوع الجهة ومعرفها وغرض الحساب. بذلك يمكن للحساب أن يرتبط بعميل أو سائق أو محفظة أو جهة خارجية أو النظام، ويمكن أيضاً أن يبقى حساباً عاماً بلا مالك فردي.
- تفرض الخدمة أن يكون لكل حساب شخصي/محفظة ربط صحيح وفريد بحسب الغرض والعملة، بينما تبقى حسابات رسوم الخدمة وتحصل الخدمة والعمولات حسابات نظام عامة قابلة لإعادة الاستخدام.

### المرحلة الأولى للأستاذ العام وكشف الحساب — 2026-09-15

- نُفذت Migration رسمية `AccountingGeneralLedgerFoundation` وطُبقت على قاعدة التطوير. أضافت `FinancialParties` للربط العام بالعملاء والسائقين والمحافظ والجهات الخارجية مستقبلاً، وطورت جداول الأستاذ القائمة بدلاً من إنشاء بديل مكرر.
- توسع `LedgerAccount` ليحمل الغرض والعملة والحالة وحساباً أباً وملكية مالية اختيارية، مع حماية فريدة للحسابات الشخصية بحسب الجهة والغرض والعملة. يمكن إنشاء حساب نظام بلا مالك أو حساب شخصي مرتبط بأي نوع جهة.
- توسع القيد وسطره برقم قيد، نوع عملية، مصدر تشغيلي، تاريخ حدوث ونشر، عملة، إجمالي مدين/دائن، ارتباط عكسي، رقم سطر، وبيان. أضيفت قيود قاعدة البيانات التي تمنع السطر الصفري أو المدين والدائن معاً، وتتحقق من توازن القيد المنشور.
- أضيفت `AccountingPostingService` كمسار النشر المركزي؛ تعمل بمعاملة متسلسلة، تتحقق من الحسابات والعملات والتوازن ومصدر العملية لمنع التكرار. القيود المنشورة والسطور لا تقبل التعديل أو الحذف؛ التصحيح سيكون بقيد عكسي مرتبط فقط.
- أضيفت شاشة «كشف الحساب» للإدارة: حساب واحد أو الكل، يوم أو نطاق أو شهر أو سنة، نوع عملية أو الكل، مع أرصدة البداية والجارية والختامية والمدين والدائن والمرجع. الشاشة قراءة فقط.
- أضيف زر «تهيئة الحسابات الأساسية» الذي ينشئ الحسابات النظامية الناقصة فقط: ذمم السائقين، تسوية المحافظ الخارجية، التزامات محافظ العملاء، رسوم الخدمة، تحصيل الخدمة، وعمولات السائقين. لا ينشئ قيوداً ولا يغير حساباً موجوداً.
- حدثت Postman بقسم `11 - General Ledger` لتهيئة الدليل وقراءته وطلب كشف حساب. لا تحتوي على أي قيمة سرية.
- التحقق: بناء API ناجح بلا تحذيرات أو أخطاء، Postman صالح JSON، لا تغييرات نموذج معلقة، واختبار تكامل مستقل نجح في تهيئة الدليل ونشر قيد متوازن ومنع التكرار ورفض عدم التوازن ومنع تعديل القيد المنشور وكشف الحساب.
- لم يُربط بعد `DriverCashPayment` أو دفع المحفظة بقيود الأستاذ الجديدة؛ ذلك هو الجزء التالي حتى نضمن أن قيد النقد المتساوي والزيادة والنقص وموافقة العميل والإلغاء تطابق الدورة المعتمدة من دون ازدواجية مع السجلات القديمة.
